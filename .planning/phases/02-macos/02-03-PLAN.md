---
phase: 02-macos
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: []
autonomous: false
requirements: [KERNEL-01, KERNEL-03]
user_setup:
  - service: fingerprint-chromium (兄弟仓库,本地磁盘交接)
    why: "本 phase 上传的是兄弟仓库产出的、已移除 LOG(INFO) 校准诊断行(D-02)的最终 arm64 ditto zip;该产物由兄弟仓库 GSD 流程产出,不在本仓库掌控"
    dashboard_config:
      - task: "确认兄弟仓库 D-02 重建已落地(LOG(INFO) 校准行移除/DLOG 保护),并提供最终 arm64 ditto zip 的本地路径"
        location: "../fingerprint-chromium(git log / 07-01-SUMMARY.md 的 Phase-8 hand-off note / 重跑 regression-cdp.js --mode calibrate 确认无 entropy gate 噪声 LOG)"

must_haves:
  truths:
    - "kernel-149.0.7827.114 release 的 assets 列表包含 `ungoogled-chromium_149.0.7827.114-1.3_macos_arm64.zip`(`gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name'` 命中,KERNEL-01)"
    - "arm64 zip 在上传前经 verify_and_upload_macos_kernel.sh 通过双二进制架构校验(launcher + Framework 均 arm64)、codesign adhoc 存活、本机 CDP 启动冒烟(KERNEL-03)"
    - "上传的 arm64 zip 是兄弟仓库 post-D-02 重建产物(LOG(INFO) 校准诊断行已移除/DLOG 保护),经人工 handoff 确认后才上传(KERNEL-01 edge,Pitfall 6)"
  artifacts:
    - .planning/phases/02-macos/02-03-SUMMARY.md
  key_links:
    - "兄弟仓库 post-D-02 arm64 ditto zip 的本地交接 —— 跨仓库 blocker(STATE.md 已记),LOG(INFO) 存活无法从 zip 内容自动检出,须人工把关(Pitfall 6)"
    - "上传资产名来自 config.CHROME_ENGINE_ZIP_URL_MACOS_ARM64 的 basename(02-02),脚本内不硬编码(SSOT)"
---

<objective>
把兄弟仓库产出的最终(post-D-02)macOS arm64 内核 ditto zip,经 02-01 脚本上传前把关(双二进制架构 + codesign + 本机 CDP 冒烟)后,`gh release upload --clobber` 发布到既有 `kernel-149.0.7827.114` release,关闭 KERNEL-01 与 KERNEL-03 的 arm64 侧。

本 plan 非自主(含人工把关),且依赖兄弟仓库交付 post-D-02 arm64 zip(跨仓库 blocker)。与 x64 上传(02-04)拆开,使 arm64 可先行独立发布、独立验收,不被 x64 的更长 blocker 阻塞(RESEARCH Open Question 2 建议)。

Purpose: KERNEL-01「arm64 内核可从 kernel release 下载」+ KERNEL-03「上传前架构验证 + 启动冒烟」在真实 arm64 产物上的闭环。
Output: kernel release 上出现正确命名的 arm64 资产;02-03-SUMMARY.md。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-macos/02-CONTEXT.md
@.planning/phases/02-macos/02-RESEARCH.md
@scripts/release/verify_and_upload_macos_kernel.sh
</context>

## Artifacts this phase produces

本 plan 不新增代码符号;产出为 kernel release 上的 arm64 资产 + SUMMARY。消费 02-01 的脚本与 02-02 的 config 常量。

## Flagged Assumptions(edge probe:KERNEL-01,unclassified — 未自动消解,须人工把关)

- **A-K01(KERNEL-01 edge,unclassified):** 拿到并上传的 arm64 zip 必须是**兄弟仓库 post-D-02 重建产物**(LOG(INFO) 校准诊断行已移除/DLOG 保护)。本会话 RESEARCH 观察到当前 `build/src/out/Default/Chromium.app`(Framework mtime 2026-07-22)为 pre-D-02 build;该诊断行只在运行期 `--enable-logging=stderr` 且走 gate-skip 码路时触发,**无法从 zip 内容静态检出**(Pitfall 6)。→ 由 Task 1 blocking checkpoint 人工确认后才上传;verifier 不得假定当前 build 即最终产物。

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 确认兄弟仓库 post-D-02 arm64 产物就位(handoff)</name>
  <read_first>
    - .planning/phases/02-macos/02-RESEARCH.md(Open Question 1、Pitfall 6:LOG(INFO) 跨仓库 handoff 风险)
    - .planning/phases/02-macos/02-CONTEXT.md(D-01/D-02:021 基线、发布前须移除校准诊断行再增量重建 —— 重建发生在兄弟仓库)
    - ../fingerprint-chromium/.planning/phases/07-021-mac-oab/07-01-SUMMARY.md(LOG(INFO) 校准行禁令、arm64 已构建并 CDP 验证)
  </read_first>
  <what-built>02-01 已产出上传前把关脚本;02-02 已回填 arm64 URL 常量。本步骤前需确认兄弟仓库交付的最终 arm64 产物就位。</what-built>
  <how-to-verify>
    1. 确认兄弟仓库 D-02 重建已落地:查 `../fingerprint-chromium` git log / `07-01-SUMMARY.md` 的 Phase-8 hand-off note,或重跑其 `regression-cdp.js --mode calibrate` 并确认低熵 canvas 绘制时 stderr 无 entropy-gate 噪声 LOG 行。
    2. 确认最终 arm64 ditto zip(post-D-02)在本地磁盘,记下其绝对路径。
    3. 确认 `gh auth status` 已登录且对 ShengSoft-Tech/Open-Anti-Browser 有 repo scope。
  </how-to-verify>
  <resume-signal>回复「approved,arm64 zip 路径=<绝对路径>」;或说明 D-02 未落地/产物未就绪需继续等待兄弟仓库</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 上传前把关并发布 arm64 内核到 kernel release</name>
  <files>(无仓库文件改动;调用 02-01 脚本执行 verify + gh 上传)</files>
  <precondition>Task 1 已提供最终 post-D-02 arm64 ditto zip 的绝对路径,且该文件存在;`gh auth status` 有效(user_setup)。未满足则 halt。</precondition>
  <read_first>
    - scripts/release/verify_and_upload_macos_kernel.sh(02-01 产出,确认调用签名:位置参数 zip 路径、`--arch arm64`、无 `--dry-run` 即真实上传)
    - .planning/phases/02-macos/02-RESEARCH.md(Pattern 3:`gh release upload --clobber` 幂等;「Confirm current release state before uploading」快照命令)
    - backend/config.py(CHROME_ENGINE_ZIP_URL_MACOS_ARM64 的 basename = 期望资产名)
  </read_first>
  <action>
对 Task 1 给出的 arm64 zip 运行 `bash scripts/release/verify_and_upload_macos_kernel.sh --arch arm64 <arm64-zip-绝对路径>`(不带 `--dry-run`,即真实路径):脚本先做双二进制架构校验 + codesign + 本机 CDP 冒烟,通过后 `gh release upload kernel-149.0.7827.114 <zip> --clobber --repo ShengSoft-Tech/Open-Anti-Browser`。上传后 `gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name'` 确认列表含 `ungoogled-chromium_149.0.7827.114-1.3_macos_arm64.zip`。若脚本在架构/签名/冒烟任一步非零退出,**不重试上传**,记录失败原因并回到 Task 1 排查产物(勿降级校验凑通过)。`--clobber` 先删后传,若上传中断不得假定原资产仍在,须重跑(Pitfall 3)。
  </action>
  <verify>
    <automated>gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name' | grep -F 'ungoogled-chromium_149.0.7827.114-1.3_macos_arm64.zip'</automated>
  </verify>
  <acceptance_criteria>
    - 脚本以退出 0 完成 arm64 verify(双二进制架构均 arm64、codesign adhoc、CDP 冒烟命中 149.0.7827.114)后才上传
    - `gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name'` 输出含 `ungoogled-chromium_149.0.7827.114-1.3_macos_arm64.zip`
    - 上传资产名与 config.CHROME_ENGINE_ZIP_URL_MACOS_ARM64 的 basename 逐字一致
  </acceptance_criteria>
  <done>arm64 内核经上传前把关后已发布到 kernel-149.0.7827.114,资产名正确且可在 release 列表查到,已记录到 SUMMARY。</done>
  <reversibility rating="costly">公开发布 `-1.3` arm64 资产后改名需同步 config.py + 重传 + 通知兄弟仓库对齐(D-08);`--clobber` 使同名重传可行,故 costly 而非 one-way,不插决策 checkpoint。</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工确认 arm64 资产已在 release 且可下载</name>
  <read_first>
    - .planning/phases/02-macos/02-RESEARCH.md(Validation Architecture:上传属 live GitHub API,须 human-gated 确认而非 unittest)
  </read_first>
  <what-built>arm64 内核已经脚本把关并 `gh release upload --clobber` 到 kernel-149.0.7827.114。</what-built>
  <how-to-verify>
    1. 运行 `gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name'`,确认新增 `ungoogled-chromium_149.0.7827.114-1.3_macos_arm64.zip`,命名含明确架构标识 `arm64`(KERNEL-03)。
    2. 可选:`gh release download kernel-149.0.7827.114 -p '*macos_arm64.zip'` 下载到临时目录,`ditto -x -k` 解压确认 .app 可用、Framework 符号链接完整。
  </how-to-verify>
  <resume-signal>回复「approved」确认 arm64 资产已在 release 且命名/可下载正确;或描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 兄弟仓库本地磁盘 → 本仓库上传 | post-D-02 arm64 zip 本地手工交接(非签名通道,同开发者同机,按现状接受) |
| 本仓库脚本 → GitHub Release | 真实 `gh release upload` 把 arm64 资产推到公开下载主机 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | 发布错架构/损坏/pre-D-02 的 arm64 zip | high | mitigate | 上传前脚本双二进制架构 + codesign + CDP 冒烟(02-01);post-D-02 由 Task 1 blocking checkpoint 人工把关(LOG(INFO) 无法静态检出,Pitfall 6);`--clobber` 允许发现后重传修正 |
| T-02-02 | Information Disclosure | 上传环节令牌泄露 | high | mitigate | 复用已认证 gh 会话,脚本无令牌字面量(02-01);SUMMARY 不记录令牌 |
| T-02-03 | Tampering/Spoofing | 交接间被替换的 zip(供应链) | low | accept | 同开发者同机本地交接,按现状接受(D-03/D-05) |
</threat_model>

<verification>
- `gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name'` 含 `ungoogled-chromium_149.0.7827.114-1.3_macos_arm64.zip`
- 两处 blocking checkpoint 均获 approved(D-02 handoff 确认 + 资产落地确认)
</verification>

<success_criteria>
最终 post-D-02 arm64 内核经上传前双二进制架构 + codesign + 本机 CDP 冒烟把关,已发布到 kernel-149.0.7827.114 并以含 `arm64` 架构标识的正确文件名可下载(KERNEL-01 + KERNEL-03 arm64 侧闭环)。
</success_criteria>

<output>
Create `.planning/phases/02-macos/02-03-SUMMARY.md` when done
</output>
