---
phase: 02-macos
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: []
autonomous: false
requirements: [KERNEL-02, KERNEL-03]
user_setup:
  - service: fingerprint-chromium (兄弟仓库,x64 交叉编译产物)
    why: "x64 内核由兄弟仓库补 downloads-macos-x64.ini + 改 target_cpu=x64 交叉编译产出(D-03);研究确认该 ini 与 x64 build 目前均不存在(跨仓库 blocker,STATE.md 已记),本 phase 只能在其交付后上传把关"
    dashboard_config:
      - task: "确认兄弟仓库已补 downloads-macos-x64.ini、完成 x64 交叉编译并产出最终 x64 ditto zip,提供其本地路径"
        location: "../fingerprint-chromium(downloads-macos-x64.ini、flags.macos.gn 的 target_cpu、交叉编译产物)"

must_haves:
  truths:
    - "kernel-149.0.7827.114 release 的 assets 列表包含 `ungoogled-chromium_149.0.7827.114-1.3_macos_x64.zip`(`gh release view ... --json assets` 命中,KERNEL-02)"
    - "x64 zip 在上传前经 verify_and_upload_macos_kernel.sh 通过双二进制架构校验(launcher + Framework 均 x86_64)、codesign adhoc 存活,并经 `arch -x86_64` Rosetta 2 CDP 启动冒烟(响应含 149.0.7827.114,KERNEL-03 + D-04)"
    - "上传文件名含明确架构标识 `x64`,与 config.CHROME_ENGINE_ZIP_URL_MACOS_X64 的 basename 逐字一致(SSOT)"
  artifacts:
    - .planning/phases/02-macos/02-04-SUMMARY.md
  key_links:
    - "兄弟仓库 x64 交叉编译产物交付 —— 当前 downloads-macos-x64.ini 与 x64 build 均不存在(RESEARCH Open Question 2),本 plan 在其交付前处于 blocked;不阻塞 02-03 arm64 先行发布"
    - "Rosetta 2(`arch -x86_64`)在当前 arm64 Mac 上完成 x64 冒烟(D-04);Intel 真机原生验证推迟到 Phase 6"
---

<objective>
待兄弟仓库补齐 downloads-macos-x64.ini 并交叉编译产出 macOS Intel x64 内核 ditto zip 后,经 02-01 脚本上传前把关(双二进制架构 + codesign + Rosetta CDP 冒烟)后 `gh release upload --clobber` 发布到 `kernel-149.0.7827.114`,关闭 KERNEL-02 与 KERNEL-03 的 x64 侧。

本 plan 非自主(含人工把关),且被兄弟仓库 x64 产物阻塞(比 arm64 更长的跨仓库 blocker:downloads-macos-x64.ini 与 x64 build 目前都不存在)。与 02-03 拆开,x64 未就绪不阻塞 arm64 发布(RESEARCH Open Question 2)。

Purpose: KERNEL-02「x64 内核可从 kernel release 下载」+ KERNEL-03「上传前架构验证 + 启动冒烟(x64 走 Rosetta)」在真实 x64 产物上的闭环。
Output: kernel release 上出现正确命名的 x64 资产;02-04-SUMMARY.md。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-macos/02-CONTEXT.md
@.planning/phases/02-macos/02-RESEARCH.md
@scripts/release/verify_and_upload_macos_kernel.sh
</context>

## Artifacts this phase produces

本 plan 不新增代码符号;产出为 kernel release 上的 x64 资产 + SUMMARY。消费 02-01 的脚本(x86_64 Rosetta 分支)与 02-02 的 config 常量。

## Flagged Assumptions(edge probe:KERNEL-02,unclassified — 未自动消解,须人工把关)

- **A-K02(KERNEL-02 edge,unclassified):** x64 上传依赖兄弟仓库先补 `downloads-macos-x64.ini`、改 `target_cpu="x64"` 交叉编译并产出 x64 ditto zip。本会话 RESEARCH 经 `find` 确认该 ini **不存在**、`build/src/out/Release/` 仅含 GN 工具无 Chromium build(STATE.md 记为跨仓库 blocker,进度不在本仓库掌控)。→ 本 plan 在 Task 1 blocking checkpoint 前处于 blocked;x64 未就绪时**不得**用 arm64 产物或 stub 凑数上传,须等待兄弟仓库真实交付。

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 确认兄弟仓库 x64 交叉编译产物交付(handoff)</name>
  <read_first>
    - .planning/phases/02-macos/02-RESEARCH.md(Open Question 2、Environment Availability:x64 build 不存在;D-04:Rosetta 冒烟)
    - .planning/phases/02-macos/02-CONTEXT.md(D-03:downloads-macos-x64.ini 补齐 + 交叉编译归兄弟仓库;D-04:x64 冒烟走 Rosetta)
    - ../fingerprint-chromium/flags.macos.gn(现 `target_cpu="arm64"`;x64 交叉编译需改 —— 在兄弟仓库)
  </read_first>
  <what-built>02-01 脚本含 x86_64 Rosetta 冒烟分支;02-02 已回填 x64 URL 常量。本步骤前需确认兄弟仓库交付最终 x64 产物。</what-built>
  <how-to-verify>
    1. 确认兄弟仓库已补 `../fingerprint-chromium/downloads-macos-x64.ini`、flags 改 `target_cpu="x64"` 并完成交叉编译。
    2. 确认最终 x64 ditto zip 在本地磁盘(post-D-02,与 arm64 同基线),记下绝对路径。
    3. 确认本机 Rosetta 2 可用(`arch -x86_64 /usr/bin/true` 退出 0)且 `gh auth status` 有效。
  </how-to-verify>
  <resume-signal>回复「approved,x64 zip 路径=<绝对路径>」;或说明兄弟仓库 x64 产物未就绪需继续等待(本 plan 保持 blocked,不阻塞 02-03)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 上传前把关(含 Rosetta 冒烟)并发布 x64 内核到 kernel release</name>
  <files>(无仓库文件改动;调用 02-01 脚本执行 verify + gh 上传)</files>
  <precondition>Task 1 已提供最终 x64 ditto zip 的绝对路径且文件存在;Rosetta 2 可用;`gh auth status` 有效。未满足则 halt。</precondition>
  <read_first>
    - scripts/release/verify_and_upload_macos_kernel.sh(02-01 产出,确认 `--arch x86_64` 分支走 `arch -x86_64` Rosetta 冒烟;调用签名)
    - .planning/phases/02-macos/02-RESEARCH.md(Pattern 4:Rosetta CDP 冒烟 cold-start 宽松重试;Pattern 3:--clobber 幂等)
    - backend/config.py(CHROME_ENGINE_ZIP_URL_MACOS_X64 的 basename = 期望资产名)
  </read_first>
  <action>
对 Task 1 给出的 x64 zip 运行 `bash scripts/release/verify_and_upload_macos_kernel.sh --arch x86_64 <x64-zip-绝对路径>`(不带 `--dry-run`):脚本先做双二进制架构校验(launcher + Framework 均 x86_64)+ codesign adhoc + `arch -x86_64` Rosetta 2 CDP 冒烟(轮询 `/json/version` 含 149.0.7827.114,cold-start 宽松重试预算),通过后 `gh release upload kernel-149.0.7827.114 <zip> --clobber --repo ShengSoft-Tech/Open-Anti-Browser`。上传后 `gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name'` 确认列表含 `ungoogled-chromium_149.0.7827.114-1.3_macos_x64.zip`。任一校验步非零退出则不上传、记录原因回 Task 1 排查(勿降级校验)。Rosetta 首启慢属正常(Pitfall 4),不因单次超时误判失败,依赖脚本的重试预算。
  </action>
  <verify>
    <automated>gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name' | grep -F 'ungoogled-chromium_149.0.7827.114-1.3_macos_x64.zip'</automated>
  </verify>
  <acceptance_criteria>
    - 脚本以退出 0 完成 x64 verify(双二进制架构均 x86_64、codesign adhoc、Rosetta CDP 冒烟命中 149.0.7827.114)后才上传
    - `gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name'` 输出含 `ungoogled-chromium_149.0.7827.114-1.3_macos_x64.zip`
    - 上传资产名与 config.CHROME_ENGINE_ZIP_URL_MACOS_X64 的 basename 逐字一致,含架构标识 `x64`
  </acceptance_criteria>
  <done>x64 内核经上传前把关(含 Rosetta 冒烟)后已发布到 kernel-149.0.7827.114,资产名正确且可在 release 列表查到,已记录到 SUMMARY。</done>
  <reversibility rating="costly">公开发布 `-1.3` x64 资产后改名需同步 config.py + 重传 + 通知兄弟仓库对齐(D-08);`--clobber` 使同名重传可行,costly 而非 one-way。</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工确认 x64 资产已在 release 且架构正确</name>
  <read_first>
    - .planning/phases/02-macos/02-RESEARCH.md(Validation Architecture:上传属 live GitHub API,human-gated 确认)
  </read_first>
  <what-built>x64 内核已经脚本把关(含 Rosetta 冒烟)并 `gh release upload --clobber` 到 kernel-149.0.7827.114。</what-built>
  <how-to-verify>
    1. `gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name'` 确认新增 `ungoogled-chromium_149.0.7827.114-1.3_macos_x64.zip`,命名含明确架构标识 `x64`(KERNEL-03)。
    2. 可选:下载并 `ditto -x -k` 解压 + `lipo -archs` 抽查 Framework 二进制为 x86_64。
    3. 若 02-03 已完成,确认 release 同时含 arm64 与 x64 两个 macOS 资产(双架构齐备,双 dmg 命名前提)。
  </how-to-verify>
  <resume-signal>回复「approved」确认 x64 资产已在 release 且命名/架构正确;或描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 兄弟仓库本地磁盘 → 本仓库上传 | x64 交叉编译 zip 本地手工交接(非签名通道,同开发者同机,按现状接受) |
| 本仓库脚本 → GitHub Release | 真实 `gh release upload` 把 x64 资产推到公开下载主机 |
| Rosetta 2 翻译层 → x64 冒烟 | 在 arm64 Mac 上经 `arch -x86_64` 运行 x64 二进制做启动冒烟(D-04,非原生) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | 发布错架构/损坏的 x64 zip(如误传 arm64 产物) | high | mitigate | 上传前脚本双二进制架构断言 expected=x86_64(launcher + Framework)+ codesign + Rosetta CDP 冒烟(02-01);架构不匹配即非零退出不上传 |
| T-02-06 | Denial of Service | Rosetta cold-start 误判冒烟失败 | low | mitigate | 冒烟用宽松重试预算而非固定短 sleep(Pattern 4/Pitfall 4);单次超时不误判 |
| T-02-02 | Information Disclosure | 上传环节令牌泄露 | high | mitigate | 复用已认证 gh 会话,脚本无令牌字面量(02-01) |
| T-02-03 | Tampering/Spoofing | 交接间被替换的 zip(供应链) | low | accept | 同开发者同机本地交接,按现状接受(D-03/D-05) |
</threat_model>

<verification>
- `gh release view kernel-149.0.7827.114 --json assets --jq '.assets[].name'` 含 `ungoogled-chromium_149.0.7827.114-1.3_macos_x64.zip`
- 两处 blocking checkpoint 均获 approved(x64 产物交付确认 + 资产落地确认)
- 本 plan 在兄弟仓库 x64 产物交付前保持 blocked,不阻塞 02-03 arm64 发布
</verification>

<success_criteria>
最终 x64 内核经上传前双二进制架构 + codesign + Rosetta CDP 冒烟把关,已发布到 kernel-149.0.7827.114 并以含 `x64` 架构标识的正确文件名可下载(KERNEL-02 + KERNEL-03 x64 侧闭环)。
</success_criteria>

<output>
Create `.planning/phases/02-macos/02-04-SUMMARY.md` when done
</output>
