---
phase: 02-macos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - tests/test_config_platform.py
autonomous: true
requirements: [KERNEL-01, KERNEL-02]
user_setup: []

must_haves:
  truths:
    - "`config.CHROME_ENGINE_ZIP_URL_MACOS_ARM64` == `f\"{_CHROME_KERNEL_BASE}/ungoogled-chromium_149.0.7827.114-1.3_macos_arm64.zip\"`,即以 `_CHROME_KERNEL_BASE` 开头、含 `-1.3`、含 `_macos_arm64`、以 `.zip` 结尾(KERNEL-01,D-07/D-08)"
    - "`config.CHROME_ENGINE_ZIP_URL_MACOS_X64` == `f\"{_CHROME_KERNEL_BASE}/ungoogled-chromium_149.0.7827.114-1.3_macos_x64.zip\"`,即以 `_CHROME_KERNEL_BASE` 开头、含 `-1.3`、含 `_macos_x64`、以 `.zip` 结尾(KERNEL-02,D-07/D-08)"
    - "两个新常量复用 `_CHROME_KERNEL_BASE`(f-string 拼接),不第二次硬编码 base URL 或 kernel release tag 字符串"
    - "两个新常量不包裹在任何 `if sys.platform` 分支内(平台无关的静态字符串,与 SYSTEM_CHROME_EXECUTABLE 那种平台分支常量不同)"
    - "`test_macos_arm64_kernel_url` 与 `test_macos_x64_kernel_url` 通过;`python -m unittest tests.test_config_platform -v` 全绿,既有 Windows 常量断言(test_windows_system_and_default_executable_values_unchanged)不回归"
  artifacts:
    - backend/config.py
    - tests/test_config_platform.py
  key_links:
    - ".github/workflows/build-release.yml:62 以 `python -c \"from backend.config import CHROME_ENGINE_ZIP_URL\"` 读取内核 URL —— Phase 5 CI macOS job 将以同样方式读取这两个 macOS 常量,故必须是模块级可 import 的静态常量"
    - "backend/config.py 是所有路径/URL 常量的单一事实来源(CLAUDE.md 「所有路径常量从这里导入」);02-01 的发布脚本亦从这里解析上传文件名"
  prohibitions:
    - statement: "禁止把 macOS 内核 URL / revision(-1.3)/ 文件名硬编码在 config.py 之外的任何地方(CI workflow、脚本内联字面量等);内核 URL 的唯一事实来源是 backend/config.py"
      status: flagged-unverified
---

<objective>
在 `backend/config.py` 回填 macOS arm64 与 x64 两条内核下载 URL 常量(Phase 1 D-05 已为此留占位),沿用现有 `_CHROME_KERNEL_BASE`/`CHROME_ENGINE_ZIP_URL` 的 Windows 模式;并在 `tests/test_config_platform.py` 补两条断言。

Purpose: KERNEL-01/KERNEL-02「可从 kernel release 下载」的 config 侧落点 —— 供 02-01 发布脚本解析上传文件名、供 Phase 5 CI macOS job 与运行时 installer 下载路径消费(单一事实来源)。此 plan 完全不受兄弟仓库产物阻塞(常量是 URL 字符串,资产就绪与否不影响其正确性)。
Output: config.py 两个新常量 + test_config_platform.py 两个新测试方法。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-macos/02-CONTEXT.md
@.planning/phases/02-macos/02-PATTERNS.md
</context>

## Artifacts this phase produces

本 plan 新增以下符号(供 plan-review 源对齐:新符号,非漂移):
- backend/config.py 模块级常量:`CHROME_ENGINE_ZIP_URL_MACOS_ARM64`、`CHROME_ENGINE_ZIP_URL_MACOS_X64`
- tests/test_config_platform.py 测试方法:`test_macos_arm64_kernel_url`、`test_macos_x64_kernel_url`

<tasks>

<task type="auto">
  <name>Task 1: config.py 回填 macOS arm64/x64 内核 URL 常量</name>
  <files>backend/config.py</files>
  <read_first>
    - backend/config.py:105-124(要扩展的 `_CHROME_KERNEL_BASE` / `CHROME_INSTALLER_URL` / `CHROME_ENGINE_ZIP_URL` / `FIREFOX_INSTALLER_URL` 块 —— 这是要复用的现有模式本体)
    - backend/config.py:86-99(现有 `if sys.platform == "darwin":` 块,house style:中文注释解释 rationale 紧贴块上方,D-0x 不内联)
    - .planning/phases/02-macos/02-PATTERNS.md(`backend/config.py` 一节:命名约定 `CHROME_<ASSET>_URL[_<PLATFORM>_<ARCH>]`,`_CHROME_KERNEL_BASE` 恒经 f-string 复用,Open Question 3 建议用显式独立常量而非 `platform.machine()` 运行时分支)
    - .planning/phases/02-macos/02-CONTEXT.md(D-05 边界、D-07 命名精确样例、D-08 revision `-1.3` 依据)
  </read_first>
  <action>
在 `CHROME_ENGINE_ZIP_URL`(config.py:117-119)之后、`FIREFOX_INSTALLER_URL` 之前,新增两个模块级常量:`CHROME_ENGINE_ZIP_URL_MACOS_ARM64` 与 `CHROME_ENGINE_ZIP_URL_MACOS_X64`,各以 f-string 复用 `_CHROME_KERNEL_BASE`,文件名分别为 `ungoogled-chromium_149.0.7827.114-1.3_macos_arm64.zip` 与 `ungoogled-chromium_149.0.7827.114-1.3_macos_x64.zip`(D-07 精确样例;revision `-1.3` 标识 021 基线,区分 Windows 现有 `-1.2`,D-08)。命名遵循现有全大写 `CHROME_<ASSET>_URL_<PLATFORM>_<ARCH>` 约定。两常量为平台无关静态字符串,**不**包进 `if sys.platform` 分支(Open Question 3:优选显式独立命名常量,无运行时 `platform.machine()` 分支)。紧贴其上方加一行简短中文注释说明这是 macOS 内核资产、`-1.3` = 021 基线、与 Windows `-1.2` 的区分(沿用该文件中文注释解释 rationale 的 house style)。不改动任何 Windows 现有常量、`_CHROME_KERNEL_BASE`、`ENGINE_METADATA`。
  </action>
  <verify>
    <automated>python3 -c "from backend import config; a=config.CHROME_ENGINE_ZIP_URL_MACOS_ARM64; x=config.CHROME_ENGINE_ZIP_URL_MACOS_X64; assert a.startswith(config._CHROME_KERNEL_BASE) and '-1.3' in a and '_macos_arm64' in a and a.endswith('.zip'), a; assert x.startswith(config._CHROME_KERNEL_BASE) and '-1.3' in x and '_macos_x64' in x and x.endswith('.zip'), x; print('OK', a, x)"</automated>
  </verify>
  <acceptance_criteria>
    - `config.CHROME_ENGINE_ZIP_URL_MACOS_ARM64` == `f"{config._CHROME_KERNEL_BASE}/ungoogled-chromium_149.0.7827.114-1.3_macos_arm64.zip"`
    - `config.CHROME_ENGINE_ZIP_URL_MACOS_X64` == `f"{config._CHROME_KERNEL_BASE}/ungoogled-chromium_149.0.7827.114-1.3_macos_x64.zip"`
    - `grep -c '_CHROME_KERNEL_BASE}/ungoogled-chromium' backend/config.py` >= 4(Windows installer+zip 两条 + 新增 macOS 两条,均复用 base)
    - `git diff backend/config.py` 未改动 `CHROME_ENGINE_ZIP_URL`(Windows -1.2)与 `_CHROME_KERNEL_BASE` 行
  </acceptance_criteria>
  <done>config.py 含两个复用 `_CHROME_KERNEL_BASE` 的 macOS 内核 URL 常量(`-1.3` revision、arch 标识文件名),Windows 常量零改动,已提交。</done>
  <reversibility rating="costly">revision `-1.3` 一旦写入 config.py 并随资产公开发布,改名需同步 config.py + 重传资产 + 通知兄弟仓库对齐(D-08);属 costly 仅标记,非 one-way,不插决策 checkpoint。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_config_platform 补 macOS URL 断言</name>
  <files>tests/test_config_platform.py</files>
  <behavior>
    - test_macos_arm64_kernel_url:`config.CHROME_ENGINE_ZIP_URL_MACOS_ARM64` 以 `config._CHROME_KERNEL_BASE` 开头、含 `-1.3`、含 `_macos_arm64`、以 `.zip` 结尾
    - test_macos_x64_kernel_url:`config.CHROME_ENGINE_ZIP_URL_MACOS_X64` 以 `config._CHROME_KERNEL_BASE` 开头、含 `-1.3`、含 `_macos_x64`、以 `.zip` 结尾
    - 两测试无需 `patch.object(sys, "platform", ...)`(常量平台无关);沿用文件既有 `tearDown` reload 约定即可
  </behavior>
  <read_first>
    - tests/test_config_platform.py:14-17(tearDown 恒 reload 真实平台,避免污染其它测试文件)
    - tests/test_config_platform.py:58-76(现有 `test_macos_engine_metadata_contains_chrome_and_firefox` 的 reload+assert 模式)
    - tests/test_config_platform.py:116-136(现有 `test_windows_system_and_default_executable_values_unchanged` 的精确相等断言风格)
    - .planning/phases/02-macos/02-PATTERNS.md(`tests/test_config_platform.py` 一节:新 URL 常量非 `if sys.platform` 分支,多数新测试无需 platform patch/reload,只需直接属性断言)
  </read_first>
  <action>
在 `ConfigPlatformTests` 内新增 `test_macos_arm64_kernel_url` 与 `test_macos_x64_kernel_url`。因两常量是平台无关静态字符串,不做 `patch.object(sys, "platform", ...)`;直接对 `config.CHROME_ENGINE_ZIP_URL_MACOS_ARM64` / `_X64` 断言:`assertTrue(url.startswith(config._CHROME_KERNEL_BASE))`、`assertIn("-1.3", url)`、`assertIn("_macos_arm64", url)`(x64 对应 `_macos_x64`)、`assertTrue(url.endswith(".zip"))`,并可用 `assertEqual` 断言完整期望字符串。放在文件现有 macOS 冻结态用例区块附近,风格对齐既有方法。不改动既有任何测试方法。
  </action>
  <verify>
    <automated>python -m unittest tests.test_config_platform -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_config_platform.ConfigPlatformTests.test_macos_arm64_kernel_url -v` 退出 0
    - `python -m unittest tests.test_config_platform.ConfigPlatformTests.test_macos_x64_kernel_url -v` 退出 0
    - `python -m unittest tests.test_config_platform -v` 全部用例通过(既有 Windows 零回归)
    - `git diff tests/test_config_platform.py` 仅新增两个方法,未改动既有方法体
  </acceptance_criteria>
  <done>两条新 macOS URL 测试通过,test_config_platform 全绿无回归,已提交。</done>
</task>

</tasks>

<verification>
- `python -m unittest tests.test_config_platform -v` 全绿
- 全量套件抽检(wave 合并前):`python -m unittest discover -s tests -v`(macOS 上受限模块除外,确认 config 侧零回归)
- `python3 -c "from backend.config import CHROME_ENGINE_ZIP_URL_MACOS_ARM64, CHROME_ENGINE_ZIP_URL_MACOS_X64; print(CHROME_ENGINE_ZIP_URL_MACOS_ARM64); print(CHROME_ENGINE_ZIP_URL_MACOS_X64)"` 打印两条以 kernel-149.0.7827.114 结尾目录、`-1.3`、arch 标识、`.zip` 的 URL
</verification>

<success_criteria>
config.py 以单一事实来源方式暴露两条 macOS 内核 URL(arm64/x64,`-1.3` revision,arch 标识文件名,复用 `_CHROME_KERNEL_BASE`),test_config_platform 覆盖并全绿,Windows 现有常量与测试零回归。
</success_criteria>

<output>
Create `.planning/phases/02-macos/02-02-SUMMARY.md` when done
</output>
