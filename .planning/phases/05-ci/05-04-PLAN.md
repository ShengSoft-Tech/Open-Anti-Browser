---
phase: 05-ci
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - .github/workflows/build-release.yml
  - scripts/release/check_version_consistency.py
  - tests/test_version_consistency.py
autonomous: true
requirements: [PKG-02, PKG-05]

must_haves:
  truths:
    - "推 tag 时,若 tag 版本与 `frontend/package.json` 的 `version` 或 `backend/main.py` 的两处 FastAPI `version=` 任一不一致,`build-macos` job 立即失败,不会产出「包名一个版本、应用内显示另一个版本」的包(D-08)。"
    - "`workflow_dispatch` 手动触发时没有 tag,版本取 `frontend/package.json`,校验降级为「package.json 与 main.py 两处必须一致」,job 仍能正常跑完并上传 artifact(D-04 的调试通道不被 D-08 的校验堵死)。"
    - "版本一致性的判定逻辑落在 `scripts/release/check_version_consistency.py` 的纯函数里,由 `tests/test_version_consistency.py` 在两个 CI runner 上单测覆盖,而不是只存在于 yml 的内联脚本里。"
    - "CI 断言 `.app` 主二进制与内嵌内核二进制均为 arm64(沿用 Phase 2 `verify_and_upload_macos_kernel.sh` 已建立的 `lipo`/`file` 架构断言口径,D-14 第 2 条)。"
    - "CI 断言 `frontend/dist` 已完整进包(`Contents/Resources/frontend/dist/index.html` 存在且目录内文件数非零)——这是 `backend/_g.py` 运行时完整性校验的前提,而该校验在 dist **缺失**时是静默跳过的,所以缺失必须靠独立的结构断言抓,不能指望校验自己报错(D-14 第 3 条 / PKG-05)。"
    - "CI 断言 Info.plist 的 `LSMinimumSystemVersion` 不低于 `.app` 内全部 Mach-O 的 `LC_BUILD_VERSION` 最小系统版本上确界,把 RESEARCH 假设 A3 从推断值转成硬门禁。"
    - "CI 用 `.app` 内的真实二进制跑一次 `--backend-only` 冒烟并轮询 `/api/bootstrap`,响应正常才继续 —— 这同时证明 `launch_app.main` 开头的 `_g.py` 运行时校验没有拒绝启动(D-14 第 4 条 / PKG-05)。"
    - "四道新门禁全部排在逐层签名与双层 codesign 验证**之后**、`Create dmg` **之前**,任一失败即中止,不产出 dmg、不上传 artifact。"
  artifacts:
    - path: "scripts/release/check_version_consistency.py"
      provides: "tag / package.json / main.py 三方版本一致性判定与版本号解析"
      exports: ["read_package_version", "read_main_versions", "normalize_tag", "check_version_consistency", "VersionMismatch", "main"]
    - path: "tests/test_version_consistency.py"
      provides: "版本一致性纯函数的回归锁"
    - path: ".github/workflows/build-release.yml"
      provides: "build-macos job 的四道硬门禁步骤"
      contains: "backend-only"
  key_links:
    - from: ".github/workflows/build-release.yml (build-macos / Resolve version)"
      to: "scripts/release/check_version_consistency.py"
      via: "步骤捕获脚本 stdout 作为 APP_VERSION 写入 $GITHUB_ENV,脚本非零退出即中止 job"
      pattern: "check_version_consistency"
    - from: ".github/workflows/build-release.yml (build-macos / Smoke test)"
      to: "launch_app.py:main → backend/_g.py:_7(\"runtime\")"
      via: "跑 .app 内二进制的 --backend-only 模式,启动成功即证明运行时完整性校验通过"
      pattern: "backend-only"
  prohibitions:
    - "不得为了让 macOS 打包跑通而移除、绕过、改写或削弱 `backend/_g.py` 的开源声明完整性校验,也不得修改被它哈希锁定的 `frontend/src/lib/openSourceNotice.js` 与 `frontend/src/App.vue`。这份校验是本项目的反商业滥用机制,不是构建障碍。本 phase 与它的唯一正当关系是**只读地断言它的前提成立**(`frontend/dist` 完整进包、运行时未拒启);若打包流程与它冲突,正确处置是修打包流程并把冲突写进 SUMMARY,而不是动它一行。"
---

<objective>
在 05-03 打通的 `build-macos` job 上补齐 D-08 的版本一致性校验与 D-14 剩余三条硬门禁(架构断言、`frontend/dist` 进包断言、`--backend-only` 冒烟),并顺带把 RESEARCH 假设 A3 转成硬断言。

Purpose: D-14 的立论是「签名过了但一启动就挂」(缺依赖 / QtWebEngine 摆不平 / `_g.py` 拒启)才是这类打包最典型的失败模式 —— 签名验证只证明结构完整,证明不了这包能跑。而 D-08 防的是另一类静默事故:tag 打成 `v0.2.0` 但仓库里三处版本号还停在旧值,发出去的包文件名一个版本、应用内显示另一个版本。两者都只有在真实构建产物上才能验,所以门禁必须长在 job 里。特别值得点出的是 `frontend/dist` 断言:`_g.py` 的运行时校验在 dist **缺失**时是静默跳过的,所以「冒烟通过」并不能反推「dist 进包了」——这两道断言互补,缺一不可。

Output: `scripts/release/check_version_consistency.py` + `tests/test_version_consistency.py` + `build-macos` job 新增两个门禁步骤并替换 `Resolve version` 步骤实现。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ci/05-CONTEXT.md
@.planning/phases/05-ci/05-RESEARCH.md
@.planning/phases/05-ci/05-PATTERNS.md
@.planning/phases/05-ci/05-VALIDATION.md
@.planning/phases/05-ci/05-03-SUMMARY.md
@.github/workflows/build-release.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 版本一致性校验脚本 + 单测 + 接进 Resolve version 步骤(D-08)</name>
  <files>scripts/release/check_version_consistency.py, tests/test_version_consistency.py, .github/workflows/build-release.yml</files>
  <read_first>
    - `.planning/phases/05-ci/05-RESEARCH.md` § Code Examples / **「版本一致性校验」** —— 给出了可直接借用的正则与比对骨架(注意其 `lstrip("v")` 写法有缺陷,见下方 action)
    - `.planning/phases/05-ci/05-CONTEXT.md` **D-08** 原文与 § Claude's Discretion 第四条(实现位置属裁量范围)
    - `CLAUDE.md` § 其他约定 —— 「版本号同时存在于 `frontend/package.json` 和 `backend/main.py` 的两个 FastAPI `version` 字段,升版本时三处一起改」,这是本脚本要守护的契约原文
    - `backend/main.py:20-30` —— 两处 FastAPI `version=` 的实际写法与上下文
    - `frontend/package.json:1-6` —— `version` 字段位置
    - `scripts/release/verify_and_upload_macos_kernel.sh` —— 同目录既有发布工具,是「什么脚本可以入仓」的先例(Phase 2 D-11);也是「失败即中止 + 给出可操作错误信息」的风格参照
    - `.github/workflows/build-release.yml` 的 `build-macos` job `Resolve version` 步骤(05-03 落地的取值版本,本任务替换其实现)
    - `tests/test_config_platform.py` —— 既有 unittest 风格参照
  </read_first>
  <behavior>
    - `normalize_tag("v0.2.0")` 返回 `"0.2.0"`;`normalize_tag("0.2.0")` 返回 `"0.2.0"`;`normalize_tag("vv0.2.0")` 返回 `"v0.2.0"`(只剥一个前导字符,证明用的不是会把前缀当字符集吞掉的实现)。
    - `read_main_versions()` 对当前 `backend/main.py` 返回至少 2 个匹配且全部相同。
    - `read_package_version()` 与 `read_main_versions()[0]` 在当前仓库相等(断言相等,**不断言具体数字** —— 版本号会随发版变化)。
    - tag 模式下三方一致 → `check_version_consistency` 返回该版本号。
    - tag 模式下 tag 与另两处不一致 → 抛 `VersionMismatch`,异常文本里同时出现 tag 值、package.json 值与 main.py 值三者。
    - 非 tag 模式(手动触发)下 package.json 与 main.py 一致 → 返回 package.json 的版本,**不参照 ref_name**。
    - 非 tag 模式下 package.json 与 main.py 不一致 → 抛 `VersionMismatch`。
    - `backend/main.py` 里 semver 形状的 `version=` 匹配少于 2 个 → 抛 `VersionMismatch`,异常文本点名实际匹配数与 CLAUDE.md 的三处同步约定。
  </behavior>
  <action>
    新建 `scripts/release/check_version_consistency.py`(与 `verify_and_upload_macos_kernel.sh` 同目录 —— 这是**发布把关工具**,归入 Phase 2 D-11 已确立的「发布工具入仓」一侧,而不是 CLAUDE.md 所说的「打包脚本不入仓」那一侧;dmg/签名这类打包动作仍然全部内联在 yml 里,与 Windows 侧一致)。

    模块级常量:仓库根路径(从 `__file__` 上溯两级)、`frontend/package.json` 与 `backend/main.py` 的路径、以及匹配 FastAPI 版本字段的正则。正则要用 **semver 形状**(三段数字)而不是 RESEARCH 示例里的宽松 `[^"]+`,否则将来 `backend/main.py` 里任何别的 `version="..."` 字面量都会把校验误伤成不一致。

    导出:`VersionMismatch`(继承 `RuntimeError`)、`read_package_version(path=None)`、`read_main_versions(path=None)`(返回按出现顺序的全部匹配列表)、`normalize_tag(ref_name)`、`check_version_consistency(ref_name, is_tag, package_path=None, main_path=None)`、`main(argv=None)`。全部路径参数都要可注入,这样单测能喂临时文件而不依赖仓库当前版本号。

    `normalize_tag` 只去掉**一个**前导 `v`,用 `str.removeprefix`;绝不能用会把前导字符当字符集反复吞掉的那个字符串方法(RESEARCH 示例里用的就是后者,是个隐患)。

    `check_version_consistency` 的判定:先读 main.py 的匹配列表,少于 2 个就抛错并点名实际匹配数与 CLAUDE.md 的三处同步约定;列表内不全相同也抛错。再读 package.json。`is_tag` 为真时,期望版本 = `normalize_tag(ref_name)`,要求它与 package.json、main.py 三者全等;为假时(手动触发,ref 是分支名)只要求 package.json 与 main.py 相等,期望版本 = package.json 的版本。一致则返回版本字符串,不一致则抛 `VersionMismatch`,异常文本必须把参与比对的每个值都打出来,让 CI 日志一眼能看出是哪一处没跟上。

    `main(argv)` 接收 ref_name 与一个表示「是不是 tag」的参数,成功时把**版本号本身**打到 stdout(只有版本号,没有别的字符,方便 shell 直接捕获)、诊断信息打到 stderr,返回 0;抛 `VersionMismatch` 时把异常文本打到 stderr,返回 1。文件末尾加 `if __name__ == "__main__": raise SystemExit(main())`。

    新建 `tests/test_version_consistency.py` 覆盖 behavior 段列出的全部断言。涉及「不一致」的用例用 `tempfile` 造临时 package.json / main.py 喂进去,不要改仓库里的真文件;涉及「当前仓库三处一致」的用例断言**相等关系**而不是具体数字。

    最后改 `.github/workflows/build-release.yml` 里 `build-macos` job 的 `Resolve version` 步骤:把 05-03 落地的取值逻辑整体换成调用本脚本 —— 用 `python3 scripts/release/check_version_consistency.py "$GITHUB_REF_NAME" <is_tag>` 捕获 stdout 到 `APP_VERSION` 并写入 `$GITHUB_ENV`,`is_tag` 由 `github.ref` 是否以 `refs/tags/` 开头决定。脚本非零退出必须让整个步骤失败(`set -euo pipefail` 下捕获命令替换的失败要显式判断退出码,别让它被赋值语句吞掉)。步骤名保持 `Resolve version` 不变,位置也不动(仍在 `Prepare browser engine` 之前 —— 版本对不上就没必要花十几分钟下内核和构建)。
  </action>
  <verify>
    <automated>python -m unittest tests.test_version_consistency -v && python3 scripts/release/check_version_consistency.py main false</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_version_consistency -v` 退出 0,用例数不少于 8。
    - `python -m unittest discover -s tests -v` 退出 0。
    - `python3 scripts/release/check_version_consistency.py main false` 退出 0,且 stdout **只有**版本号一行(`| wc -l` 为 1,且该行匹配 `^[0-9]+\.[0-9]+\.[0-9]+$`)。
    - `python3 scripts/release/check_version_consistency.py v9.9.9 true` 退出 1(当前仓库版本不是 9.9.9),且 stderr 中同时出现 `9.9.9` 与 `package.json` 与 `main.py` 三个线索词。
    - `python3 -c "import sys;sys.path.insert(0,'scripts/release');import check_version_consistency as c;print(c.normalize_tag('vv0.2.0'))"` 输出 `v0.2.0`(证明只剥一个前导字符)。
    - `grep -c 'lstrip' scripts/release/check_version_consistency.py` 为 0。
    - `grep -n 'Resolve version' .github/workflows/build-release.yml` 仍恰好命中一次,且该步骤体内出现 `check_version_consistency`。
    - `git diff -U0 .github/workflows/build-release.yml` 的删除行**全部**落在 `Resolve version` 步骤体内(windows job 与 `build-macos` 其余步骤零改动)。
  </acceptance_criteria>
  <done>版本一致性把关落在可单测的入仓脚本里并接进 CI;tag 模式三方比对、手动触发模式两方比对,后者不阻塞 D-04 的调试通道。</done>
</task>

<task type="auto">
  <name>Task 2: 架构 / frontend-dist / LSMinimumSystemVersion 断言 + --backend-only 冒烟(D-14)</name>
  <files>.github/workflows/build-release.yml</files>
  <read_first>
    - `.planning/phases/05-ci/05-RESEARCH.md` § Code Examples / **「`--backend-only` CI 冒烟」** —— 权威模板,含「为什么推荐这条而不是启动完整 QWebEngineView GUI」的理由
    - `.planning/phases/05-ci/05-RESEARCH.md` § Architecture Patterns / **Pattern 1** —— `.app` 内真实数据在 `Contents/Resources/`,`Contents/Frameworks` 下是符号链接;`Path.exists()` / `Path.rglob()` 透明穿透
    - `.planning/phases/05-ci/05-RESEARCH.md` § **Assumptions Log A3**(`LSMinimumSystemVersion=12.0` 未查证,建议用 `otool -l` 检查 `LC_BUILD_VERSION` 后定稿)
    - `.planning/phases/05-ci/05-03-SUMMARY.md` —— 05-03 那次真实 CI 跑出来的嵌套 bundle 枚举与 `minos` 实测值,本任务的断言阈值以它为依据
    - `scripts/release/verify_and_upload_macos_kernel.sh` —— Phase 2 已建立的 `lipo` / `file` 架构断言写法,本任务沿用同一口径
    - `.planning/phases/05-ci/05-CONTEXT.md` **D-14** 全文与 § Claude's Discretion 第五条(冒烟判定粒度属裁量范围)
    - `.planning/phases/05-ci/05-RESEARCH.md` § Integration Points 里关于 `_g.py` 的说明 —— 运行时真正生效的是对 `FRONTEND_DIST_DIR` 内标记字符串的检查,而 **dist 缺失时是静默跳过**(这是本任务为何要单独加结构断言的原因)
    - `launch_app.py:363-373` —— `main()` 开头调用运行时完整性校验、随后分派 `--backend-only` 的既有分支
  </read_first>
  <action>
    在 `build-macos` job 里、`Verify code signatures (outer + nested kernel)` 之后与 `Create dmg` 之前,插入两个新步骤。位置很关键:必须在签名与双层验证**之后**(断言的是最终形态的产物),又在 dmg 制作**之前**(有问题就别浪费时间做 dmg,更不能让有问题的包上传成 artifact)。

    **步骤一 `Assert bundle contents (arch / frontend-dist / min-os)`** —— 四组断言,任一失败即中止:

    架构:对 `Contents/MacOS/Open-Anti-Browser` 主二进制与 `Contents/Resources/engines/chrome/Chromium.app/Contents/MacOS/Chromium` 内核二进制,各跑 `lipo -archs` 并断言输出含 arm64,再用 `file` 交叉印证。沿用 Phase 2 `verify_and_upload_macos_kernel.sh` 的断言口径与错误信息风格。

    `frontend/dist` 进包:断言 `Contents/Resources/frontend/dist` 是目录、其中 `index.html` 存在、且目录内递归文件数大于零。**这一条不能省、也不能用冒烟代替** —— 运行时完整性校验在 dist 目录整个缺失时是静默跳过的,所以「冒烟通过」推不出「dist 进包了」,只有独立的结构断言能抓住这种缺失。这是 PKG-05 在 CI 侧的落点。同时断言 `Contents/Frameworks/frontend` 是指向 `../Resources/frontend` 的符号链接(RESEARCH Pattern 1 实测布局,也是 `config.FRONTEND_DIST_DIR` 解析正确的前提)。

    最低系统版本(核销 A3):用 `plutil -extract LSMinimumSystemVersion raw` 读出 Info.plist 里的值;再遍历 `Contents/Frameworks` 下的 Mach-O 文件(用 05-03 那个不跟随符号链接的 `find` 口径,排除内核子树),用 `otool -l` 抽出各自的 `LC_BUILD_VERSION` 最低系统版本,取上确界(用 `sort -V | tail -1`);断言 plist 里的值不低于该上确界(把两个值一起丢给 `sort -V`,断言排在最后的是 plist 的值)。若断言失败,错误信息要同时打印两个值,让人一眼知道该把 `Patch Info.plist` 里的取值调到多少。

    **步骤二 `Smoke test (--backend-only)`** —— 照 RESEARCH「Code Examples / `--backend-only` CI 冒烟」实现:后台启动 `dist/Open-Anti-Browser.app/Contents/MacOS/Open-Anti-Browser --backend-only --port 18123`,循环轮询 `curl -sf http://127.0.0.1:18123/api/bootstrap`,拿到响应即判通过并结束子进程;30 次(每次间隔 1 秒)仍无响应则打印可定位的失败信息并以非零码中止。选 `--backend-only` 而不是完整 GUI 是 RESEARCH 的明确建议:它绕开 QtWebEngine/GPU 在无人值守 runner 上的不确定性,却仍然覆盖 D-14 想抓的全部失败模式 —— 模块加载失败、依赖缺失、以及运行时完整性校验拒绝启动,在这条路径上都会原形毕露(`launch_app.main` 是先跑校验、再分派 `--backend-only` 的)。轮询失败时要把子进程的 stdout/stderr 一并打到日志,否则 CI 上只能看到「超时」这一个无用信息。**不要**为了让步骤好过而给 curl 或 kill 加吞掉失败的兜底。

    完整 GUI 冒烟按 RESEARCH Open Question 3 的建议**不在本 phase 加**,不阻塞本 phase。
  </action>
  <verify>
    <automated>python3 -c "t=open('.github/workflows/build-release.yml',encoding='utf-8').read();m=t.split('build-macos:')[1];i=m.index('Verify code signatures');j=m.index('Assert bundle contents');k=m.index('Smoke test');l=m.index('Create dmg');assert i&lt;j&lt;k&lt;l;assert 'LSMinimumSystemVersion' in m and 'lipo -archs' in m and 'api/bootstrap' in m;print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 上面那条 `python3 -c` 步骤序断言退出 0:在 `build-macos` job 段落内,`Verify code signatures` → `Assert bundle contents` → `Smoke test` → `Create dmg` 四个步骤名按此顺序出现。
    - `build-macos` 段落内 `grep -c 'lipo -archs'` ≥ 2(主二进制与内核二进制各一次)。
    - `build-macos` 段落内出现 `Contents/Resources/frontend/dist/index.html` 与 `LSMinimumSystemVersion` 与 `api/bootstrap` 三个字面量。
    - `build-macos` 段落内 `grep -c 'continue-on-error'` 为 0;`grep -c 'if: always()'` 为 0。
    - 新增两个步骤都以 `set -euo pipefail` 开头。
    - `git diff -U0 .github/workflows/build-release.yml | grep '^-' | grep -v '^---'` 的删除行**全部**落在 `Resolve version` 步骤体内(Task 1 的替换),两个新门禁步骤是纯新增。
    - 本任务不修改 `backend/_g.py`、`frontend/src/lib/openSourceNotice.js`、`frontend/src/App.vue` —— `git diff` 对这三个文件为空。
  </acceptance_criteria>
  <done>四道硬门禁(架构 / dist 进包 / 最低系统版本 / 启动冒烟)落在签名验证与 dmg 制作之间,任一失败即中止发布链路。</done>
</task>

<task type="auto">
  <name>Task 3: 触发 workflow_dispatch 验证四道门禁全部通过,并做一次故意失败的反证</name>
  <precondition>`gh auth status` 成功且当前工作分支已推到 origin</precondition>
  <files>.github/workflows/build-release.yml</files>
  <read_first>
    - `.planning/phases/05-ci/05-03-SUMMARY.md` § 成功 run 的 id 与两个 build job 的结论(本次跑完要与之对比,确认新增门禁没有把原本能过的链路卡住)
    - `.planning/phases/05-ci/05-CONTEXT.md` **D-04**(调试通道复用 `workflow_dispatch`,不打 rc tag)
    - `.planning/phases/05-ci/05-VALIDATION.md` § Sampling Rate —— 「涉及 CI 工作流改动的 wave 至少 `workflow_dispatch` 触发一次完整跑通」
  </read_first>
  <action>
    提交并推送本 plan 的改动,用 `gh workflow run build-release.yml --ref <当前分支>` 触发一次完整跑。与 05-03 同样用轮询而不是长时间阻塞等待(macOS job 10-20 分钟,超过单次 Bash 工具超时上限)。

    正向验证:`build-macos` job 结论为 success,且四个新/改步骤(`Resolve version`、`Assert bundle contents`、`Smoke test`、以及它们之间的既有步骤)全部 success。把 `Resolve version` 打印出的版本号、`Assert bundle contents` 里 `lipo -archs` 与最低系统版本比对的实际输出、`Smoke test` 用了几秒拿到 `/api/bootstrap` 响应,逐项抄进 SUMMARY。

    A3 核销:把 `Assert bundle contents` 打印的「Info.plist 值 vs Mach-O 上确界」两个实测数字写进 SUMMARY,明确回答 `LSMinimumSystemVersion` 定为 `12.0` 是否成立。若实测上确界高于 12.0,把 `Patch Info.plist` 步骤里的取值改到实测值并重跑一次,SUMMARY 记录最终定稿值与改动理由。

    反证(证明门禁真的有牙,不是摆设):做一次**故意失败**的验证 —— 在本地用 `python3 scripts/release/check_version_consistency.py v9.9.9 true` 复现 tag 不一致时的非零退出与错误文本(这是不需要跑 CI 就能证明的部分,已在 Task 1 的 acceptance criteria 覆盖)。对于 CI 侧的门禁,至少要通过阅读日志确认 `Assert bundle contents` 与 `Smoke test` 两步真的执行了断言并打印了实测值,而不是因为某个变量为空而无声跳过 —— 把两步的完整日志摘录进 SUMMARY 作为证据。

    另外仍要确认 windows `build` job 在本次 run 中照常 success 并上传 artifact(Windows 零回归的持续基线)。
  </action>
  <verify>
    <automated>gh run list --workflow=build-release.yml --limit 5 --json databaseId,conclusion,event --jq '[.[] | select(.event=="workflow_dispatch" and .conclusion=="success")] | length' | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - 本 plan 推送后的那次 `workflow_dispatch` run,`build-macos` 与 `build` 两个 job 结论均为 `success`(run id 记入 SUMMARY)。
    - SUMMARY 含 `Assert bundle contents` 步骤的完整日志摘录,其中可见:两个二进制的 `lipo -archs` 实际输出、`frontend/dist` 断言通过的证据、Info.plist 最低系统版本值与 Mach-O 上确界值两个具体数字。
    - SUMMARY 含 `Smoke test` 步骤的日志摘录,可见 `/api/bootstrap` 拿到响应(而不是靠超时后被当作通过)。
    - SUMMARY 明确给出 A3 的最终结论:`LSMinimumSystemVersion` 定稿值是多少、依据的实测上确界是多少。
    - SUMMARY 含 `python3 scripts/release/check_version_consistency.py v9.9.9 true` 的非零退出与 stderr 文本摘录(门禁有牙的反证)。
    - `python -m unittest discover -s tests -v` 在本机退出 0。
  </acceptance_criteria>
  <done>四道门禁在真实 runner 上全部通过并留下可核对的实测数字;A3 假设核销定稿;Windows 侧持续零回归。</done>
</task>

</tasks>

<probe_assumptions>
## Spec-less Probe — 已明确标记的 planner 假设(本 phase 共 6 条,本 plan 承载 1 条)

本 phase 无 `*-SPEC.md`,边界覆盖由确定性 edge-probe 兜底生成,报告 6 行全部返回 `unresolved`,一律不得自动降级为 backstop、不得静默丢弃。其余 5 条见 `05-03-PLAN.md` 的同名章节。

| # | Requirement | Probe 分类 | 状态 | Planner 处置 |
|---|---|---|---|---|
| 6 | PKG-05 | unclassified | unresolved | **未解析假设。** PKG-05 有两个半句,自动化覆盖程度截然不同。后半句「`backend/_g.py` 完整性校验在 macOS 构建与启动中保持有效」由本 plan 的两道断言覆盖:构建期由 `npm run build` 的 prebuild/postbuild 钩子跑源文件哈希校验(源码未改则通过),运行时由 `--backend-only` 冒烟证明 `launch_app.main` 开头的校验没有拒绝启动;dist 缺失时校验静默跳过这一漏洞另由结构断言补上。**但**「校验依然**有效**」的完整语义(即它在被真正篡改时确实会拒绝)本 phase 未做反向验证 —— 没有构造一个 marker 被改的包去确认它真的拒启,因为那需要故意破坏受哈希锁定的资产。前半句「两个 dmg 与 Windows 安装包挂到同一 GitHub Release」按 arm64-only scope 读作「一个 dmg」,其验证落在 05-05,且只有真正推一次 `v*` tag 才能端到端证实 —— 本 phase 的 `workflow_dispatch` 通道按 D-04 刻意不建 Release,故该半句在本 phase 内**结构上无法闭环**,只能靠 `release` job 的 needs/if 条件与 artifact 齐备性间接支撑。两处未覆盖点均记录在此,不做静默通过。 |
</probe_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tag 字符串 → dmg 文件名与应用内显示版本 | 版本号是用户判断「我装的是哪个版本、要不要升级」的唯一依据;两处不一致会让后续所有问题排查失去坐标 |
| 打包产物 → `backend/_g.py` 的开源声明完整性校验 | 该校验是本项目的反商业滥用机制;打包流程若破坏其前提(dist 未完整进包),校验会静默失效而无人察觉 |
| CI 冒烟启动的本地服务端口 → runner 环境 | 冒烟会在 runner 上真实监听 127.0.0.1 端口并写入用户级 Application Support 目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Tampering(开源声明校验被静默架空) | `frontend/dist` 进包断言 | high | mitigate | 独立的结构断言(目录存在 + `index.html` 存在 + 递归文件数非零 + `Contents/Frameworks/frontend` 符号链接成立)覆盖「dist 缺失时校验静默跳过」这一漏洞;冒烟另行覆盖「校验主动拒启」。本 plan 的 prohibitions 条目明文禁止为让打包跑通而改动该校验或其哈希锁定资产。 |
| T-05-15 | Spoofing(版本号伪装) | `Resolve version` / `check_version_consistency.py` | high | mitigate | tag 模式下 tag、`package.json`、`main.py` 三方全等才放行;semver 形状正则避免误伤;不一致时异常文本打印全部参与值,CI 立即失败。Task 1 的 acceptance criteria 用 `v9.9.9` 做非零退出反证,证明门禁有牙。 |
| T-05-16 | Tampering(发出非目标架构的包) | `lipo -archs` 断言 | medium | mitigate | 对主二进制与内核二进制分别断言 arm64,沿用 Phase 2 已在真机验证过的架构断言口径(该口径当时正是靠 `lipo` 取证拒绝了一个 arm64 冒充 x64 的产物)。 |
| T-05-17 | Denial of Service(发出装了跑不起来的包) | `Smoke test (--backend-only)` | high | mitigate | 用最终签名产物真起一次并轮询 `/api/bootstrap`,覆盖模块加载失败、依赖缺失、完整性校验拒启三类失败;失败时打印子进程 stdout/stderr 便于定位;禁止给 curl / kill 加吞掉失败的兜底。 |
| T-05-18 | Information Disclosure | 冒烟步骤的日志输出 | low | accept | 冒烟输出的是本地服务启动日志与 `/api/bootstrap` 的可达性判定,不打印响应体,不涉及凭据;工作流不使用除 `GITHUB_TOKEN` 之外的 secret。 |
| T-05-19 | Repudiation(门禁无声跳过被误当作通过) | 两个新门禁步骤 | medium | mitigate | Task 3 要求把两步的完整日志摘录进 SUMMARY,确认断言真的执行并打印了实测值,而不是因变量为空而空转;两步均以 `set -euo pipefail` 开头。 |
| T-05-SC | Tampering | 包管理器安装 | low | accept | 本 plan 不新增任何 `pip` / `npm` / `brew` 安装(新增脚本只用 Python 标准库 `json` / `re` / `pathlib`),包合法性门禁不适用;job 内既有的三处安装已在 `05-03-PLAN.md` 的 T-05-SC 中处置。 |
</threat_model>

<verification>
1. `python -m unittest discover -s tests -v` 退出 0(含新增的 `tests/test_version_consistency.py`)。
2. `python3 scripts/release/check_version_consistency.py main false` 退出 0 且 stdout 只有一行版本号;`... v9.9.9 true` 退出 1。
3. `build-macos` job 段落内四个门禁步骤按 `Verify code signatures` → `Assert bundle contents` → `Smoke test` → `Create dmg` 的顺序排列。
4. 一次真实 `workflow_dispatch` run 中 `build-macos` 与 `build` 两个 job 均 success,且 SUMMARY 留有两个门禁步骤的日志摘录与 A3 定稿结论。
5. `git diff` 对 `backend/_g.py`、`frontend/src/lib/openSourceNotice.js`、`frontend/src/App.vue` 三个文件为空。

**发版前提醒(不属本 plan 任务,记入 SUMMARY 供后续参考):** 当前 `frontend/package.json` 与 `backend/main.py` 两处 FastAPI `version=` 均为同一旧版本号。真正推 `v0.2.0` tag 之前,必须先按 CLAUDE.md 的三处同步约定把它们改到 `0.2.0`,否则 `Resolve version` 会按设计直接 fail。`workflow_dispatch` 手动触发不受此影响(走的是 package.json 取值分支)。
</verification>

<success_criteria>
- 版本号在 tag 与仓库三处之间不可能再静默分叉。
- `.app` 的架构、`frontend/dist` 完整性、最低系统版本三项在 CI 内被硬断言,不再依赖人工抽查。
- 「签名过了但一启动就挂」这类失败在 dmg 制作之前就被 `--backend-only` 冒烟拦下。
- RESEARCH 假设 A3 从推断值转为有实测依据的定稿值。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

本 plan 产出的符号(供下游 review 的 source-grounding 排除新符号误报):

**新文件**
- `scripts/release/check_version_consistency.py`
- `tests/test_version_consistency.py`

**新导出符号(`scripts/release/check_version_consistency.py`)**
- `VersionMismatch`(异常类)
- `read_package_version(path=None)`
- `read_main_versions(path=None)`
- `normalize_tag(ref_name)`
- `check_version_consistency(ref_name, is_tag, package_path=None, main_path=None)`
- `main(argv=None)`
- 模块级常量:仓库根路径、`frontend/package.json` 路径、`backend/main.py` 路径、FastAPI 版本字段的 semver 形状正则

**新 CI step(`build-macos` job 内)**
- `Assert bundle contents (arch / frontend-dist / min-os)`
- `Smoke test (--backend-only)`

**被本 plan 替换实现的既有 CI step(名称不变)**
- `Resolve version`(05-03 落地取值逻辑 → 本 plan 换成调用 `check_version_consistency.py`)

**新 CI 端口占用**
- 冒烟步骤监听 `127.0.0.1:18123`

**本 plan 不产出** 任何资产文件、`launch_app.py` 符号或前端符号。

*(本 phase 其余 plan 产出的完整符号清单见 `05-01-PLAN.md`、`05-02-PLAN.md`、`05-03-PLAN.md`、`05-05-PLAN.md` 各自的同名章节。)*
</artifacts_produced>

<output>
Create `.planning/phases/05-ci/05-04-SUMMARY.md` when done.
SUMMARY 必须包含:(1) `Assert bundle contents` 与 `Smoke test` 两步的完整日志摘录;(2) A3 的最终定稿值与实测上确界;(3) `check_version_consistency.py v9.9.9 true` 的非零退出与 stderr 摘录;(4) 成功 run 的 id 与两个 build job 的结论;(5) 发版前需同步版本号三处的提醒。
</output>

