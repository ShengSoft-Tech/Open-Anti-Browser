---
phase: 07-patch-release
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - .planning/phases/07-patch-release/evidence/sc1-close-button-evidence.md
  - .planning/phases/07-patch-release/evidence/sc2-sc3-human-review.md
  - .planning/phases/07-patch-release/07-VERDICTS.md
  - .planning/phases/06-release-docs/06-RESEARCH.md
  - .planning/phases/07-patch-release/07-RESEARCH.md
autonomous: false
requirements: [UI-05, PKG-06]

must_haves:
  truths:
    - statement: "在真机（Apple Silicon + macOS 15.7）上运行**来自 v0.2.1 Release 的 dmg 安装出的冻结 `.app`**，点窗口关闭按钮后进程真正消失——`ps` 输出中不存在任何残留进程，也没有停在约 60% CPU 空转的进程（SC1 / UI-05）"
      verification: backstop
    - statement: "Cmd+Q 与 Dock/菜单栏退出两条路径同样让进程真正消失，`a886dc7` 未把 05-06 的 Quit 事件死循环再引回来"
      verification: backstop
    - statement: "人工在 v0.2.1 Release 页面复核确认：正文中的手写模板内容确实存在、且与 07-02 自动断言得出的顺序结论一致（SC2/SC3）"
      verification: backstop
    - "SC1 的通过结论有可复核的一手证据落盘（`ps` 命令原始输出，不是「已确认」这类转述）"
    - "06-RESEARCH.md 假设 A2 行被追加一条注明日期与证据来源的判定注记；原文逐字保留，不被改写或删除"
    - "07-RESEARCH.md 假设 A1 行被追加同一条判定的交叉引用，且明确标注 06-RESEARCH A2 ≡ 07-RESEARCH A1、与 07-RESEARCH 自己的 A2（删 tag 重推）不是同一条"
    - "退出后 `data/profiles.json` 仍为合法 JSON，且没有被应用启动过的 Chromium 子进程残留——关闭按钮的退出路径没有变成无提示的强杀"
  artifacts:
    - ".planning/phases/07-patch-release/evidence/sc1-close-button-evidence.md — 三条退出路径的真机验证记录，含 `ps` 原始输出"
    - ".planning/phases/07-patch-release/evidence/sc2-sc3-human-review.md — Release 页面人工复核记录"
    - ".planning/phases/07-patch-release/07-VERDICTS.md — SC1/SC2/SC3 三项判定与 UI-05/PKG-06 的核销结论"
    - ".planning/phases/06-release-docs/06-RESEARCH.md — A2 行追加判定注记（append-only）"
    - ".planning/phases/07-patch-release/07-RESEARCH.md — A1 行追加交叉引用注记（append-only）"
  key_links:
    - "v0.2.1 Release 的 dmg → `/Applications/Open-Anti-Browser.app` → 冻结态 Qt/Cocoa 事件循环：05-02 SIGSEGV 与 05-06 Quit 死循环两次缺陷都只在这条路径上复现过，源码直跑不在这条链上"
    - "`launch_app.py` 的 `should_close_to_tray()` / `handle_macos_quit_request()` / `DesktopApplication.event()` → `force_exit()` → `closeEvent` 的 shutdown 分支：三条退出路径共用同一个收敛点，所以三条都要测"
    - "07-02 的自动断言结论 ↔ 本计划的人工复核：两者若不一致，以人工在页面上看到的为准并记录分歧"
  prohibitions:
    - "SC1 的「真机通过」结论不得由未真实执行该操作的一方（执行 agent 自身）自我认证——必须由人在真机上操作，并把 `ps` 原始输出作为一手证据落盘；无一手证据则判为未验证，不得判 PASS"
    - "不得用源码直跑 `python launch_app.py` 冒充冻结 `.app` 的等价证据（07-RESEARCH Pitfall 4 / 假设 A4）；也不得用 07-02 之前 `workflow_dispatch` 产出的临时 artifact 替代 Release 的 dmg（D-11/D-12）"
    - "关闭按钮的退出路径不得绕过既有的有序关停逻辑去直接杀进程，导致用户已启动的浏览器会话或 `data/*.json` 配置在无提示的情况下损坏或丢失——本计划不改任何代码，此条作为真机验收的观察项"
    - "若正文实际由 `gh release edit` 人工补救，不得在 07-VERDICTS.md 或 SUMMARY 里被表述为「流水线自动渲染通过」；PKG-06 相应判为未达成"
    - "不得在本计划中修改 `.github/RELEASE_NOTES_TEMPLATE.md` 的排版以「顺手修正」渲染顺序（D-14：只记录判定，排版调整另开 phase——就地改排版需要另一次真实 tag push 才能验证，与「一次发版」的约束冲突）"
  flagged_assumptions:
    - "边界探针对 UI-05 返回 `unclassified` → `unresolved`，未产出任何 covered/backstop 边界断言。确定性探针词表为英文驱动、UI-05 原文为中文，这是分类落空的最可能原因，不代表 UI-05 无边界。本计划的边界覆盖（三条退出路径 + 数据完整性观察项）来自 05-02/05-06 的历史缺陷成因，未经探针背书。"
    - "边界探针对 PKG-06 返回 `unclassified` → `unresolved`，同上。"
    - "07-RESEARCH.md 假设 A3（GitHub-hosted macOS runner 上 Accessibility/`kTCCServiceAccessibility` 无法非交互式授予）来自公开 issue 交叉印证，非本仓库实测。它是 D-13「第三维度不可行、就此关闭」的依据。若 GitHub 后续更改 runner 镜像预授权策略，该结论需重新评估——但本 phase 不为此花任何执行时间。"
    - "07-RESEARCH.md 假设 A4（05-02/05-06 两次缺陷只在冻结 `.app` 复现是冻结态特有运行时因素导致，而非偶然）是基于历史事实的因果推断，未被专门验证。它是 D-12「SC1 必须用冻结 `.app`」的依据。风险不对称：错误采信「源码跑等价」会漏检只在冻结态出现的回归，而坚持冻结态的代价只是多花验证时间，故保守处理。"
    - "07-RESEARCH.md 假设 A1 ≡ 06-RESEARCH.md 假设 A2（`body_path` 内容前置）：本计划 Task 3 落盘的正是它的实测判定，判定完成后该假设不再是假设。"
---

<objective>
用 07-02 发布的那一份真实产物，把三条 success criteria 全部收口：SC1 由人在真机上对 **v0.2.1 Release 的 dmg 装出的冻结 `.app`** 实测三条退出路径并留下 `ps` 一手证据；SC2/SC3 由人在 Release 页面上复核 07-02 的自动断言；然后把三项判定与 06-RESEARCH.md 假设 A2 的正反结论落盘。

Purpose：`a886dc7` 至今只有单测与 AST 结构断言，没有真机证据——而 05-02（SIGSEGV）与 05-06（Quit 事件死循环）两次缺陷都出在这条路径上、且都只在冻结包里暴露过。SC1 没有自动化等价物（07-RESEARCH Pitfall 3 / D-13：点击窗口关闭按钮需要 `System Events` UI Scripting，受 `kTCCServiceAccessibility` 网关，GitHub-hosted runner 上无法非交互式授予），因此这条只能由人来做。

Output：三份判定记录 + 两处假设行的 append-only 判定注记。

本计划**不改动任何源码、CI 配置或发布模板**——只验证与记录。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-patch-release/07-CONTEXT.md
@.planning/phases/07-patch-release/07-RESEARCH.md
@.planning/phases/07-patch-release/07-VALIDATION.md
@.planning/phases/07-patch-release/07-02-SUMMARY.md
@.planning/phases/07-patch-release/evidence/v0.2.1-observation.md
@launch_app.py
</context>

<phase_decisions>
## 适用于本计划的 phase 级决策（在 07-02 中落盘，此处仅重申，不重新权衡）

- **D-11 先发后验**：SC1 用 v0.2.1 Release 页面上的 dmg，不用 `workflow_dispatch` 的临时 artifact。
- **D-12 SC1 载体 = 冻结的 `.app`，覆盖三条退出路径**（窗口关闭按钮 / Cmd+Q / Dock 或菜单栏退出）。源码直跑不被接受为等价证据。三条都测的理由：`a886dc7` 让三条路径收敛到同一个 `force_exit()`，05-06 的死循环缺陷正是在这个共用收敛点上；只测关闭按钮会漏掉共用点上的回归。
- **D-13（RESOLVED，结论为不可行）**：不给 `build-macos` 的 GUI 冒烟门禁加「点击关闭按钮」第三维度，本条讨论就此关闭。现有两维度（18s 存活 + `osascript ... to quit` 有界退出）已是 CI 侧上限。**本计划不得花任何时间尝试该维度。**
- **D-14 A2 被推翻时只记录判定，不就地调模板排版。**
- **D-15 失败回退策略：取证优先的三段式**——若 07-02 已判 SC2 FAIL，本计划 Task 2 的人工复核确认现状后，才允许执行第三步（`gh release edit` 手工补正面向用户的正文），并在 07-VERDICTS.md 里显式标注「本次由人工补救，PKG-06 未达成」。
</phase_decisions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: SC1 真机验收——冻结 .app 的三条退出路径</name>
  <precondition>v0.2.1 Release 页面上存在 `Open-Anti-Browser-*-arm64.dmg` 资产（07-02 已完成发布），且执行验收的机器是 Apple Silicon、macOS 15 或更新版本。</precondition>
  <what-built>
`a886dc7`（已在 main，本 phase 未改动）让 macOS 上点窗口关闭按钮直接退出应用，不再隐藏到菜单栏挂在后台。相关函数在 `launch_app.py`：`should_close_to_tray()` 在 darwin 上返回 False（于是 `closeEvent` 走 shutdown 分支而不是托盘分支）、`should_intercept_quit_event()` / `handle_macos_quit_request()` 接住 Cmd+Q、`DesktopApplication.event()` 无条件转发 `super().event(e)` 以免重演 05-06 的 Quit 死循环。三条退出路径最终都收敛到同一个 `force_exit()`。

现有的自动化护栏只覆盖其中两个维度（`build-macos` job 的 GUI 冒烟门禁：18s 存活防 SIGSEGV 回归 + `osascript ... to quit` 有界退出防死循环回归），**关闭按钮点击这条路径没有、也不可能有自动化护栏**（D-13）。所以需要你在真机上跑一次。
  </what-built>
  <how-to-verify>
**准备（必须用 Release 的 dmg，不能用源码跑，也不能用 workflow_dispatch 的 artifact）：**

1. 打开 v0.2.1 Release 页面（URL 见 `.planning/phases/07-patch-release/evidence/v0.2.1-release-meta.json`），下载 `Open-Anti-Browser-*-arm64.dmg`
2. 挂载 dmg，把 `Open-Anti-Browser.app` 拖进 `/Applications`
3. 首次打开若被 Gatekeeper 拦截，按 Release 正文里的放行指引处理（首选：被拦截、应用自动退出后再次双击打开）
4. 应用起来后，先创建或打开一个配置并**启动一次 Chrome 浏览器实例**，这样才能观察到「退出是否波及已启动的子进程与配置数据」

**A 组 — 窗口关闭按钮（本次的核心，SC1 本体）：**

5. 点应用主窗口左上角的红色关闭按钮
6. 在终端执行 `ps aux | grep -i open-anti-browser | grep -v grep` 并把**完整输出**（哪怕是空的）记下来
7. 同时打开活动监视器搜索 `Open-Anti-Browser`，确认没有任何进程停在高 CPU（05-06 的死循环表现为约 60% 甚至 113% CPU 空转不退出）
8. 点 Dock 上是否还残留应用图标；菜单栏右上角是否还有孤儿图标

**B 组 — Cmd+Q：** 重新打开应用，按 Cmd+Q，重复第 6–8 步取证。

**C 组 — Dock 右键退出 / 菜单栏图标退出：** 重新打开应用，用 Dock 图标右键「退出」（若菜单栏仍有图标，也走一次它的退出项），重复第 6–8 步取证。

**D 组 — 数据完整性（观察项，防止「退出」退化成无提示强杀）：**

9. 三组退出各自完成后，确认 `~/Library/Application Support/Open-Anti-Browser/` 下的 `data/profiles.json` 仍是合法 JSON（`python3 -m json.tool <该文件路径> > /dev/null` 退出码 0）
10. 执行 `ps aux | grep -i chromium | grep -v grep`，记录是否有被应用启动过、退出后仍残留的 Chromium 子进程

**记录：** 把 A/B/C 三组各自的 `ps` 原始输出（逐字，不要转述成「已确认无残留」）、活动监视器观察、D 组两项结果，连同 macOS 版本与芯片型号（苹果菜单「关于本机」）写进 `.planning/phases/07-patch-release/evidence/sc1-close-button-evidence.md`。

**判定口径：** A 组通过 = SC1 达成（UI-05 核销的核心）。B/C 组是回归确认，任何一组出现残留进程或高 CPU 空转都属于新缺陷，必须记录并上报，不要自行修代码——本 phase 的边界是「发布并验证」。
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/07-patch-release/evidence/sc1-close-button-evidence.md` 存在且非空
    - 该文件中含 A/B/C 三组各自的 `ps aux | grep -i open-anti-browser` **原始输出**（逐字粘贴，不是「无残留」这类转述）
    - 该文件中记录了验收机器的 macOS 版本与芯片型号，且明确写明所用安装包来自 **v0.2.1 Release 的 dmg**（含 dmg 文件名），而非源码直跑或 workflow_dispatch artifact
    - 该文件中对 A 组给出明确的 PASS 或 FAIL 字样
    - 该文件中含 D 组两项结果：`data/profiles.json` 的 JSON 合法性校验结论、退出后 Chromium 子进程残留情况
    - 行为断言（A 组通过时）：点关闭按钮后 `ps aux | grep -i open-anti-browser | grep -v grep` 无输出，且活动监视器中无同名高 CPU 进程
  </acceptance_criteria>
  <resume-signal>回复 "approved" 表示 A 组通过、SC1 达成；或描述实际观察到的问题（残留进程 / 高 CPU / 数据异常），并说明是哪一组</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SC2/SC3 人工复核——Release 页面实际渲染结果</name>
  <what-built>
07-02 已经用 `gh release view v0.2.1 --json body` 把已发布正文的一手原文取回并落盘，对 SC2（正文是否含手写模板内容）与 SC3（手写内容与自动 changelog 的实际先后顺序）做了自动断言，结论写在 `.planning/phases/07-patch-release/evidence/v0.2.1-observation.md`。

自动断言看的是 API 返回的 markdown 源码；这一步请你在**浏览器渲染后的页面**上确认同一件事，并确认没有任何人工编辑痕迹。
  </what-built>
  <how-to-verify>
1. 打开 v0.2.1 Release 页面（URL 见 `evidence/v0.2.1-release-meta.json`）
2. **SC2**：确认正文顶部能看到中文「本次更新」与英文「What's Changed」两段，各含 macOS 与 Windows 两条；再往下确认原有的系统要求 / 签名信任说明 / 放行指引整篇都在。这是 `524aeb1` 给 `release` job 补上 `actions/checkout` 后的首次真实执行——对照 v0.2.0 首发时正文只剩一行 Full Changelog 的故障模式
3. **SC2 补充**：确认页面上没有 "edited" 标记（GitHub 会在被编辑过的 Release 上显示编辑痕迹），并回想/确认自 07-02 发布以来无人执行过 `gh release edit` 或在 Web UI 上改过正文
4. **SC3**：记下手写内容与 `Full Changelog` 链接在页面上的实际先后位置，与 `evidence/v0.2.1-observation.md` 里的自动断言结论比对。两者一致 → 确认；不一致 → 以你在页面上看到的为准，并把分歧记下来
5. **对照 D-04**：确认正文里**没有**提到 `524aeb1` 那类 CI 内部修复（正文只写用户能感知的变化）
6. **对照 CONTEXT.md §Specific Ideas**：确认 v0.2.0 的 Release 页面保持原样、本 phase 未被触碰

把以上六点的结论写进 `.planning/phases/07-patch-release/evidence/sc2-sc3-human-review.md`，SC3 那条要写清楚**实际观测到的顺序**（哪一块在上面），不要写成「符合预期」。

**若 SC2 未达成**（正文里看不到手写内容）：按 D-15，07-02 应当已经把失败正文原文取证落盘。此时你可以选择授权执行第三步——用 `gh release edit` 手工补正面向用户的正文——但必须在记录里显式标注「本次由人工补救，PKG-06 未达成」。**不要**删除 tag 或 Release，**不要**烧 v0.2.2 版本号。
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/07-patch-release/evidence/sc2-sc3-human-review.md` 存在且非空
    - 该文件对上述六点各给出明确结论，SC2 有 PASS 或 FAIL 字样
    - 该文件中 SC3 一条写明**页面上实际观测到的先后顺序**（哪一块在上），并注明与 `evidence/v0.2.1-observation.md` 自动断言结论是否一致；不一致时记录分歧内容
    - 该文件中含一句关于「自 07-02 发布以来是否对该 Release 执行过人工编辑」的明确声明
    - 该文件中含「v0.2.0 Release 页面在本 phase 中未被触碰」的确认
    - 行为断言：若记录中出现「已执行 `gh release edit`」，则同一文件中必须同时出现「PKG-06 未达成」的判定，二者不得只有其一
  </acceptance_criteria>
  <resume-signal>回复 "approved" 表示 SC2/SC3 复核完成；或描述页面上与自动断言不一致的地方</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 三项判定落盘并回填两处假设行</name>
  <files>.planning/phases/07-patch-release/07-VERDICTS.md, .planning/phases/06-release-docs/06-RESEARCH.md, .planning/phases/07-patch-release/07-RESEARCH.md</files>
  <read_first>
    - `.planning/phases/07-patch-release/evidence/sc1-close-button-evidence.md`（Task 1 产出）
    - `.planning/phases/07-patch-release/evidence/sc2-sc3-human-review.md`（Task 2 产出）
    - `.planning/phases/07-patch-release/evidence/v0.2.1-observation.md`（07-02 产出）
    - `.planning/phases/06-release-docs/06-RESEARCH.md` §Assumptions 表——找到 **A2 行**的确切位置与原文（这是 SC3 要判定/推翻的那条）
    - `.planning/phases/07-patch-release/07-RESEARCH.md` §Assumptions Log——找到 **A1 行**（≡ 06-RESEARCH A2）与 **A2 行**（删 tag 重推，与本判定无关，不要改错行）
    - `.planning/ROADMAP.md` §Phase 7 的 SC1/SC2/SC3 原文；`.planning/REQUIREMENTS.md` 的 UI-05 / PKG-06 原文
  </read_first>
  <action>
**新建 `.planning/phases/07-patch-release/07-VERDICTS.md`**，逐条收口：

- SC1（关闭按钮真机退出）：PASS / FAIL + 证据文件路径 + 关键的 `ps` 输出摘录 + 验收机器的 macOS 版本与芯片。三条退出路径（A/B/C 组）各自结论。明确标注「实测」而非「推断」（Phase 5/6 惯例）。
- SC2（正文由流水线自动渲染、零 `gh release edit`）：PASS / FAIL + 自动断言与人工复核两路证据的路径 + 若曾人工补救则显式标注「PKG-06 未达成」。
- SC3（手写正文与自动 changelog 的实际先后顺序）：记录实际观测到的顺序原文片段，给出对 06-RESEARCH.md 假设 A2 的判定（支持 / 推翻 / 无从判定）。按 D-14，无论哪种结果本 phase 都只记录，不就地调模板排版。
- UI-05 / PKG-06 两条 requirement 的核销结论各一行，附对应 SC 与证据文件。
- 一节「本 phase 未触碰的东西」：`launch_app.py`（`a886dc7` 已在 main）、`.github/workflows/build-release.yml` 的 `release` job（`524aeb1`，是观测对象）、v0.2.0 的 Release 页面。
- 一节「已关闭的 discretion 条目」：D-13（给 GUI 冒烟门禁加第三维度）**结论为技术上不可行、就此关闭**，依据是 `kTCCServiceAccessibility` 在 GitHub-hosted macOS runner 上无法非交互式授予（07-RESEARCH Pitfall 3 / 假设 A3）；同时注明关闭按钮这条路径因此**永久只有人工回归护栏**，未来任何 macOS 退出路径改动都需要重跑本计划的真机流程。
- 一节「仍然标记为假设的条目」：把 07-RESEARCH 的 A2（删 tag 重推，本 phase 刻意未依赖故未验证）、A3（TCC 结论，非本仓库实测）、A4（冻结态特有差异的因果推断）三条原样带出来，说明它们在本 phase 结束后依然是假设。

**回填 06-RESEARCH.md 的 A2 行**：在 §Assumptions 表的 A2 行原文之后**追加**（append-only）一条判定注记，形如「2026-07-31 起：经 v0.2.1 真实 tag push 实测 —— <实际顺序结论>，本假设 <成立 / 被推翻>，证据见 `.planning/phases/07-patch-release/07-VERDICTS.md`」。原文逐字保留，不改写、不删除——那是当时的判断记录，价值在于对照。

**回填 07-RESEARCH.md 的 A1 行**：同样 append-only 追加一条交叉引用，并显式写明「本条 ≡ 06-RESEARCH.md 假设 A2；与本文件自己的 A2（删 tag 重推能否重新触发 workflow）不是同一条，后者在本 phase 中刻意未被依赖、故仍未验证」。这句消歧必须写——两份文档里的 A2 指向不同的东西，是后续读者最容易踩的坑。

三处文件改动都是文档追加，不涉及任何源码或 CI 配置。
  </action>
  <verify>
    <automated>test -s .planning/phases/07-patch-release/07-VERDICTS.md && grep -q 'SC1' .planning/phases/07-patch-release/07-VERDICTS.md && grep -q 'SC2' .planning/phases/07-patch-release/07-VERDICTS.md && grep -q 'SC3' .planning/phases/07-patch-release/07-VERDICTS.md && grep -q 'UI-05' .planning/phases/07-patch-release/07-VERDICTS.md && grep -q 'PKG-06' .planning/phases/07-patch-release/07-VERDICTS.md && grep -q '07-VERDICTS.md' .planning/phases/06-release-docs/06-RESEARCH.md && grep -q '06-RESEARCH' .planning/phases/07-patch-release/07-RESEARCH.md && .venv/bin/python -m unittest discover -s tests -v && node --test frontend/src/lib/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-patch-release/07-VERDICTS.md` 中 SC1 / SC2 / SC3 三节各含明确的 PASS 或 FAIL（或 SC3 的「支持 / 推翻 / 无从判定」）字样，且各自引用了具体证据文件路径
    - 该文件中 UI-05 与 PKG-06 各有一行核销结论
    - 该文件中含「已关闭的 discretion 条目」一节，其中 D-13 被记为不可行且已关闭（不是待办、不是开放问题）
    - 该文件中含「仍然标记为假设的条目」一节，列出 07-RESEARCH 的 A2 / A3 / A4 三条
    - `.planning/phases/06-release-docs/06-RESEARCH.md` 的 A2 行之后含一条带日期、含 `07-VERDICTS.md` 路径的判定注记；`git diff .planning/phases/06-release-docs/06-RESEARCH.md` 显示只有新增行，无删除行（`git diff --numstat` 的删除计数为 0）
    - `.planning/phases/07-patch-release/07-RESEARCH.md` 的 A1 行之后含交叉引用注记，且该注记中同时出现字符串 `06-RESEARCH` 与消歧说明；`git diff --numstat` 对该文件的删除计数为 0
    - 行为断言：`git diff --name-only` 的结果中不含 `launch_app.py`、`.github/workflows/build-release.yml`、`.github/RELEASE_NOTES_TEMPLATE.md`、`scripts/`、`backend/`、`frontend/` 下的任何文件
    - `.venv/bin/python -m unittest discover -s tests -v` 与 `node --test frontend/src/lib/*.test.js` 仍全绿（本计划零代码改动，测试数字应与 07-01 收尾时一致）
  </acceptance_criteria>
  <done>SC1/SC2/SC3 三项判定与 UI-05/PKG-06 的核销结论落在 07-VERDICTS.md；06-RESEARCH.md 的 A2 与 07-RESEARCH.md 的 A1 各追加一条 append-only 判定注记并完成消歧；未触碰的对象与仍为假设的条目都被显式记录。</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces（本计划部分）

以下产物在本计划执行前**不存在**：

| 类型 | 标识符 / 路径 |
|---|---|
| 证据文件 | `.planning/phases/07-patch-release/evidence/sc1-close-button-evidence.md` |
| 证据文件 | `.planning/phases/07-patch-release/evidence/sc2-sc3-human-review.md` |
| 判定文档 | `.planning/phases/07-patch-release/07-VERDICTS.md` |
| 文档追加 | `.planning/phases/06-release-docs/06-RESEARCH.md` A2 行的判定注记（append-only） |
| 文档追加 | `.planning/phases/07-patch-release/07-RESEARCH.md` A1 行的交叉引用注记（append-only） |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 公开 Release 的 dmg → 本机 `/Applications` | 用户（和本次验收者）从公网下载未签名安装包并在本机运行，Gatekeeper 是唯一的系统侧关口 |
| 真机观测 → 验证记录 | 人的观察结果转成 `07-VERDICTS.md` 里的判定；此边界上的失真会让 UI-05/PKG-06 的「已核销」名不副实 |
| 已完成 phase 的研究文档 → 本次追加 | 回填 06-RESEARCH.md 是对历史记录的写入，改写而非追加会毁掉「当时怎么判断」的对照价值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Repudiation | SC1 的「真机通过」结论 | high | mitigate | 结论由未真实执行的一方自我认证：SC1 被建模为 `checkpoint:human-verify`（阻断式），验收标准强制要求 `ps` **原始输出**逐字落盘而非转述，并要求记录验收机器型号与所用 dmg 文件名；无一手证据则判为未验证 |
| T-07-07 | Tampering | 已完成 phase 的研究文档（06-RESEARCH.md） | medium | mitigate | 回填时改写或删除原文会抹掉历史判断：Task 3 明文要求 append-only，验收用 `git diff --numstat` 的删除计数为 0 作为硬断言 |
| T-07-02 | Tampering | v0.2.1 Release 正文中的放行指引 | medium | mitigate | 验收者会照着正文里的放行步骤操作未签名应用：正文内容已由 Phase 6 的 `releaseNotesTemplate.test.js` 黑名单（`FORBIDDEN_FRAGMENTS` 常量）与 `GATEKEEPER_XATTR_COMMAND` 逐字锁在 07-01 阶段把关，本计划的 Task 3 验收重跑 `node --test` 确认未漂移 |
| T-07-08 | Spoofing | 验收所用安装包的来源 | medium | mitigate | 用源码直跑或 `workflow_dispatch` 临时 artifact 冒充 Release 产物会让 SC1 证据失效（07-RESEARCH Pitfall 4）：验收标准强制记录 dmg 文件名并声明来源为 v0.2.1 Release 页面 |
| T-07-SC | Tampering | npm/pip/cargo 安装 | — | accept | 不适用：本计划不安装任何外部包，也不改动任何依赖清单（07-RESEARCH § Package Legitimacy Audit 判定该协议不适用） |
</threat_model>

<verification>
1. `evidence/sc1-close-button-evidence.md` 含 A/B/C 三组 `ps` 原始输出、机器型号、dmg 文件名，A 组有明确 PASS/FAIL
2. `evidence/sc2-sc3-human-review.md` 含六点结论、SC2 的 PASS/FAIL、SC3 的实际顺序观测与一致性比对
3. `07-VERDICTS.md` 含 SC1/SC2/SC3 判定、UI-05/PKG-06 核销结论、已关闭的 discretion 条目、仍为假设的条目四节
4. `git diff --numstat .planning/phases/06-release-docs/06-RESEARCH.md .planning/phases/07-patch-release/07-RESEARCH.md` 的删除计数均为 0（append-only）
5. `git diff --name-only` 不含任何源码、CI 配置或发布模板文件
6. `.venv/bin/python -m unittest discover -s tests -v` 与 `node --test frontend/src/lib/*.test.js` 全绿
</verification>

<success_criteria>
- SC1 在真机上、对来自 v0.2.1 Release 的冻结 `.app` 完成三条退出路径实测，有 `ps` 一手证据
- SC2/SC3 在渲染后的 Release 页面上完成人工复核，与自动断言结论一致（或分歧被记录）
- 06-RESEARCH.md 假设 A2 得到实测判定并 append-only 回填，07-RESEARCH.md A1 完成交叉引用与消歧
- UI-05 与 PKG-06 各有一条基于证据的核销结论
- D-13 作为已关闭的 discretion 条目落盘（不可行，非待办）
- 本 phase 全程只发一次版，`launch_app.py` / `release` job / v0.2.0 页面零改动
</success_criteria>

<output>
Create `.planning/phases/07-patch-release/07-03-SUMMARY.md` when done.

SUMMARY 中必须严格区分「实测」与「推断」，并如实记录：
- SC1 三组的实际观测（包括任何异常），验收机器与 dmg 文件名
- SC2/SC3 的最终判定，以及是否发生过人工补救
- 06-RESEARCH A2 的判定结论（支持 / 推翻 / 无从判定）与消歧说明
- 关闭按钮这条路径「永久只有人工回归护栏」这一结论，供后续任何 macOS 退出路径改动参考
</output>
