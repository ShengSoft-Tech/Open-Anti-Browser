---
phase: 06-release-docs
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-release-docs/06-VALIDATION.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "Phase 6's success criteria describe the flow Phase 5 measured, so a verifier checking them against the shipped docs finds agreement rather than contradiction"
    - "No acceptance criterion in the roadmap or the requirements can only be satisfied by an Intel package that this milestone does not build"
    - "The reason each criterion changed is recorded inline, so a future reader can tell a correction from a scope cut"
    - "The same terminal command string appears, byte-identical, in every one of the four surfaces that carry it"
    - "The full Python and Node test suites pass together on one machine before the phase reaches human verification"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 success criteria SC1/SC2/SC3 rewritten to the measured flow, arm64-only, with dated rationale"
      contains: "Phase 6"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DOCS-01 and DOCS-02 rewritten to the measured flow and the prerequisite-checklist framing"
      contains: "DOCS-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "DOCS-01/DOCS-02 text and Phase 6 SC1/SC2/SC3 must state the same thing after the rewrite"
      pattern: "DOCS-0[12]"
    - from: ".planning/ROADMAP.md"
      to: ".planning/PROJECT.md"
      via: "the arm64-only rewrite cites the dated Out of Scope entry as its justification"
      pattern: "arm64"
  prohibitions:
    - statement: "The rewrite MUST NOT quietly weaken a criterion the phase actually delivered — every changed criterion must carry an inline, dated reason naming the evidence that superseded it, so a correction is never mistakable for a scope cut"
      status: resolved
      verification: judgment
    - statement: "The rewrite MUST NOT leave an acceptance criterion that no artifact in this milestone can ever satisfy, such as native verification on a package that is not built"
      status: resolved
      verification: judgment
    - statement: "This plan MUST NOT edit .planning/ROADMAP.md by whole-file rewrite — a full-file write outside the Phase 6 region destroys the other five phases' entries; every change must be a scoped replacement"
      status: resolved
      verification: test
---

<objective>
Correct the two planning documents that still describe Phase 6 as it was imagined on 2026-07-23,
before any real hardware had run the app — then prove, across all four shipped surfaces at once,
that they now agree with each other and with the code.

Purpose: RESEARCH Pitfall 1 names this precisely — the requirement text itself is stale, not just
the eventual prose. Leaving ROADMAP SC1 saying "System Settings first" and SC3 demanding native
Intel verification means a verifier checking this phase against its own acceptance criteria would
have to either fail correct work or quietly ignore the criteria. D-14 also refuses to leave behind a
criterion nothing in this milestone can satisfy.

Output: rewritten Phase 6 success criteria and DOCS requirements, plus a recorded cross-surface
parity audit and a green full-suite run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-release-docs/06-CONTEXT.md
@.planning/phases/06-release-docs/06-RESEARCH.md
@.planning/phases/06-release-docs/06-01-SUMMARY.md
@.planning/phases/06-release-docs/06-02-SUMMARY.md
@.planning/phases/06-release-docs/06-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<!-- planner-discipline-allow: x64 -->
<!-- planner-discipline-allow: 双架构 -->
<!-- planner-discipline-allow: Rosetta -->

<task type="auto">
  <name>Task 1: Rewrite Phase 6 success criteria and the DOCS requirements to the measured, arm64-only reality</name>
  <reversibility rating="reversible">D-14 is a pure documentation change. If a later milestone reintroduces the Intel build — its kernel asset is already published under `kernel-149.0.7827.114` — these criteria are edited back with no migration and no dependent system affected.</reversibility>
  <read_first>
    - `.planning/ROADMAP.md` lines 173-199 (the Phase 6 block: Goal, Depends on, Requirements, SC1/SC2/SC3, Plans, and the progress table row)
    - `.planning/ROADMAP.md` lines 136-140 (the Phase 5 block's dated scope-change callout — the established in-file format for recording a scope correction, which this rewrite should mirror)
    - `.planning/REQUIREMENTS.md` lines 45-49 (the DOCS section) and lines 74-109 (the Traceability table and the Coverage note)
    - `.planning/PROJECT.md` § Out of Scope and § Key Decisions (the dated 2026-07-27 entry removing the Intel build from v0.2 — the justification the rewrite must cite)
    - `.planning/phases/05-ci/05-06-SUMMARY.md` lines 175-235 and 410-445 (the measured first-run sequence and the macOS 15.0 floor)
    - `.planning/phases/06-release-docs/06-CONTEXT.md` § D-01, D-05, D-14
    - `.github/RELEASE_NOTES_TEMPLATE.md` (the delivered wording the criteria must now describe)
  </read_first>
  <action>
Edit `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` so their Phase 6 acceptance language
matches what this phase actually ships.

Use scoped `Edit` replacements only. A whole-file `Write` on `.planning/ROADMAP.md` would destroy
the other five phases' entries, the overview, and the progress table; replace only the specific
lines named below.

**ROADMAP, Phase 6 Success Criteria:**

- SC1 — restate the 放行 walkthrough as the progressive chain the release notes actually carry: the
  primary and usually sufficient action is opening the app a second time after the first launch is
  blocked and the app closes itself; the first fallback is the System Settings → Privacy & Security
  → Open Anyway path; the last resort is the terminal command, which must be written with the bundle
  path inside double quotes so it matches the shipped string exactly.
- SC2 — replace the download-chooser framing with the prerequisite checklist the release notes and
  both READMEs carry: an Apple Silicon chip and macOS 15 or newer, self-checkable through the Apple
  menu without a terminal. State that a single arm64 package is published for this milestone; do not
  frame the reader as choosing between packages.
- SC3 — restate the end-to-end criterion as arm64-only, and replace "a Mac that has never had this
  app installed" with the operationally achievable form D-13 defines: a freshly created macOS user
  account on a machine where any prior installation has first been removed from the shared
  `/Applications` directory. Keep the substance of the criterion intact — download, approve, install,
  create a profile, launch a Chrome profile — and add D-15's constraint that the person performing it
  may use only what the shipped documentation says.

Immediately below the Phase 6 Success Criteria list, add a short dated callout in the same visual
style as the Phase 5 block's existing scope-change note, recording three things: that SC1's original
System-Settings-first ordering was superseded by `05-06-SUMMARY.md`'s timestamped real-hardware
capture on 2026-07-28; that SC2's original two-package framing became unsatisfiable when the Intel
build left v0.2 on 2026-07-27, per PROJECT.md; and that SC3's native-Intel half was dropped for the
same reason, with the Intel kernel asset noted as already published so the criterion can be restored
in a later milestone. This note is what keeps the change legible as a correction rather than a
quiet reduction in scope.

Do not change the Phase 6 Goal line, the Depends-on line, the Requirements line, the Plans list, or
any other phase's block.

**REQUIREMENTS, the DOCS section:**

- DOCS-01 — rewrite to match the rewritten SC1: stepwise 放行 instructions whose primary step is
  opening the app a second time, with the System Settings path and the double-quoted terminal
  command as documented fallbacks.
- DOCS-02 — rewrite to match the rewritten SC2: release notes and README carry a prerequisite
  checklist (Apple Silicon plus macOS 15 or newer) with a GUI-first self-check, rather than guidance
  for choosing between packages.

Append a short dated line to the file's existing trailing "Last updated" note recording that DOCS-01
and DOCS-02 were rewritten during Phase 6 and why, citing the same two dated sources. Leave the
checkbox state of both requirements alone — the phase's own execution flow owns that. Leave the
Traceability table, the Coverage counts, the Future Requirements section, and the Out of Scope table
untouched; the requirement identifiers, their count, and their phase mapping are all unchanged.
  </action>
  <verify>
    <automated>test "$(awk '/^### Phase 6:/,/^## Progress/' .planning/ROADMAP.md | grep -Ec 'x64|Intel|Rosetta|双架构')" = "0" && echo "no unsatisfiable multi-architecture criterion remains in the Phase 6 block"</automated>
    <automated>awk '/^### Phase 6:/,/^## Progress/' .planning/ROADMAP.md | grep -q 'macOS 15' && awk '/^### 发布文档 \(DOCS\)/,/^## Future Requirements/' .planning/REQUIREMENTS.md | grep -q 'macOS 15' && echo "macOS 15 floor stated in both documents"</automated>
    <automated>test "$(grep -c '^### Phase ' .planning/ROADMAP.md)" = "6" && test "$(grep -c '^- \[.\] \*\*Phase ' .planning/ROADMAP.md)" = "6" && echo "all six phase blocks and all six phase list entries survive"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` still contains exactly 6 `### Phase ` headings and exactly 6 phase entries in the top-level list — no scoped edit clobbered a sibling phase
    - The Phase 6 block contains zero occurrences of `x64`, `Intel`, `Rosetta`, or `双架构`, verified by the awk-range command in `<verify>`
    - The Phase 6 block contains `macOS 15` and `Apple Silicon`
    - The Phase 6 block contains the double-quoted command form `"/Applications/Open-Anti-Browser.app"`
    - The Phase 6 block contains a dated callout naming both `2026-07-28` (the real-hardware capture) and `2026-07-27` (the Intel-build removal)
    - The Phase 6 `**Goal**:` line, `**Depends on**:` line, and `**Requirements**:` line are byte-identical to their pre-task values, verified by `git diff`
    - `.planning/REQUIREMENTS.md` DOCS section contains `macOS 15` and no longer describes choosing between two packages
    - `grep -c 'DOCS-01' .planning/REQUIREMENTS.md` and `grep -c 'DOCS-02' .planning/REQUIREMENTS.md` are unchanged from their pre-task values (the Traceability table rows survive)
    - `grep -c 'v0.2 requirements: 22 total' .planning/REQUIREMENTS.md` returns 1 — the coverage count was not disturbed
    - `git diff --name-only` for this task lists exactly `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`
    - `git diff --stat .planning/ROADMAP.md` shows changes confined to the Phase 6 region (line numbers within the Phase 6 block's range)
  </acceptance_criteria>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <done>A verifier reading Phase 6's success criteria and the DOCS requirements finds statements the shipped documentation actually satisfies, each correction carrying its dated evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-surface parity audit and full-suite gate before human verification</name>
  <precondition>`pip install -r requirements.txt` has been run in the active environment — `tests/test_macos_desktop_runtime.py` imports `launch_app`, which imports `uvicorn`, so the Python suite cannot run in a bare checkout</precondition>
  <read_first>
    - `.planning/phases/06-release-docs/06-VALIDATION.md` (the sampling contract, the per-task verification map to be filled in, and the manual-only rows)
    - `.planning/phases/06-release-docs/06-01-SUMMARY.md`, `06-02-SUMMARY.md`, `06-03-SUMMARY.md` (what each earlier plan claims to have delivered — the audit checks the claims against the files)
    - `CLAUDE.md` § 常用命令 and § 测试环境 (the two documented test commands and the import constraints on the Python suite)
    - `.github/RELEASE_NOTES_TEMPLATE.md`, `frontend/src/lib/macosGatekeeperNotice.js`, `launch_app.py`, `frontend/src/i18n/zh-CN.js`, `frontend/src/i18n/en-US.js`, `README.md`, `README_EN.md` (the seven surfaces under audit)
  </read_first>
  <action>
Run both documented test suites in full and then audit every user-facing surface this phase touched,
recording the evidence in the SUMMARY rather than asserting the phase is consistent.

Run `python -m unittest discover -s tests -v` and `node --test frontend/src/lib/*.test.js`. Record
the pass counts. If the Python suite cannot start because a dependency is missing, install the
project requirements first rather than skipping it — the Python half is where the JS-constant to
Python-message lock lives, and skipping it would leave one hop of the three-surface lock unchecked.

Then produce, in the SUMMARY, a parity table with one row per surface covering these checks:

- The terminal command string: extract it from `frontend/src/lib/macosGatekeeperNotice.js`, from
  `.github/RELEASE_NOTES_TEMPLATE.md`, and from the output of
  `launch_app.build_quarantine_failure_message(None)`, and show all three side by side. They must be
  the same bytes, with the bundle path inside double quotes.
- The primary 放行 step: quote the first step as it appears in the template's Chinese section, in the
  template's English section, in `gatekeeper.step1` of each locale, and in the regenerated dmg
  background's footer text (from plan 06-03's SUMMARY, since the image is not greppable). All five
  must name opening the app a second time as the action that resolves the block.
- The minimum system version: confirm the same number appears in the template, in both READMEs, in
  `build-release.yml`'s Info.plist patch step, and in the rewritten roadmap and requirements text.
- The forbidden-fragment audit: run the fragment list over the template, both READMEs, and both
  locale files, and record the zero-match output.

Finally, fill in `.planning/phases/06-release-docs/06-VALIDATION.md`'s "Per-Task Verification Map"
rows with the real task ids, plan numbers, waves, threat references, and automated commands from the
executed plans, and set each executed row's status. Leave the two manual-only rows pending — plan
06-05 owns them. Do not set `nyquist_compliant` in that file; that flag belongs to
`/gsd-validate-phase`.

If any parity check disagrees, stop and fix the disagreeing surface rather than recording the
mismatch and continuing — a documented mismatch here is exactly the drift D-02 and D-12 exist to
prevent, and the human verification in plan 06-05 would be run against inconsistent instructions.
  </action>
  <verify>
    <automated>python -m unittest discover -s tests -v</automated>
    <automated>node --test frontend/src/lib/*.test.js</automated>
    <automated>test "$(grep -ho 'xattr -dr com.apple.quarantine "[^"]*"' frontend/src/lib/macosGatekeeperNotice.js .github/RELEASE_NOTES_TEMPLATE.md | sort -u | wc -l | tr -d ' ')" = "1" && echo "template and JS constant render one identical command string"</automated>
    <automated>python3 -c "import launch_app,re,pathlib; msg=launch_app.build_quarantine_failure_message(None); js=re.search(r\"GATEKEEPER_XATTR_COMMAND\s*=\s*'([^']+)'\", pathlib.Path('frontend/src/lib/macosGatekeeperNotice.js').read_text(encoding='utf-8')).group(1); tpl=pathlib.Path('.github/RELEASE_NOTES_TEMPLATE.md').read_text(encoding='utf-8'); assert js in msg and js in tpl, (js, js in msg, js in tpl); print('three-way lock holds:', js)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest discover -s tests -v` exits 0; the SUMMARY records the reported test count
    - `node --test frontend/src/lib/*.test.js` exits 0 with `# fail 0`; the SUMMARY records the reported test count
    - The three-way lock assertion in `<verify>` prints the single command string, and that string contains `"/Applications/Open-Anti-Browser.app"` with both quote characters
    - The SUMMARY contains a parity table with one row per surface for the command string, the primary step, and the minimum system version, with quoted evidence per cell rather than a yes/no mark
    - The forbidden-fragment sweep over `.github/RELEASE_NOTES_TEMPLATE.md`, `README.md`, `README_EN.md`, `frontend/src/i18n/zh-CN.js`, and `frontend/src/i18n/en-US.js` reports zero matches for `sudo`, `spctl`, `--master-disable`, `csrutil`, and `~/Downloads`, with the command and its output quoted
    - `.planning/phases/06-release-docs/06-VALIDATION.md`'s Per-Task Verification Map has no remaining `TBD` in the Task ID, Plan, or Wave columns for the four executed plans
    - `.planning/phases/06-release-docs/06-VALIDATION.md` still has `nyquist_compliant: false` in its frontmatter — that flag is not this plan's to flip
    - `git status --porcelain` is clean at task end
  </acceptance_criteria>
  <files>.planning/phases/06-release-docs/06-VALIDATION.md</files>
  <done>Both suites are green on one machine and every surface has been shown, with quoted evidence, to tell the same story before a human is asked to follow it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning document → verifier | ROADMAP and REQUIREMENTS text is the contract `/gsd-verify-work` checks delivered work against |
| four shipped surfaces → user | The parity audit is the last automated gate before a human is asked to follow the instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Repudiation | Silent weakening of an acceptance criterion during the rewrite | high | mitigate | Task 1 requires an inline dated callout naming both superseding events (`2026-07-28` real-hardware capture, `2026-07-27` Intel removal), and an acceptance criterion asserts both dates are present, so a correction cannot be mistaken for or disguised as a scope reduction |
| T-06-11 | Denial of Service | Whole-file rewrite of `.planning/ROADMAP.md` destroying the other five phases | high | mitigate | `must_haves.prohibitions` and the task action forbid a whole-file write; an acceptance criterion counts the `### Phase ` headings and the top-level phase list entries and requires both to still be 6 |
| T-06-01 | Tampering | The copy-pasteable command, re-checked across all four surfaces | high | mitigate | Task 2's `<verify>` runs the real three-way comparison — JS constant, Python-rendered message, and template — and fails if any pair diverges; this is the last automated point before human verification |
| T-06-02 | Elevation of Privilege | Every user-facing text surface this phase produced | high | mitigate | Task 2 sweeps the forbidden-fragment list across all five greppable text surfaces at once and requires the zero-match output to be quoted; the dmg background's audit is carried forward from plan 06-03's SUMMARY since the asset is an image |
| T-06-SC | Tampering | npm/pip/cargo installs | medium | accept | This plan runs `pip install -r requirements.txt` against the repository's existing, unchanged manifest to make the Python suite runnable; it adds no package and changes no pin, so RESEARCH § Package Legitimacy Audit's not-applicable finding stands. Any change to `requirements.txt` in this task would void that finding and is out of scope |
</threat_model>

<verification>
- `python -m unittest discover -s tests -v` exits 0
- `node --test frontend/src/lib/*.test.js` exits 0 with zero failures
- The three-way command lock assertion passes and prints one string containing the quoted bundle path
- The Phase 6 roadmap block contains no criterion referencing a package this milestone does not build
- `.planning/ROADMAP.md` still contains all six phase blocks
</verification>

<success_criteria>
- Phase 6's success criteria and the DOCS requirements describe what was actually built and what a verifier can actually check
- Every changed criterion carries a dated reason naming its superseding evidence
- All seven touched surfaces are shown, with quoted evidence, to state the same command, the same primary step, and the same minimum system version
- Both documented test suites pass together on one machine
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback — unresolved edge rows)

1. **DOCS-01 and DOCS-02 — both `unresolved` / `unclassified`.** The edge probe was fed the current
   REQUIREMENTS.md text for both, returned `unclassified` for both, and therefore both stay
   `unresolved` rather than being auto-backstopped or dismissed. This plan is the one that closes the
   root cause: it rewrites the stale requirement prose the probe was reading. Note the ordering — the
   probe's inability to classify was a symptom of the requirement text describing a flow that real
   hardware had already falsified and a download choice that no longer exists. After Task 1, a
   re-run of the probe would be reading D-01/D-05-aligned text. Re-running the probe is **not** part
   of this phase; the rows remain flagged, not silently closed.

2. **Carried assumption:** that the rewritten SC3 is achievable as written. D-13's clean-account
   procedure has not been executed yet — plan 06-05 is the first attempt. If a freshly created macOS
   user account turns out not to produce a truthful "never installed" state (for example if some
   quarantine or Gatekeeper record proves machine-scoped rather than user-scoped), SC3 will need
   revisiting again. RESEARCH § Runtime State Inventory records the per-user scoping as the basis for
   this expectation.
</flagged_assumptions>

<artifacts_produced>
## Artifacts this phase produces

New symbols created by this plan (excluded from source-grounding drift verification):

- **New ROADMAP content:** a dated scope-correction callout under Phase 6's Success Criteria, in the same format as the existing Phase 5 callout
- **New REQUIREMENTS content:** an appended dated line on the file's trailing "Last updated" note
- **Rewritten prose (identifiers unchanged):** ROADMAP Phase 6 SC1/SC2/SC3, and REQUIREMENTS DOCS-01/DOCS-02 descriptions
- **Filled-in table rows:** `.planning/phases/06-release-docs/06-VALIDATION.md`'s Per-Task Verification Map
- No new files, no new tests, no new constants, and no new workflow keys are introduced by this plan
</artifacts_produced>

<output>
Create `.planning/phases/06-release-docs/06-04-SUMMARY.md` when done
</output>
