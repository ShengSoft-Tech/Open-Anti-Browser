---
phase: 06-release-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - launch_app.py
  - frontend/src/lib/macosGatekeeperNotice.js
  - frontend/src/lib/macosGatekeeperNotice.test.js
  - frontend/src/lib/releaseNotesTemplate.test.js
  - tests/test_macos_desktop_runtime.py
  - .github/RELEASE_NOTES_TEMPLATE.md
  - .github/workflows/build-release.yml
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "A reader of the GitHub Release body sees 「再双击一次」 as the first and primary 放行 step, matching what Phase 5 measured on real hardware"
    - "The terminal fallback command shown in the Release notes, in the in-app notice constant, and in the Python fallback dialog is one byte-identical string with the bundle path wrapped in double quotes"
    - "A path containing a space no longer splits the copy-pasteable command into two arguments"
    - "The release job passes the committed template file to the GitHub Release body and still appends the auto-generated changelog after it"
    - "Renaming or deleting the release notes template makes the test suite go red instead of silently publishing an empty Release body"
  artifacts:
    - path: ".github/RELEASE_NOTES_TEMPLATE.md"
      provides: "Single source of truth for the 放行 walkthrough injected into every GitHub Release body"
      contains: 'xattr -dr com.apple.quarantine "/Applications/Open-Anti-Browser.app"'
    - path: "frontend/src/lib/releaseNotesTemplate.test.js"
      provides: "Zero-dependency node:test lock between the template, the JS constant, and the workflow wiring"
    - path: "frontend/src/lib/macosGatekeeperNotice.js"
      provides: "GATEKEEPER_XATTR_COMMAND with the quoted canonical bundle path"
    - path: "launch_app.py"
      provides: "build_quarantine_failure_message rendering the quoted command"
  key_links:
    - from: ".github/workflows/build-release.yml"
      to: ".github/RELEASE_NOTES_TEMPLATE.md"
      via: "release job's softprops/action-gh-release@v2 body_path input"
      pattern: "body_path:\\s*\\.github/RELEASE_NOTES_TEMPLATE\\.md"
    - from: "frontend/src/lib/releaseNotesTemplate.test.js"
      to: "frontend/src/lib/macosGatekeeperNotice.js"
      via: "reads GATEKEEPER_XATTR_COMMAND and asserts the template embeds it verbatim"
      pattern: "GATEKEEPER_XATTR_COMMAND"
    - from: "tests/test_macos_desktop_runtime.py"
      to: "frontend/src/lib/macosGatekeeperNotice.js"
      via: "existing regex cross-language literal lock (05-02 precedent), literal updated to the quoted form"
      pattern: "GATEKEEPER_XATTR_COMMAND"
  prohibitions:
    - statement: "The 放行 walkthrough MUST NOT instruct the user to weaken system-wide security: no privilege-escalation prefix, no global Gatekeeper disable switch, no System Integrity Protection change, and no recursive quarantine removal targeting anything broader than the single Open-Anti-Browser.app bundle"
      status: resolved
      verification: test
    - statement: "The 放行 walkthrough MUST NOT claim or imply that the app becomes Apple-signed, notarized, Gatekeeper-approved, or trusted after the user follows the steps — it runs because it is no longer quarantined, and spctl assessment still reports a rejection"
      status: resolved
      verification: judgment
---

<objective>
Prove the whole Phase 6 pipeline end-to-end on the single most load-bearing string in the phase:
the terminal fallback command. One quoted literal has to travel from the JS constant, through the
Python fallback dialog, into a brand-new release-notes template, out through the CI release job's
`body_path`, and stay byte-identical the whole way — with a test that goes red the moment any hop
drifts.

Purpose: D-04's quoting fix and D-09's `body_path` wiring are the two mechanical changes every
other plan in this phase depends on. Landing them together with the 放行 section they carry means
the architecture (source of truth → template → CI → Release body) is proven before any expansion
prose is written. If the wiring is wrong, it is wrong after one commit instead of after five.

Output: `.github/RELEASE_NOTES_TEMPLATE.md` carrying the bilingual progressive 放行 walkthrough,
the quoted `GATEKEEPER_XATTR_COMMAND`, the quoted Python fallback message, `body_path` in the
release job, and a zero-dependency node:test that locks all of it together.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-docs/06-CONTEXT.md
@.planning/phases/06-release-docs/06-RESEARCH.md
@.planning/phases/06-release-docs/06-PATTERNS.md
@.planning/phases/05-ci/05-06-SUMMARY.md
@.planning/phases/05-ci/05-REVIEW.md
@CLAUDE.md
</context>

<tasks>

<!-- planner-discipline-allow: shlex -->
<!-- planner-discipline-allow: continue-on-error -->

<task type="tracer">
  <name>Task 1: End-to-end quoted 放行 command — JS constant → Python dialog → release notes template → CI release body</name>
  <reversibility rating="costly">D-09 changes the `release` job that Phase 5 just accepted and whose real publish path has never executed; a mistake only surfaces at the next `v*` tag push, and undoing it means re-verifying Phase 5's PKG-05 acceptance.</reversibility>
  <read_first>
    - `frontend/src/lib/macosGatekeeperNotice.js` (lines 9-11 hold `GATEKEEPER_XATTR_COMMAND` and the comment stating the single-bundle safety posture)
    - `launch_app.py` lines 23-30 and 113-143 (`CANONICAL_INSTALL_BUNDLE`, `quarantine_command_target`, `build_quarantine_failure_message`, `maybe_strip_quarantine`)
    - `tests/test_macos_desktop_runtime.py` lines 116-142 (`BuildQuarantineFailureMessageTests` — the 05-02 cross-language lock whose literals must change in this same commit)
    - `frontend/src/lib/macosGatekeeperNotice.test.js` lines 111-117 (the prefix-strip assertion that currently expects an unquoted path)
    - `.github/workflows/build-release.yml` lines 785-819 (the whole `release` job: `needs`, the tag guard, the asset verification step, the `Create GitHub Release` step)
    - `.planning/phases/05-ci/05-06-SUMMARY.md` lines 175-235 and 398-445 (the measured first-launch decision sequence — the ONLY factual basis for the 放行 prose — plus Open Items 2 and 4)
    - `.planning/phases/06-release-docs/06-RESEARCH.md` § "Architecture Patterns → Pattern 2" and § "Common Pitfalls" 3 and 4
    - `.planning/phases/06-release-docs/06-PATTERNS.md` § `.github/RELEASE_NOTES_TEMPLATE.md` and § `launch_app.py::build_quarantine_failure_message`
  </read_first>
  <action>
Make one quoted command string exist in exactly one canonical form across four surfaces, and wire
the new template into the release job.

(1) `frontend/src/lib/macosGatekeeperNotice.js` — change `GATEKEEPER_XATTR_COMMAND` so the target
path is wrapped in double quotes, producing exactly `xattr -dr com.apple.quarantine
"/Applications/Open-Anti-Browser.app"` (per D-04, fixing 05-REVIEW WR-01). The export name, the
single-quoted JS string delimiter, and the surrounding comment block stay as they are — the Python
test regexes `GATEKEEPER_XATTR_COMMAND\s*=\s*'([^']+)'`, so the outer delimiter must remain a
single quote. Do not touch `buildGatekeeperNoticeHtml`, `GATEKEEPER_NOTICE_KEY`, or any storage
function.

(2) `launch_app.py` — in `build_quarantine_failure_message`, change the `command` f-string so the
interpolated `target` is wrapped in double quotes. Use an explicit literal double-quote wrap; do
NOT use `shlex.quote`, which is a documented no-op for `/Applications/Open-Anti-Browser.app` and
uses single quotes when it does quote (RESEARCH Pitfall 3) — it would not reproduce the string the
cross-language lock asserts. Leave `quarantine_command_target`, `CANONICAL_INSTALL_BUNDLE`,
`QUARANTINE_ATTRIBUTE`, `strip_quarantine_from_bundle`'s list-form `subprocess.run` call, and
`maybe_strip_quarantine`'s three-tier guard byte-identical. The Chinese prose of the message body
also stays as-is; only the command line changes.

(3) `tests/test_macos_desktop_runtime.py` — update the two literal expectations in
`BuildQuarantineFailureMessageTests` in this same commit (RESEARCH Pitfall 3 flags them as the
existing regression guards that would otherwise fail): the `assertEqual` in
`test_translocated_scenario_matches_frontend_constant` and the `assertIn` f-string in
`test_non_translocated_bundle_message_points_to_its_own_path` both gain the double-quote wrap
around the path. `test_message_contains_no_dangerous_command_fragments` is orthogonal to quoting —
leave it untouched.

(4) `frontend/src/lib/macosGatekeeperNotice.test.js` — the last test in the file (the one asserting
`GATEKEEPER_XATTR_COMMAND` targets a single `.app` bundle and carries no privilege-escalation or
Gatekeeper-disabling fragment) strips the fixed prefix and then checks the remainder ends with
`.app`; that check now fails because the remainder ends with a quote character.
Change it to an exact-literal equality assertion against the full quoted target
(`"/Applications/Open-Anti-Browser.app"` including both quote characters). Keep the two
forbidden-fragment assertions in that test exactly as they are — they are the Phase 4/5 safety
boundary and must persist.

(5) Create `.github/RELEASE_NOTES_TEMPLATE.md` carrying the bilingual 放行 walkthrough only (the
prerequisite checklist and the trust caveat arrive in plan 06-02, which owns the same file in wave
2). Structure it per D-03/D-10 and RESEARCH Pattern 2: a Chinese `## ` section first, then an
English `## ` section with the same content. Inside each section, step 1 is a visible, standalone,
bolded instruction — double-click again after the first launch is blocked and the app closes itself
— and steps 2 and 3 live inside a collapsed `<details>` block so a normal reader only reads step 1.
Step 2 is the 系统设置 → 隐私与安全性 → 安全性 → 仍要打开 path. Step 3 is the terminal command,
embedded verbatim as `xattr -dr com.apple.quarantine "/Applications/Open-Anti-Browser.app"` inside a
fenced code block, followed by one sentence saying that a different install location means
substituting the path inside the quotes. Frame the block as expected behaviour, not a fault,
matching the tone already established in `gatekeeper.intro`. Every factual claim about what
actually happens must trace to `05-06-SUMMARY.md`'s measured sequence — nothing from code comments
or recall. The command shown to the user must stay scoped to that one application bundle; the
forbidden fragment list is enumerated in the acceptance criteria below and is the same boundary
Phase 4 D-04 and Phase 5 T-05-01 already established.

(6) `.github/workflows/build-release.yml` — in the `release` job's `Create GitHub Release` step,
add `body_path: .github/RELEASE_NOTES_TEMPLATE.md` to the existing `with:` block and keep
`generate_release_notes: true` (D-09: the auto changelog appends after the template body). Add
nothing else: `needs: [build, build-macos]`, the `if: startsWith(github.ref, 'refs/tags/')` guard,
the `Download all build artifacts` step, and the `List and verify release assets` step all stay
byte-identical, and the action pin stays at `softprops/action-gh-release@v2` (RESEARCH explicitly
scopes the v3 bump out of this phase).
  </action>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
    <automated>test "$(grep -o 'xattr -dr com.apple.quarantine "[^"]*"' .github/RELEASE_NOTES_TEMPLATE.md | sort -u | wc -l | tr -d ' ')" = "1"</automated>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/build-release.yml')); s=[x for x in d['jobs']['release']['steps'] if x.get('uses','').startswith('softprops/action-gh-release')][0]; assert s['with']['body_path']=='.github/RELEASE_NOTES_TEMPLATE.md'; assert s['with']['generate_release_notes'] is True; print('body_path wired, generate_release_notes retained')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'xattr -dr com.apple.quarantine "/Applications/Open-Anti-Browser.app"' frontend/src/lib/macosGatekeeperNotice.js` returns 1
    - `grep -Eq "GATEKEEPER_XATTR_COMMAND\s*=\s*'" frontend/src/lib/macosGatekeeperNotice.js` succeeds (outer delimiter is still a single quote, so the Python regex lock still matches)
    - `grep -cF '{QUARANTINE_ATTRIBUTE} "{target}"' launch_app.py` returns 1 — the Python renderer interpolates the target inside literal double quotes
    - `grep -c 'shlex' launch_app.py` returns 0
    - `.github/RELEASE_NOTES_TEMPLATE.md` exists and `grep -c 'xattr -dr com.apple.quarantine "/Applications/Open-Anti-Browser.app"' .github/RELEASE_NOTES_TEMPLATE.md` returns 1
    - `grep -c 'sudo' .github/RELEASE_NOTES_TEMPLATE.md` returns 0
    - `grep -c 'spctl' .github/RELEASE_NOTES_TEMPLATE.md` returns 0
    - `grep -c -- '--master-disable' .github/RELEASE_NOTES_TEMPLATE.md` returns 0
    - `grep -c '~/Downloads' .github/RELEASE_NOTES_TEMPLATE.md` returns 0
    - `grep -c 'csrutil' .github/RELEASE_NOTES_TEMPLATE.md` returns 0
    - `.github/RELEASE_NOTES_TEMPLATE.md` contains exactly two `<details>` opening tags (one per language) and at least two `## ` headings
    - `grep -c '右键' .github/RELEASE_NOTES_TEMPLATE.md` returns 0 — the superseded right-click guidance from the shipped dmg background is not reproduced (05-06 Open Item 2)
    - `node --test frontend/src/lib/*.test.js` exits 0 with 0 failures
    - `python3 -c` YAML assertion in `<verify>` prints `body_path wired, generate_release_notes retained`
    - `git diff --name-only` for this task lists exactly the six files in `<files>` and no others — in particular `frontend/src/App.vue`, `frontend/src/lib/openSourceNotice.js`, and `backend/_g.py` are untouched
  </acceptance_criteria>
  <files>frontend/src/lib/macosGatekeeperNotice.js, frontend/src/lib/macosGatekeeperNotice.test.js, launch_app.py, tests/test_macos_desktop_runtime.py, .github/RELEASE_NOTES_TEMPLATE.md, .github/workflows/build-release.yml</files>
  <done>The quoted command renders identically from the JS constant, the Python fallback message, and the template; the release job hands the template to the Release body; `node --test frontend/src/lib/*.test.js` is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the three surfaces and the CI wiring with a zero-dependency node:test</name>
  <precondition>`.github/RELEASE_NOTES_TEMPLATE.md` exists at the repository root's `.github/` directory (created by Task 1)</precondition>
  <read_first>
    - `frontend/src/lib/macosGatekeeperNotice.test.js` (the node:test conventions this repo uses: `node:test` + `node:assert/strict`, one behaviour per `test()`)
    - `frontend/src/lib/i18n-parity.test.js` lines 1-40 (how an existing lib test imports sibling modules and builds derived structures)
    - `tests/test_macos_desktop_runtime.py` lines 116-142 (the 05-02 two-way lock this test extends to a third surface)
    - `.planning/phases/06-release-docs/06-PATTERNS.md` § "New D-12 cross-file consistency test" and § "Shared Patterns → Cross-file verbatim consistency lock"
    - `.planning/phases/06-release-docs/06-VALIDATION.md` § "Wave 0 Requirements"
    - `CLAUDE.md` § 常用命令 (the frontend test command is `node --test frontend/src/lib/*.test.js`, which is why a new node test must live under `frontend/src/lib/`)
  </read_first>
  <behavior>
    - Given the committed template and the JS module, the command literal exported as `GATEKEEPER_XATTR_COMMAND` appears verbatim inside `.github/RELEASE_NOTES_TEMPLATE.md`
    - Given the template, none of the privilege-escalation / global-disable / wide-recursion fragments appear anywhere in it
    - Given `build-release.yml`, the release job's release-creation step declares a `body_path` and that path resolves to a file that exists on disk
    - Given `build-release.yml`, `generate_release_notes: true` is still present so the auto changelog is not lost
    - Deleting or renaming `.github/RELEASE_NOTES_TEMPLATE.md` makes the suite fail rather than pass vacuously
  </behavior>
  <action>
Create `frontend/src/lib/releaseNotesTemplate.test.js` implementing the behaviours above.

Placement rationale (D-12 left the side to the planner's discretion): the node side is chosen
because `node --test frontend/src/lib/*.test.js` runs with zero installed dependencies, whereas
`tests/test_macos_desktop_runtime.py` imports `launch_app`, which imports `uvicorn` — unavailable
in a bare checkout, so the Python half only runs after `pip install -r requirements.txt`. Splitting
the lock this way keeps every hop covered: this new node test locks template ↔ JS constant, and the
already-existing `BuildQuarantineFailureMessageTests` locks JS constant ↔ Python-rendered message,
so the three surfaces are transitively byte-locked and at least one half is runnable anywhere.

Resolve repository-root paths from `import.meta.url` (the test file sits four levels below the
repository root) and read both `.github/RELEASE_NOTES_TEMPLATE.md` and
`.github/workflows/build-release.yml` with `node:fs`'s `readFileSync` at `utf-8`. Import
`GATEKEEPER_XATTR_COMMAND` from `./macosGatekeeperNotice.js` rather than re-declaring the literal —
re-declaring it would create a fourth copy and defeat the purpose of the lock.

Parse the workflow as text, not YAML (no YAML parser is available to `node:test` without adding a
dependency, and this phase adds none): extract the `body_path:` value with a regular expression,
assert it is non-empty, and assert `existsSync` on the resolved path. Assert the literal
`generate_release_notes: true` is still present. Do not use an error-suppressing fallback anywhere
in the file-reading path — a missing file must throw and fail the test, never default to an empty
string that would make an assertion pass vacuously.

For the forbidden-fragment check, iterate a `const` array of the fragments enumerated in the
acceptance criteria below and assert each is absent from the template text, mirroring the shape of
`test_message_contains_no_dangerous_command_fragments` in `tests/test_macos_desktop_runtime.py`.

Write the test bodies first and confirm they go red against a deliberately broken input (temporarily
point the template path at a non-existent file, or temporarily strip the quotes from the template's
command) before confirming green against the real files. Comments in this file are Chinese, matching
every other test file in `frontend/src/lib/`.
  </action>
  <verify>
    <automated>node --test frontend/src/lib/releaseNotesTemplate.test.js</automated>
    <automated>node --test frontend/src/lib/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/releaseNotesTemplate.test.js` exists and `grep -c "from './macosGatekeeperNotice.js'" frontend/src/lib/releaseNotesTemplate.test.js` returns 1
    - `grep -c "2>/dev/null" frontend/src/lib/releaseNotesTemplate.test.js` returns 0 and the file contains no `try {` around the template read — a missing template throws
    - The forbidden-fragment array in the test contains, at minimum, the string values `sudo`, `spctl`, `--master-disable`, `~/Downloads`, and `csrutil`
    - `node --test frontend/src/lib/releaseNotesTemplate.test.js` exits 0 and reports at least 4 passing tests
    - `node --test frontend/src/lib/*.test.js` exits 0 with `# fail 0`
    - Fail-first evidence is recorded in the SUMMARY: the exact assertion message produced when the template path is pointed at a non-existent file, proving the test is not vacuous
    - `git diff --name-only` for this task lists only `frontend/src/lib/releaseNotesTemplate.test.js`
  </acceptance_criteria>
  <files>frontend/src/lib/releaseNotesTemplate.test.js</files>
  <done>A renamed, emptied, or drifted template, and a removed `body_path`, each turn `node --test frontend/src/lib/*.test.js` red.</done>
</task>

<task type="auto">
  <name>Task 3: workflow_dispatch regression proving the modified release job still parses and keeps its job graph</name>
  <precondition>`gh auth status` reports an authenticated account with push access to the repository's `origin` remote</precondition>
  <read_first>
    - `.github/workflows/build-release.yml` lines 785-820 (the modified `release` job)
    - `.planning/phases/05-ci/05-05-SUMMARY.md` (the `workflow_dispatch` regression precedent — run 30402103536, "release=skipped as designed")
    - `.planning/phases/06-release-docs/06-RESEARCH.md` § "Common Pitfalls → Pitfall 5" (what a dispatch run can and cannot prove)
  </read_first>
  <action>
Commit and push Task 1 and Task 2's work to the current branch, then trigger
`.github/workflows/build-release.yml` via `gh workflow run` on that same branch and watch the run to
completion with `gh run watch`. Record, in the SUMMARY, the run id and the per-job conclusion for
`build`, `build-macos`, and `release`.

The expected and required outcome is: `build` success, `build-macos` success, `release` **skipped**.
`release` is skipped because its `if: startsWith(github.ref, 'refs/tags/')` guard cannot be true on a
branch ref — this is the designed behaviour Phase 5 already observed, not a failure. This run
therefore proves exactly two things and nothing more: the modified YAML parses server-side and the
`needs: [build, build-macos]` job graph is intact. It cannot prove how `body_path` renders in a real
Release body; that residual risk is D-09's and is carried to plan 06-05's gated decision. Record
that limitation verbatim in the SUMMARY rather than describing `body_path` as verified.

This job takes roughly an hour of wall-clock time (`build-macos` dominates). Do not add
`continue-on-error`, do not narrow the matrix, and do not otherwise modify the workflow to make it
faster — the point of the regression is that the workflow runs exactly as it will at tag time.

If `build` or `build-macos` fails, treat it as a real regression from this plan's workflow edit,
diagnose against the run log, and fix before proceeding. If `release` reports anything other than
`skipped`, stop and surface it — that would mean the tag guard changed.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/build-release.yml')); assert set(d['jobs']) >= {'build','build-macos','release'}; assert d['jobs']['release']['needs']==['build','build-macos']; assert d['jobs']['release']['if'].startswith(\"startsWith(github.ref\"); print('job graph intact')"</automated>
    <automated>gh run list --workflow build-release.yml --limit 1 --json databaseId,conclusion,headBranch</automated>
  </verify>
  <acceptance_criteria>
    - `gh run view <run-id> --json jobs` shows `build` conclusion `success`
    - `gh run view <run-id> --json jobs` shows `build-macos` conclusion `success`
    - `gh run view <run-id> --json jobs` shows `release` conclusion `skipped`
    - The SUMMARY records the run id, the three conclusions, and one explicit sentence stating that `body_path` rendering remains unverified until a real `v*` tag push
    - `git status --porcelain` is clean at task end (nothing uncommitted)
    - `grep -c 'continue-on-error' .github/workflows/build-release.yml` returns 0
  </acceptance_criteria>
  <files>none — CI verification only, no repository files change in this task</files>
  <done>A real `workflow_dispatch` run of the modified workflow completed with build and build-macos green and release skipped as designed, and the un-provable half is written down rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app / docs → user's Terminal | A command string rendered by the app or by the Release notes is copy-pasted by a human into a privileged interactive shell |
| repository file → public GitHub Release body | `body_path` publishes repo-controlled Markdown to a user-visible page |
| unsigned `.app` → macOS Gatekeeper | The app ships ad-hoc signed; the docs describe how the user crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `launch_app.py::build_quarantine_failure_message`, `GATEKEEPER_XATTR_COMMAND`, template command block | high | mitigate | Task 1 wraps the interpolated bundle path in literal double quotes so a path containing a space cannot split into two shell arguments (D-04 / 05-REVIEW WR-01); Task 2 locks the rendered string byte-identical across all three surfaces so a future edit to one cannot silently unquote it |
| T-06-02 | Elevation of Privilege | `.github/RELEASE_NOTES_TEMPLATE.md` prose (NEW text surface) | high | mitigate | Task 1 acceptance criteria assert zero occurrences of privilege-escalation prefixes, the global Gatekeeper disable switch, System Integrity Protection tooling, and wide-directory targets; Task 2 turns the same list into a standing automated gate. Carries `T-05-01`/`T-05-25`'s boundary verbatim into the new surface |
| T-06-03 | Spoofing | GitHub Release body content | medium | accept | The body originates from a file in the repository, reaching the Release only through a tag push by a maintainer with write access; tampering is equivalent to tampering with the repository itself, which GitHub's commit history and branch protection cover — same rationale as `T-05-03` |
| T-06-04 | Repudiation | Claim of Gatekeeper trust in user-facing prose | medium | mitigate | `must_haves.prohibitions` forbids implying the app becomes Apple-signed/notarized/approved; the plain-fact framing is required by 05-06 Open Item 4 (`spctl --assess` still reports a rejection) and enforced at plan 06-02's caveat task and the phase parity audit |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; RESEARCH § Package Legitimacy Audit records the gate as not applicable for this phase. Every tool used (`node`, `python3`, `gh`) is already present in the project's documented toolchain |
</threat_model>

<verification>
- `node --test frontend/src/lib/*.test.js` exits 0 with zero failures
- `python -m unittest tests.test_macos_desktop_runtime -v` exits 0 (requires `pip install -r requirements.txt`; CI's `ci-tests.yml` runs it on both `windows-latest` and `macos-latest`)
- The quoted command string is grep-identical in `frontend/src/lib/macosGatekeeperNotice.js`, `.github/RELEASE_NOTES_TEMPLATE.md`, and the output of `launch_app.build_quarantine_failure_message(None)`
- `.github/workflows/build-release.yml` parses as YAML and its `release` job declares `body_path` pointing at a file that exists
- A real `workflow_dispatch` run completed with `build`/`build-macos` success and `release` skipped
</verification>

<success_criteria>
- One byte-identical quoted `xattr` command exists across three surfaces, provably locked by tests that go red on drift
- The GitHub Release body will contain the hand-written 放行 walkthrough followed by the auto-generated changelog
- The primary 放行 instruction a user reads is the one Phase 5 measured on real hardware, not the superseded System-Settings-first or right-click guidance
- No text surface added by this plan instructs the user to weaken system-wide macOS security
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback — unresolved edge rows)

This phase has no SPEC file, so the edge-coverage probe was fed the current REQUIREMENTS.md prose.
Both rows returned `unclassified` and therefore remain **`unresolved`**. They are surfaced here
rather than auto-backstopped or dropped:

1. **DOCS-01 — `unresolved` / `unclassified`.** The probe classified the requirement text
   "Release notes 提供分步放行说明(启动被拦 → 系统设置 → 隐私与安全性 → 仍要打开…)" and could not
   derive a defensible acceptance criterion. That requirement text is **stale**: CONTEXT.md D-01
   supersedes it with the Phase 5 measured path (blocked once → app exits → double-click again).
   This plan is written against D-01, not against the requirement prose. The requirement text itself
   is rewritten in plan 06-04. Assumption carried: that D-01's measured path generalises beyond the
   single macOS 15.7 arm64 machine it was observed on — D-03's fallback chain exists precisely
   because this is unproven.

2. **DOCS-02 — `unresolved` / `unclassified`.** Not owned by this plan (see 06-02).
</flagged_assumptions>

<artifacts_produced>
## Artifacts this phase produces

New symbols created by this plan (excluded from source-grounding drift verification because they do
not exist yet at plan time):

- **New file:** `.github/RELEASE_NOTES_TEMPLATE.md`
- **New file:** `frontend/src/lib/releaseNotesTemplate.test.js`
- **New workflow YAML key:** `body_path` under `jobs.release.steps[].with` in `.github/workflows/build-release.yml`
- **New node:test case names** (in `releaseNotesTemplate.test.js`): a template↔constant verbatim-embedding test, a forbidden-fragment absence test, a `body_path`-resolves-to-an-existing-file test, and a `generate_release_notes`-retained test
- **Changed value (name unchanged):** `GATEKEEPER_XATTR_COMMAND` in `frontend/src/lib/macosGatekeeperNotice.js` — the exported identifier already exists; only its string value gains the quote characters
</artifacts_produced>

<output>
Create `.planning/phases/06-release-docs/06-01-SUMMARY.md` when done
</output>
