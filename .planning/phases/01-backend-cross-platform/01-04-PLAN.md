---
phase: 01-backend-cross-platform
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - .github/workflows/ci-tests.yml
autonomous: true
requirements: [XPLAT-01, XPLAT-02, XPLAT-03, XPLAT-04]
user_setup: []

must_haves:
  truths:
    - "push 与 pull_request 触发新增 CI workflow `.github/workflows/ci-tests.yml`,与既有 build-release.yml(v* tag 触发发版)互不干扰"
    - "workflow 含 windows-latest job:pip install -r requirements.txt(含 pywin32/ruyipage)后跑全量 `python -m unittest discover -s tests -v`(D-11 的持续化)"
    - "workflow 含 macos job:pip install -r requirements.txt(跳过 pywin32/ruyipage 成功,即 XPLAT-01 的 CI 断言)后跑经实测圈定的 macOS 可运行测试范围(全量或子集 + 明确 skip 列表)"
    - "macOS 可运行测试范围由实际在 macOS 上跑一遍 `python -m unittest discover -s tests -v` 的结果确定,不凭假设"
  artifacts:
    - .github/workflows/ci-tests.yml
  key_links:
    - "ci-tests.yml 的 macos job 依赖 Plan 01 打通的 import 链 + Plan 01/02/03 新增的四个测试文件 —— 这些测试在 macOS 上可跑是本 workflow 变绿的前提"
    - "windows-latest job 跑全量 unittest —— 把 D-11 的一次性 Windows 手动验证转为每次 push/PR 的持续零回归保障"
  prohibitions:
    - statement: "禁止改动或删除既有 .github/workflows/build-release.yml(发版 workflow),ci-tests.yml 是新增的独立文件"
      status: flagged-unverified
    - statement: "禁止在 CI 里构建 Chromium 内核或下载引擎资产(本 phase 是纯后端适配;引擎构建属 Phase 2、打包属 Phase 5)"
      status: flagged-unverified
    - statement: "禁止凭假设写死 macOS 测试范围 —— 必须先在 macOS 上实跑 discover 得到通过/失败清单,再据实圈定全量或子集+skip"
      status: flagged-unverified
---

<objective>
新增一个 push/PR 触发的 CI 测试 workflow(`.github/workflows/ci-tests.yml`),把「Windows 零回归」从 D-11 的一次性手动验证升级为持续保障:windows-latest 跑全量 unittest,macos runner 跑经实测圈定的可运行测试范围(其中 `pip install -r requirements.txt` 在 macOS 成功本身即 XPLAT-01 的 CI 级断言)。

Purpose: Plan 01/02/03 已让后端在 macOS 可导入、可测,并新增了四个平台分支测试文件。本 plan 把这一切钉进 CI,让两平台的零回归/跨平台行为每次提交都被验证。这是本 phase 的收口 plan(Wave 3),必须在前三个 plan 的代码与测试就位后执行。
Output: 双 runner(windows-latest + macos)的测试 workflow + 一份「macOS 上哪些测试可跑」的实测结论。

覆盖需求(CI 级验证):XPLAT-01(macOS pip install 成功)、XPLAT-02、XPLAT-03、XPLAT-04(四个平台分支测试在 CI 运行)。实现决策:D-11(Windows 全量作为持续保障)、D-12(新增独立测试 workflow)、D-13(新增测试在两平台可跑)。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-cross-platform/01-CONTEXT.md
@.planning/phases/01-backend-cross-platform/01-RESEARCH.md
@.planning/phases/01-backend-cross-platform/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实测圈定 macOS 可运行测试范围</name>
  <files>tests/(只读运行,不改测试文件)</files>
  <read_first>
    - .planning/phases/01-backend-cross-platform/01-RESEARCH.md(Open Questions 1:7 个测试文件中 test_firefox_extensions_and_selenium / test_concurrent_profile_storage / test_api_docs_content 经导入链触达 browser_manager/main,Phase 1 后应可导入;test_sync_regressions 顶层不含 win32;需实跑确认)
    - .planning/phases/01-backend-cross-platform/01-RESEARCH.md(Validation Architecture:Sampling Rate、Wave 0 Gaps —— 四个新测试文件清单)
    - tests/ 目录现有全部测试文件(test_api_docs_content / test_concurrent_profile_storage / test_firefox_extensions_and_selenium / test_geo_resolution / test_launch_geo_fallback / test_proxy_bypass_domains / test_sync_regressions 各文件的顶部 import 语句)
  </read_first>
  <precondition>本机为 macOS(darwin);已执行 `pip install -r requirements.txt`(pywin32/ruyipage 被跳过),Plan 01/02/03 的代码改动与四个新测试文件已在工作区就位。</precondition>
  <action>
在 macOS 上实跑 `python -m unittest discover -s tests -v`(从仓库根目录),记录每个测试模块/用例的通过、失败、跳过状态。对失败项判定原因:属「真正依赖 Windows 专属行为(如窗口句柄断言、win32 API 调用)」的用例应在 CI macos job 用 `@unittest.skipUnless(sys.platform == "win32", ...)` 排除(若该 skip 尚未在测试文件里,记录需补 skip 的用例清单供后续处理);属「缺可选依赖(如某些网络/浏览器依赖)」而非平台原因的,单独归类。产出一份结论:macOS CI 应跑「全量」还是「子集 + 明确 skip 列表」,以及 windows-latest 跑全量的确认。此结论直接决定 Task 2 的 macos job 命令。不修改任何测试文件的断言逻辑(如需补 skipUnless 属既有测试的最小平台守卫,记录清单,不改断言)。
  </action>
  <verify>
    <automated>python -m unittest discover -s tests -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - macOS 上 `python -m unittest discover -s tests -v` 已实跑并记录完整通过/失败/跳过结果
    - 四个新测试文件(test_window_manager_posix / test_runtime_control_posix / test_config_platform / test_synchronizer_platform_gate)在 macOS 上全部通过
    - 产出明确结论:macOS CI job 跑全量 或 子集+skip 列表(含每个 skip 用例的平台原因),记录在 SUMMARY
    - 结论区分「平台原因需 skip」与「缺可选依赖」两类失败,不混为一谈
  </acceptance_criteria>
  <done>已在 macOS 实测得到测试通过/失败清单,并据实圈定 macOS CI 测试范围;结论写入 SUMMARY,供 Task 2 写 workflow 命令。</done>
</task>

<task type="auto">
  <name>Task 2: 编写双 runner CI 测试 workflow(D-12)</name>
  <files>.github/workflows/ci-tests.yml</files>
  <read_first>
    - .github/workflows/build-release.yml:1-30(参考现有唯一 workflow 的 YAML 结构:on/permissions/jobs/runs-on/setup-python@v5(python 3.11)/actions/checkout@v4;注意其触发为 v* tag,本 workflow 触发不同)
    - Task 1 SUMMARY 结论(macOS 测试范围:全量或子集+skip)
    - .planning/phases/01-backend-cross-platform/01-CONTEXT.md(D-12:push/PR 触发,独立于发版 workflow;命名/触发细节属 Claude's Discretion)
  </read_first>
  <action>
新建 `.github/workflows/ci-tests.yml`(D-12,文件名可自定,须区别于 build-release.yml):
- 触发:`on: [push, pull_request]`(可加 workflow_dispatch);不使用 v* tag 触发(那是发版 workflow 的职责)。
- job A `windows-latest`:actions/checkout@v4 → setup-python@v5(python 3.11,与 build-release.yml 一致)→ `pip install -r requirements.txt`(会装 pywin32/ruyipage)→ `python -m unittest discover -s tests -v`(全量,D-11 持续化)。
- job B macos runner(如 macos-latest;若无特殊需求用 macos-latest 即可,arm64 runner 细节属 Phase 5 打包范围,本 phase 只需一个 macOS runner 跑测试):actions/checkout@v4 → setup-python@v5(python 3.11)→ `pip install -r requirements.txt`(macOS 上跳过 pywin32/ruyipage 成功,此步成功即 XPLAT-01 的 CI 断言)→ 按 Task 1 结论跑全量 `python -m unittest discover -s tests -v` 或子集命令。
- 两个 job 并行、互不 needs 依赖;失败即红。不构建前端、不下载引擎、不打包(纯后端测试)。不改动 build-release.yml。
  </action>
  <verify>
    <automated>test -f .github/workflows/ci-tests.yml && grep -q 'windows-latest' .github/workflows/ci-tests.yml && grep -q 'macos' .github/workflows/ci-tests.yml && grep -q 'unittest' .github/workflows/ci-tests.yml && grep -q 'pull_request' .github/workflows/ci-tests.yml && echo 'ci-tests.yml structure ok'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci-tests.yml` 存在,且含 `windows-latest`、macos runner、`unittest`、`pull_request`(与 `push`)触发关键字
    - windows-latest job 步骤链:checkout → setup-python 3.11 → `pip install -r requirements.txt` → 全量 `python -m unittest discover -s tests -v`
    - macos job 步骤链:checkout → setup-python 3.11 → `pip install -r requirements.txt` → 按 Task 1 结论的测试命令(全量或子集)
    - 既有 `.github/workflows/build-release.yml` 未被改动(本 plan git diff 不含该文件)
    - workflow 不含前端构建 / 引擎下载 / 打包步骤(纯后端测试)
    - (可选)`actionlint .github/workflows/ci-tests.yml` 无错误(若环境有 actionlint)
  </acceptance_criteria>
  <done>ci-tests.yml 就位:windows-latest 全量 + macos 按实测范围,push/PR 触发,与发版 workflow 独立;结构校验通过并已提交。</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces(Plan 04)

- `.github/workflows/ci-tests.yml`(新文件):CI 测试 workflow,含 job A `windows-latest`(全量 unittest)与 job B macos runner(pip install 成功 + 实测圈定范围的 unittest)
- 无源码符号新增;既有 build-release.yml 不改动
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → 仓库 | CI workflow 在 push/PR 时以只读方式检出并运行测试,不需要写权限、不需要 secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Elevation of Privilege | ci-tests.yml 的 GITHUB_TOKEN 权限 | low | mitigate | 测试 workflow 不需要写权限;不声明 `permissions: contents: write`(区别于发版 workflow),默认最小权限即可,不引入 secret |
| T-01-SC | Tampering | CI 中 `pip install -r requirements.txt` | low | accept | 安装的是既有已审计依赖(RESEARCH Package Legitimacy Audit),无新包引入;CI 不构建/下载引擎二进制,供应链面不扩大 |
</threat_model>

<verification>
- `.github/workflows/ci-tests.yml` 结构校验通过(含 windows-latest / macos / unittest / pull_request)
- 推 push/PR 后,windows-latest job 全量 unittest 变绿、macos job(pip install 成功 + 测试范围)变绿(D-12 双 runner 持续保障)
- build-release.yml 未受影响
</verification>

<success_criteria>
D-12 落地:push/PR 触发的 ci-tests.yml 在 windows-latest(全量)与 macos(实测范围)两个 runner 上运行,把 XPLAT-01~04 的跨平台行为与 Windows 零回归转为每次提交的持续保障;发版 workflow 不受影响。
</success_criteria>

<flagged_assumptions>
边缘探针未分类项(specless fallback,需人工审阅,严禁静默丢弃):
- **XPLAT-02(concurrency,已在 Plan 03 以 backstop 记录):** 并发/重复调用平台门禁端点的行为由 Plan 03 must_haves backstop 覆盖;CI 层不额外并发压测,若人工审阅认为需要并发回归用例,单独立项。
- macos runner 具体标签(macos-latest vs 特定架构)属 Claude's Discretion + Phase 5 打包范围;本 phase 只要求「一个 macOS runner 能跑测试」,arm64/x64 矩阵留待 Phase 5(PKG-01)。
</flagged_assumptions>

<output>
Create `.planning/phases/01-backend-cross-platform/01-04-SUMMARY.md` when done
</output>