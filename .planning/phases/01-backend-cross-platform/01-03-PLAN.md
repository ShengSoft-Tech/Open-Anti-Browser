---
phase: 01-backend-cross-platform
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/services/synchronizer.py
  - tests/test_synchronizer_platform_gate.py
autonomous: true
requirements: [XPLAT-02]
user_setup: []

must_haves:
  truths:
    - "macOS(非 win32)上 BrowserSynchronizer.start(master, followers, options) 在任何参数校验之前抛 RuntimeError,detail 为「窗口同步仅在 Windows 上可用」"
    - "经 main.py `/api/synchronizer/start` 调用时,该 RuntimeError 被既有 try/except 转为 HTTPException 400 + 中文 detail(无需改 main.py)"
    - "win32 上 BrowserSynchronizer.start 保持原有校验与启动逻辑不变(门禁只在非 win32 触发)"
    - "main.py 的 open_system_url(`/api/system/open-url`)已是跨平台实现(os.name == 'nt' → os.startfile,else → webbrowser.open_new_tab),D-04 核销为验证项、不改代码"
    - { statement: "macOS 上并发/重复调用同步器 start 与四个窗口排列端点,均各自独立返回 400「仅在 Windows 上可用」,无半可用状态、无残留会话(D-03 同步器整体不可用;XPLAT-02 concurrency edge)", verification: backstop }
  artifacts:
    - backend/services/synchronizer.py
    - tests/test_synchronizer_platform_gate.py
  key_links:
    - "BrowserSynchronizer.start(synchronizer.py:1393)是 /api/synchronizer/start 的唯一执行体 —— D-03 要求的同步器整体门禁必须落在这里(window_manager 桩函数覆盖另外四个窗口排列端点)"
    - "main.py:245-250 的 start_synchronizer 端点 try/except → HTTPException(400) —— RuntimeError 自动冒泡为 400,门禁无需改路由层"
  prohibitions:
    - statement: "禁止把已跨平台的 main.py open_system_url(os.startfile 分支)当作待修复项改动 —— RESEARCH 已核销 D-04,diff 若显示改动前后逻辑一致即说明该任务本不该执行(Pitfall 1)"
      status: flagged-unverified
    - statement: "禁止顺手统一 main.py 现有的 `os.name == \"nt\"` 判断为 sys.platform,或借本 phase 改动版本号(scope creep,Pitfall 3)"
      status: flagged-unverified
    - statement: "禁止改动 synchronizer.py 中 win32 路径的同步逻辑(CDP/Marionette 事件转发),门禁只在 start 顶部新增一处非 win32 拦截"
      status: flagged-unverified
    - statement: "禁止在 main.py 路由层或 browser_manager 里另加平台判断,同步器门禁必须收敛在 BrowserSynchronizer.start 内部"
      status: flagged-unverified
---

<objective>
在 `BrowserSynchronizer.start`(`backend/services/synchronizer.py:1393`)顶部新增一处平台门禁:非 Windows 一律抛 RuntimeError(「窗口同步仅在 Windows 上可用」),使 macOS 上同步器整体不可用、不出现半可用状态(D-03)。同时把 D-04(main.py `os.startfile`)核销为验证项 —— RESEARCH 已确认该端点在当前代码库中早已是跨平台实现,本 plan 只做冒烟验证、不改代码。

Purpose: 窗口排列四个端点由 Plan 01 的 window_manager 桩函数自然拦截,但同步器启动(`/api/synchronizer/start`)走的是独立的 CDP/Marionette 转发链,没有天然门禁点,需在 start 单点新增拦截,补齐 XPLAT-02「窗口排列/同步 API 在 macOS 返回错误」的最后一块。
Output: synchronizer 平台门禁 + 门禁单元测试;main.py 跨平台 URL 打开端点的验证确认。

覆盖需求:XPLAT-02。实现决策:D-02(错误响应沿用 HTTPException 400 现状)、D-03(同步器 start 门禁 + 拦截范围)、D-04(os.startfile 核销)。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-cross-platform/01-CONTEXT.md
@.planning/phases/01-backend-cross-platform/01-RESEARCH.md
@.planning/phases/01-backend-cross-platform/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 同步器启动平台门禁(D-03)+ 门禁单元测试</name>
  <files>backend/services/synchronizer.py, tests/test_synchronizer_platform_gate.py</files>
  <read_first>
    - backend/services/synchronizer.py:1-14(顶层导入块 —— 当前无 `import sys`,需新增)、1382-1414(BrowserSynchronizer 类 __init__ 与 start 方法本体,门禁插入点在 start 第一行校验 `master_profile_id = str(...)` 之前)
    - backend/browser_manager.py:315-319(start_synchronizer → self.synchronizer.start(...) 的调用链)
    - backend/main.py:245-250(/api/synchronizer/start 端点的 try/except → HTTPException(400) 包裹,确认无需改动)
    - .planning/phases/01-backend-cross-platform/01-PATTERNS.md(synchronizer.py 段落:start 门禁骨架 + 错误传播路径)
    - .planning/phases/01-backend-cross-platform/01-RESEARCH.md(Pattern 3 + D-03 拦截范围核对:四个窗口排列端点走 window_manager 桩函数、start 走本门禁)
    - tests/test_sync_regressions.py:1-30(同一模块的打桩风格:from backend.services import synchronizer + mock.patch)
  </read_first>
  <behavior>
    - mock 使 synchronizer 模块内 sys.platform 为 "darwin":`BrowserSynchronizer(runtime_resolver, profile_resolver).start("m", ["f"])` 抛 RuntimeError,消息为「窗口同步仅在 Windows 上可用」。
    - 门禁在参数校验之前触发:即使传入空 master_profile_id,macOS 上也先抛「仅在 Windows」而非「请选择主浏览器」。
    - mock sys.platform 为 "win32" 时,start 不因平台门禁抛错(继续走既有校验 —— 传空 master 时抛既有的 ValueError「请选择主浏览器」,证明 win32 路径未被门禁改变)。
  </behavior>
  <action>
在 `backend/services/synchronizer.py` 顶层导入块(第 1-14 行区域)新增 `import sys`。在 `BrowserSynchronizer.start`(第 1393 行)方法体的第一行、现有 `master_profile_id = str(master_profile_id or "").strip()` 之前,插入平台门禁:当 `sys.platform != "win32"` 时 `raise RuntimeError`,消息为指向「窗口同步」功能的中文文案(措辞与既有 API 错误风格及 window_manager 的「窗口排列仅在 Windows 上可用」保持一致)。start 其余方法体、以及类内其他方法与整个 CDP/Marionette 转发逻辑零改动。main.py 不改动(RuntimeError 经既有 try/except 自动转 400)。

新建 `tests/test_synchronizer_platform_gate.py`:纯 unittest.TestCase,`from backend.services import synchronizer`;用 `unittest.mock.patch("backend.services.synchronizer.sys.platform", "darwin")` 断言 `BrowserSynchronizer(lambda pid: None, lambda pid: None).start("m", ["f"])` 抛 RuntimeError;另加 patch 为 "win32" 的用例断言传空 master 时抛的是既有 ValueError(证明门禁不影响 win32 路径)。测试从仓库根目录运行。
  </action>
  <verify>
    <automated>python -m unittest tests.test_synchronizer_platform_gate -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_synchronizer_platform_gate -v` 退出码 0
    - synchronizer.py 顶层导入块含 `import sys`
    - BrowserSynchronizer.start 的第一条可执行语句是非 win32 平台的 RuntimeError 门禁(在 master_profile_id 校验之前)
    - 测试断言:darwin 下 start 抛 RuntimeError;win32 下传空 master 抛 ValueError(而非平台 RuntimeError)
    - 门禁文案指向「窗口同步」功能(非全应用范围),与既有中文错误风格一致
  </acceptance_criteria>
  <done>macOS 上 `/api/synchronizer/start` 经门禁 → RuntimeError → HTTPException 400 + 中文 detail;win32 同步逻辑不变;测试全绿并已提交。</done>
  <reversibility rating="reversible">后续里程碑做 CDP-only 跨平台同步(SYNC-01)时,移除这一处非 win32 门禁即可放开,无迁移成本(D-03)。</reversibility>
</task>

<task type="auto">
  <name>Task 2: 核销 D-04 —— 验证 main.py open_system_url 已跨平台(不改代码)</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py:474-491(open_system_url 端点全文:确认 `if os.name == "nt": os.startfile(...) else: webbrowser.open_new_tab(...)` 分支已存在)
    - .planning/phases/01-backend-cross-platform/01-RESEARCH.md(Pitfall 1:D-04 状态核销,git blame 显示该跨平台分支自提交 8afdce3 已存在)
    - .planning/phases/01-backend-cross-platform/01-PATTERNS.md(main.py 段落:验证性任务而非修改任务)
  </read_first>
  <action>
纯验证任务,不修改任何代码(D-04 已被 RESEARCH 核销)。核对 `backend/main.py` 的 open_system_url(第 474-491 行)确为跨平台实现:Windows 走 `os.startfile`、非 Windows 走 `webbrowser.open_new_tab`,异常经 HTTPException(400) 包裹。在 macOS 上做一次冒烟验证:对该端点逻辑传入一个 http(s) URL,确认非 Windows 分支调用 webbrowser 而不触发 os.startfile(可用 mock webbrowser.open_new_tab 断言被调用,不真的弹浏览器)。若发现 main.py 需要任何字符改动,说明理解有误 —— 应停下核对 RESEARCH Pitfall 1,不得「顺手修复」已跨平台的代码。在 SUMMARY 中记录核销结论。
  </action>
  <verify>
    <automated>python -c "import ast,sys; src=open('backend/main.py',encoding='utf-8').read(); assert 'webbrowser.open_new_tab' in src and 'os.startfile' in src; print('D-04 verified: cross-platform branch present')"</automated>
  </verify>
  <acceptance_criteria>
    - main.py open_system_url 同时含 `os.startfile`(Windows 分支)与 `webbrowser.open_new_tab`(非 Windows 分支)
    - 本 plan 的 git diff 中 backend/main.py 无任何改动(纯验证,零 diff)
    - macOS 冒烟:mock 下对 http(s) URL 走 webbrowser.open_new_tab 分支,不抛异常
    - SUMMARY 记录 D-04 已核销(现状即跨平台,无需改动)
  </acceptance_criteria>
  <done>确认 main.py URL 打开端点已跨平台、macOS 可用;D-04 核销为验证项;main.py 零改动。</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces(Plan 03)

- `backend/services/synchronizer.py`:顶层新增 `import sys`;BrowserSynchronizer.start 顶部新增一处非 win32 平台门禁语句(无新增函数/类符号,仅在既有 start 方法内插入分支)
- `tests/test_synchronizer_platform_gate.py`(新文件):测试类(如 `SynchronizerPlatformGateTests`)
- `backend/main.py`:无改动(D-04 验证项)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API 路由 → 同步器 services 层 | /api/synchronizer/start 是唯一入口,门禁下沉到 BrowserSynchronizer.start 内部 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering/DoS | 同步器启动在 macOS 的平台门禁 | low | mitigate | 门禁下沉到 BrowserSynchronizer.start 内部(而非仅前端隐藏或仅路由层判断),任何调用路径都触达同一 RuntimeError;经 main.py 既有 try/except → 400,不暴露 500/底层 CDP 异常。避免半可用状态(D-03) |
| T-01-04 | Information Disclosure | 门禁错误文案 | low | accept | 文案仅声明「窗口同步仅在 Windows 上可用」,不泄漏内部实现细节;与既有中文错误风格一致 |
</threat_model>

<verification>
- macOS:`python -m unittest tests.test_synchronizer_platform_gate -v` 全绿
- macOS:D-04 验证命令通过,main.py git diff 为空
- 回归:macOS `python -m unittest discover -s tests -v` 无因本 plan 改动新增失败(test_sync_regressions 等既有同步器测试仍应通过 —— 它们不经 start 门禁)
</verification>

<success_criteria>
XPLAT-02(同步部分):macOS 上 `/api/synchronizer/start` 返回 400 +「窗口同步仅在 Windows 上可用」,同步器整体不可用无半可用状态;win32 同步逻辑字节级不变;D-04 核销、main.py 零改动。
</success_criteria>

<output>
Create `.planning/phases/01-backend-cross-platform/01-03-SUMMARY.md` when done
</output>
