---
phase: 01-backend-cross-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - backend/services/window_manager.py
  - backend/runtime_control.py
  - tests/test_window_manager_posix.py
  - tests/test_runtime_control_posix.py
autonomous: true
requirements: [XPLAT-01, XPLAT-02, XPLAT-04]
user_setup: []

must_haves:
  truths:
    - "macOS 上 `pip install --dry-run -r requirements.txt` 跳过 pywin32 与 ruyipage,不产生解析错误(pip 输出含 'Ignoring pywin32' / 'Ignoring ruyipage')"
    - "macOS 上 `python -c \"import backend.main\"` 退出码 0,不再因 window_manager 顶层 win32api 导入而崩溃"
    - "macOS(非 win32)上调用 list_monitors / show_windows / set_uniform_size / arrange_windows 均抛 RuntimeError,detail 为「窗口排列仅在 Windows 上可用」"
    - "macOS 上 `--backend-only` 派生的子进程不向 subprocess.Popen 传入非零 creationflags,改用 start_new_session=True;派生后可被 psutil 检活、可被 stop_backend_only 停止"
    - "Windows 分支(sys.platform == 'win32')下 window_manager 的四个函数体与 runtime_control 的 Popen creationflags 行为字节级不变(仅缩进变化)"
    - { statement: "macOS 上并发调用四个窗口排列桩函数,每次调用相互独立各自抛 RuntimeError,桩函数无共享可变状态、无竞态(XPLAT-02 concurrency edge)", verification: backstop }
  artifacts:
    - requirements.txt
    - backend/services/window_manager.py
    - backend/runtime_control.py
    - tests/test_window_manager_posix.py
    - tests/test_runtime_control_posix.py
  key_links:
    - "browser_manager.py:36 具名导入 window_manager 的四个函数 —— 桩函数必须同名同签名,否则导入即失败"
    - "launch_app.main → run_backend_only → runtime_control.start_backend_only 的 Popen 调用 —— creationflags 平台条件化的唯一落点"
  prohibitions:
    - statement: "禁止改动 window_manager.py 与 runtime_control.py 中 Windows 分支(sys.platform == 'win32')的任何字符,只允许整体缩进进 if 块;不得借迁移之机重构或优化写法"
      status: flagged-unverified
    - statement: "禁止用 preexec_fn 实现 POSIX 后台派生,必须用 subprocess.Popen(start_new_session=True)"
      status: flagged-unverified
    - statement: "禁止在 browser_manager.py 新增任何平台判断,窗口排列的平台分支必须完全收敛在 window_manager.py 内部(D-01:调用方零改动)"
      status: flagged-unverified
    - statement: "禁止改动 backend/services/chrome.py 与 backend/services/firefox.py 的 CREATE_NEW_PROCESS_GROUP / creationflags(其兜底值为 0,POSIX 安全;属 Phase 3 范围,本 phase 越界即拒绝)"
      status: flagged-unverified
    - statement: "禁止拆分 pyinstaller 等构建期依赖到 requirements-build.txt(D-10:两平台都需要,保持单文件)"
      status: flagged-unverified
---

<objective>
让 macOS 用户能装、能导入后端,并能派生纯后端模式 —— 这是本 phase 的端到端纵切(tracer),证明「`sys.platform` 分支使后端跨平台可运行」这一核心架构在最短路径上成立。

三处改动构成一条真实用户路径:`requirements.txt` 加 PEP 508 平台标记(pip 装得上)→ `window_manager.py` 条件导入 + 桩函数(`import backend.main` 不再崩)→ `runtime_control.py` 的 Popen creationflags 平台条件化(`--backend-only` 派生得起来)。

Purpose: 先在 agent 的早期上下文里把「后端在 macOS 上跑得起来」这条脊柱验证到位,避免后续 config/synchronizer 扩展任务在一个已死的架构上叠加。
Output: macOS 上 pip 可安装、`import backend.main` 成功、`--backend-only` 可派生/检活/停止;Windows 行为字节级不变;两个新测试文件锁定行为。

覆盖需求:XPLAT-01(pip 安装)、XPLAT-02(导入 + 窗口排列门禁的 window_manager 部分)、XPLAT-04(纯后端模式派生)。
实现决策:D-01(window_manager 条件导入)、D-09(requirements 平台标记)、D-10(不拆构建依赖)。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-cross-platform/01-CONTEXT.md
@.planning/phases/01-backend-cross-platform/01-RESEARCH.md
@.planning/phases/01-backend-cross-platform/01-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: macOS 可安装 + 可导入纵切(requirements 标记 + window_manager 条件导入)</name>
  <files>requirements.txt, backend/services/window_manager.py, tests/test_window_manager_posix.py</files>
  <read_first>
    - backend/services/window_manager.py(全文 238 行:确认第 1-10 行顶层 import 与第 13-238 行全部函数体,这些是要整体缩进进 win32 分支的对象)
    - backend/browser_manager.py:34-37(确认 `from .services.window_manager import arrange_windows, list_monitors, set_uniform_size, show_windows` 具名导入 —— 桩函数必须同名同签名)
    - requirements.txt(全文 15 行:pywin32 在第 14 行、ruyipage 在第 15 行)
    - .planning/phases/01-backend-cross-platform/01-PATTERNS.md(window_manager 段落:Windows 分支逐字迁移 + 非 Windows 桩函数骨架)
    - tests/test_sync_regressions.py:1-30(打桩风格参考:纯 unittest.TestCase + unittest.mock.patch,无 pytest fixture)
  </read_first>
  <precondition>本机为 macOS(darwin);验证步骤前已执行 `pip install -r requirements.txt`,fastapi/uvicorn/psutil/pydantic/websocket-client 等运行时依赖可用(pywin32、ruyipage 被平台标记跳过,不安装)。</precondition>
  <behavior>
    - 在 macOS(darwin)上 `import backend.services.window_manager` 成功(顶层不再无条件 import win32api)。
    - 在 macOS 上 `import backend.main` 退出码 0(整条 win32 导入链被打通)。
    - 在 macOS 上 list_monitors() 抛 RuntimeError,消息为「窗口排列仅在 Windows 上可用」。
    - 在 macOS 上 show_windows(lookup, ids) / set_uniform_size(lookup, ids) / arrange_windows(lookup, ids) 三者均抛同一 RuntimeError。
    - 四个桩函数的名称与参数签名与 win32 分支完全一致(browser_manager 具名导入不报 ImportError)。
    - 在 win32 平台上,四个函数保持原有实现不被桩替换(用 `@unittest.skipUnless(sys.platform == "win32", ...)` 包裹的一组用例断言可正常调用,不抛「仅在 Windows 上可用」)。
  </behavior>
  <action>
在 `requirements.txt` 给 pywin32(第 14 行)与 ruyipage(第 15 行)各追加 PEP 508 环境标记 `; sys_platform == "win32"`,不改版本号(`pywin32>=308`、`ruyipage>=1.0.0` 维持现值),其余依赖行原样保留(D-09)。不新建 requirements-build.txt,pyinstaller 等构建依赖保持在本文件(D-10)。

改造 `backend/services/window_manager.py`(D-01):顶层新增 `import sys`;把现有第 3-10 行的 `import math` / `import psutil` / `import win32api` / `import win32con` / `import win32gui` / `import win32process`、`ENGINE_WINDOW_CLASSES` 常量、以及 list_monitors / show_windows / set_uniform_size / arrange_windows 和全部私有辅助函数(_pick_monitor / _arrange_grid / _arrange_overlap / _collect_profile_windows / _pick_primary_window / _process_tree_pids)整体缩进进 `if sys.platform == "win32":` 分支,逐字迁移不改一个字符。新增 `else:` 分支:定义常量 `_UNSUPPORTED_MSG` 值为中文错误文案(指向「窗口排列」功能,措辞与既有 API 错误风格一致),并导出四个同名同签名函数(list_monitors、show_windows、set_uniform_size、arrange_windows),函数体只做 `raise RuntimeError(_UNSUPPORTED_MSG)`。`from __future__ import annotations` 保留在文件首行。browser_manager.py 零改动。

新建 `tests/test_window_manager_posix.py`:纯 unittest.TestCase,用 `unittest.mock.patch` + `importlib.reload(window_manager)` 在 mock `sys.platform` 为 "darwin" 后断言四个函数 assertRaises(RuntimeError);另加一组 `@unittest.skipUnless(sys.platform == "win32", ...)` 用例断言 win32 分支函数可正常导入调用。测试从仓库根目录运行。
  </action>
  <verify>
    <automated>python -m unittest tests.test_window_manager_posix -v</automated>
    <automated>python -c "import backend.main; print('import-ok')"</automated>
    <automated>pip install --dry-run -r requirements.txt 2>&1 | grep -Ei "Ignoring pywin32|Ignoring ruyipage"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_window_manager_posix -v` 退出码 0
    - macOS 上 `python -c "import backend.main"` 退出码 0
    - `pip install --dry-run -r requirements.txt` 输出包含 `Ignoring pywin32` 与 `Ignoring ruyipage`(macOS 环境跳过 Windows-only 依赖)
    - requirements.txt 第 14/15 行分别含 `pywin32>=308; sys_platform == "win32"` 与 `ruyipage>=1.0.0; sys_platform == "win32"`
    - window_manager.py 顶层含 `import sys` 且存在 `if sys.platform == "win32":` 与 `else:` 两个分支
    - macOS 上 `python -c "from backend.services.window_manager import list_monitors, show_windows, set_uniform_size, arrange_windows; print('names-ok')"` 退出码 0(四个名字可导入)
  </acceptance_criteria>
  <done>macOS 上 pip 可安装(跳过 pywin32/ruyipage)、`import backend.main` 成功、四个窗口排列函数在 macOS 抛「窗口排列仅在 Windows 上可用」;win32 分支行为不变;测试全绿并已提交。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 纯后端模式跨平台派生(runtime_control creationflags 条件化)</name>
  <files>backend/runtime_control.py, tests/test_runtime_control_posix.py</files>
  <read_first>
    - backend/runtime_control.py:18-19(两个常量定义 CREATE_NEW_PROCESS_GROUP / DETACHED_PROCESS,保留不动)、136-160(start_backend_only 的 subprocess.Popen 调用点,唯一改动落点)
    - backend/runtime_control.py:1-16(顶层已 `import sys`、`import subprocess`,无需新增 import)
    - .planning/phases/01-backend-cross-platform/01-PATTERNS.md(runtime_control 段落:_POPEN_KWARGS 平台分支骨架)
    - .planning/phases/01-backend-cross-platform/01-RESEARCH.md(Pattern 2 + Pitfall 2:DETACHED_PROCESS 兜底值 0x00000008 非零是 macOS 崩溃根因;creationflags != 0 才报错)
    - tests/test_sync_regressions.py:1-30(mock.patch 打桩风格)
  </read_first>
  <precondition>本机为 macOS(darwin);Task 1 已完成使 `import backend.main` / launch_app 在 macOS 可导入(真实派生冒烟需要整条导入链可用)。</precondition>
  <behavior>
    - 在 POSIX(非 win32)上,start_backend_only 调用 subprocess.Popen 时传入 `start_new_session=True`,且不传 creationflags(或不传非零 creationflags)。
    - 在 win32 上,start_backend_only 调用 subprocess.Popen 时传入 `creationflags=DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP`,不传 start_new_session。
    - CREATE_NEW_PROCESS_GROUP 与 DETACHED_PROCESS 两个模块级常量定义保持存在(win32 分支仍引用)。
  </behavior>
  <action>
改造 `backend/runtime_control.py` 的 start_backend_only(仅第 143-152 行的 Popen 调用点,XPLAT-04):在 Popen 调用前构造平台条件化的 kwargs 字典 `_POPEN_KWARGS`;当 `sys.platform == "win32"` 时设 `_POPEN_KWARGS["creationflags"] = DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP`,否则设 `_POPEN_KWARGS["start_new_session"] = True`。把 Popen 调用改为把 `creationflags=...` 那一行删去、末尾展开 `**_POPEN_KWARGS`,其余参数(command、cwd、stdin/stdout/stderr=DEVNULL、close_fds=True、env)原样保留。第 18-19 行两个常量定义保留不动。不改动 chrome.py/firefox.py(Pitfall 2)。

新建 `tests/test_runtime_control_posix.py`:纯 unittest.TestCase,用 `unittest.mock.patch("subprocess.Popen")` 捕获调用参数。POSIX 用例(mock sys.platform 为 "darwin" 或直接依赖本机非 win32)断言调用 kwargs 含 `start_new_session=True`、不含非零 creationflags;win32 用例(mock sys.platform 为 "win32")断言 kwargs 含 `creationflags` 等于 `DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP`。为避免真实起子进程,同时 mock `_wait_for_port` 返回 True、`get_backend_only_status` 返回 running 状态。测试从仓库根目录运行。
  </action>
  <verify>
    <automated>python -m unittest tests.test_runtime_control_posix -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_runtime_control_posix -v` 退出码 0
    - runtime_control.py 含 `_POPEN_KWARGS` 且存在 `start_new_session` 与 `creationflags` 两条平台分支赋值
    - runtime_control.py 第 18-19 行 CREATE_NEW_PROCESS_GROUP / DETACHED_PROCESS 常量定义仍存在
    - 测试断言:POSIX 分支 Popen kwargs 含 `start_new_session=True` 且不含非零 `creationflags`;win32 分支含 `creationflags=DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP`
    - (integration smoke,macOS)`python -c "from backend import runtime_control as r; s=r.start_backend_only(); assert s['running'] and s['pid']; import psutil; assert psutil.Process(s['pid']).is_running(); print(r.stop_backend_only()['running'])"` 输出末行为 `False`(派生 → psutil 检活 → 停止)
  </acceptance_criteria>
  <done>macOS 上 `--backend-only` 可派生、被 psutil 检活、可停止,且不向 POSIX 传非法 creationflags;win32 分支保留原 creationflags;测试全绿并已提交。</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces(Plan 01)

本 plan 新建/新增的符号(供 plan-review-convergence source-grounding 排除新建符号的漂移误报):

- `requirements.txt`:pywin32 / ruyipage 行追加 `; sys_platform == "win32"` 标记(无新符号,仅标记)
- `backend/services/window_manager.py`:模块顶层新增 `import sys`;新增 `else:` 分支下的模块级常量 `_UNSUPPORTED_MSG`;四个桩函数 `list_monitors` / `show_windows` / `set_uniform_size` / `arrange_windows` 的非 Windows 实现(同名重定义)
- `backend/runtime_control.py`:start_backend_only 内新增局部变量 `_POPEN_KWARGS`(平台条件化 Popen kwargs)
- `tests/test_window_manager_posix.py`(新文件):测试类(如 `WindowManagerPosixTests`)
- `tests/test_runtime_control_posix.py`(新文件):测试类(如 `RuntimeControlPosixTests`)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 → services 层 | browser_manager/API 调用窗口排列与派生函数,平台可用性门禁在 services 层内部判定 |
| 父进程 → 派生子进程 | start_backend_only 用 Popen 派生独立后端进程,继承环境与文件描述符 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering/DoS | window_manager 四个函数在 macOS 的平台门禁 | low | mitigate | 门禁下沉到 window_manager.py 内部,任何调用路径都触达同一 RuntimeError,经 main.py 既有 try/except → HTTPException(400) 兜住,不会暴露 500/底层异常 |
| T-01-02 | Elevation of Privilege | POSIX 派生进程的 fd/会话归属 | low | accept | start_new_session=True 仅改变会话归属不提升权限;既有 close_fds=True、stdin/stdout/stderr=DEVNULL 良好实践保持不变 |
| T-01-SC | Tampering | pip 依赖(pywin32/ruyipage) | low | accept | 本 phase 不引入新依赖,仅对既有包追加平台标记,RESEARCH.md Package Legitimacy Audit 已核对两者为长期在用依赖,无 [ASSUMED]/[SUS]/[SLOP] 新包,无需安装前人工放行 checkpoint |
</threat_model>

<verification>
- macOS:`python -m unittest tests.test_window_manager_posix tests.test_runtime_control_posix -v` 全绿
- macOS:`python -c "import backend.main"` 退出码 0
- macOS:`pip install --dry-run -r requirements.txt` 跳过 pywin32/ruyipage
- macOS:`--backend-only` integration smoke 派生/检活/停止成功
- 回归:本机 macOS `python -m unittest discover -s tests -v` 无因本 plan 改动新增失败(此前无法导入的用例现应可导入)
</verification>

<success_criteria>
XPLAT-01(pip 跳过 pywin32/ruyipage)、XPLAT-02(import backend.main 成功 + 四个窗口排列函数在 macOS 抛「窗口排列仅在 Windows 上可用」)、XPLAT-04(--backend-only 派生/检活/停止)在 macOS 上均可复现;Windows 分支代码字节级不变。
</success_criteria>

<flagged_assumptions>
边缘探针未分类项(specless fallback,不可自动兜底,需人工审阅,严禁静默丢弃):
- **XPLAT-01(unclassified):** pip 在「部分依赖已装 / 安装中途网络中断」时的部分安装状态未分类。假设:安装原子性由 pip 自身保证,本 phase 只对 requirements.txt 追加平台标记、不引入自定义安装编排,故不额外处理;若后续验证发现 pip 部分失败态影响 macOS 首装体验,需人工确认是否补充重试/清理逻辑。
- **XPLAT-04(unclassified):** 派生后 psutil 检活在「子进程僵尸态 / 端口未就绪超时」时的边界行为未分类。假设:既有 `_wait_for_port` 超时 + `kill_process_tree` 清理 + `_is_pid_alive` 排除 STATUS_ZOMBIE 的逻辑已覆盖该边界,本 phase 不改这部分;若人工审阅认为需要更强的超时/清理保证,单独立项。
</flagged_assumptions>

<output>
Create `.planning/phases/01-backend-cross-platform/01-01-SUMMARY.md` when done
</output>
