---
phase: 04-frontend-platform-gating
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - frontend/src/App.vue
  - frontend/src/assets/global.css
  - backend/_g.py
autonomous: true
requirements: [UI-01, UI-02, UI-04]

must_haves:
  truths:
    - "在 capabilities.engines.firefox.available === false 的机器上,侧栏内核状态卡片不再出现 Firefox 那一行,Chrome 行照常显示(D-01 / UI-01)。"
    - "在 capabilities.window.sync.available === false 的机器上,侧栏「同步器」导航项呈置灰态并带原因气泡,但点击仍能进入同步器视图 —— 置灰不等于屏蔽入口(D-02 / UI-02 / SC2)。"
    - "macOS 首次运行时,在开源声明首启提示之后弹出一次 Gatekeeper 放行指引;标记已看过后不再弹出;Windows 上从不弹出(D-04 / UI-04 / SC4)。"
    - "放行指引中的 `xattr -dr com.apple.quarantine` 按字面渲染(不被引号美化、不被 HTML 转义),用户可完整复制该命令。"
    - "backend/_g.py 中 frontend/src/App.vue 的记录摘要等于该文件改动后的真实 SHA-256,`python3 -m backend._g --mode build` 与 `npm run build` 均通过。"
    - statement: "capabilities 迟到加载时,侧栏 syncer 项与 SyncManager 视图内控件的禁用态响应式生效,不残留首次渲染的可点击状态。"
      verification: backstop
  artifacts:
    - frontend/src/App.vue
    - frontend/src/assets/global.css
    - backend/_g.py
  key_links:
    - "App.vue 的 onMounted → store.bootstrap() → store.capabilities → 侧栏状态行/导航置灰/首启弹窗三处门控:三者都在同一次 bootstrap 解析后生效。"
    - "App.vue 文件字节 → backend/_g.py 的 SHA-256 记录 → npm run build 的 prebuild/postbuild 钩子与 launch_app 的启动校验:摘要不同步则构建与启动同时硬失败。"
  prohibitions:
    - "backend/_g.py 的开源声明完整性机制不得被削弱、绕过或移除:不得删除 _1 中任何条目、不得放宽 _5/_6/_7、不得修改 frontend/src/lib/openSourceNotice.js,也不得让现有首次使用开源声明弹窗对首次运行用户不再出现或被新弹窗取代;唯一允许的改动是把 App.vue 的记录摘要更新为其真实新摘要。"
    - "macOS 放行文案不得引导用户全局关闭 Gatekeeper 或对宽泛目录递归剥离 quarantine —— 命令必须限定到单个已安装的应用 bundle;也不得把限制表述成「本应用不支持 macOS」。"
---

<objective>
在应用外壳上完成本 phase 的最后三处门控 —— 侧栏 Firefox 内核状态行隐藏(UI-01)、侧栏「同步器」导航项置灰带原因气泡但不屏蔽入口(UI-02 导航层)、macOS 首次运行弹出一次 Gatekeeper 放行指引(UI-04)—— 并在全部 App.vue 改动定稿之后,把该文件的 SHA-256 重新写回 `backend/_g.py`。

Purpose: `frontend/src/App.vue` 被 `backend/_g.py` 的开源声明完整性机制哈希锁定,任何改动不同步摘要就会让 `npm run build`(prebuild/postbuild 钩子)与桌面启动(`launch_app.main` 的运行时校验)同时硬失败。本 phase 把所有 App.vue 改动集中在这一个 plan、并把重算摘要放在最后一个任务,是为了让「重算发生在最后一次 App.vue 编辑之后」这条不变式成立且可复核;这也是本 plan 被排到独立 wave(晚于其余全部实现 plan)的原因。

**规划裁量记录(需在 SUMMARY 中复述):** `GroupManager.vue` 第 36 行的分组内 Firefox 配置计数**不做隐藏**。D-01 枚举的三个隐藏点不含它;它报告的是既有配置的真实构成,隐藏会让分组数量对不上,与 D-01「既有配置不删不藏、不静默丢数据」相冲突。它不是创建 Firefox 配置的入口,不影响 SC1。

**现状勘误:** CONTEXT/D-01 提到的「App.vue 顶部引擎状态徽章/计数」在当前代码里并不存在 —— `header-badges`(第 88-91 行)只有配置总数与运行数两个标签,没有引擎项;Firefox 的引擎状态实际在侧栏状态卡片(第 71-76 行)。本 plan 按实际代码处理侧栏那一行,不要去 header 里找不存在的 Firefox 徽章。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-platform-gating/04-CONTEXT.md
@.planning/phases/04-frontend-platform-gating/04-PATTERNS.md
@.planning/phases/04-frontend-platform-gating/04-RESEARCH.md
@.planning/phases/04-frontend-platform-gating/04-01-SUMMARY.md
@.planning/phases/04-frontend-platform-gating/04-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 侧栏 Firefox 状态行隐藏 + 同步器导航项置灰(入口不屏蔽)</name>
  <files>frontend/src/App.vue, frontend/src/assets/global.css</files>
  <read_first>
    - `frontend/src/App.vue`(第 16-27 行侧栏导航 `v-for`;第 63-77 行内核状态卡片的 Chrome/Firefox 两行;第 150-190 行 import 区与 `useProfileStore()`;第 203-212 行 `navItems`;第 351-356 行 `setActiveNav`;第 428-441 行 `engineStatusText`/`engineTagType`)
    - `frontend/src/assets/global.css` 第 169-200 行(`.nav-item`、`.nav-item:hover`、`.nav-item.active` 三条既有规则 —— 目前没有任何禁用态样式)
    - `frontend/src/lib/capabilitiesGating.js`(04-01 产出的 `isFirefoxEngineAvailable` 与 `getWindowFeatureGate` 语义)
    - `.planning/phases/04-frontend-platform-gating/04-02-SUMMARY.md`(i18n key 清单,本任务用 `platformLimits.windowsOnlyFallback`)
    - `.planning/phases/04-frontend-platform-gating/04-PATTERNS.md` § `frontend/src/App.vue`(导航置灰的模板形状;以及本文件被哈希锁定的警告)
  </read_first>
  <precondition>`backend/_g.py` 记录的 `frontend/src/App.vue` 摘要与该文件当前字节一致(执行前 `python3 -m backend._g --mode build` 退出码为 0);本任务会让它暂时失配,由 Task 3 恢复。</precondition>
  <action>
    在 `<script setup>` 里从 `./lib/capabilitiesGating.js` import `isFirefoxEngineAvailable` 与 `getWindowFeatureGate`。新增一个 `firefoxEngineVisible` computed 调用 `isFirefoxEngineAvailable(store.capabilities)`,以及两个具名函数 `isNavDisabled(key)` 与 `navDisabledReason(key)`:前者在 `key === 'syncer'` 时返回 `getWindowFeatureGate(store.capabilities, 'sync').disabled`、其余 key 一律返回 false;后者在 `key === 'syncer'` 时返回该 gate 的 reason 原值、为空时回退 `t('platformLimits.windowsOnlyFallback')`、其余 key 返回空字符串。不得从浏览器提供的平台字符串或 user-agent 推导平台(D-00),唯一信号源是 store 上的 capabilities;因为走的是 computed 与响应式 store,bootstrap 迟到解析时两处门控都会自动重算,不残留首帧状态。

    UI-01 侧栏状态行:给第 71-76 行那个包着 Firefox 名称与状态 `el-tag` 的整个 `<div class="status-row">` 加 `v-if="firefoxEngineVisible"`。Chrome 那一行(第 65-70 行)不加任何条件。`engineStatusText` 与 `engineTagType` 两个函数体保持原样、不改逻辑 —— 只是它们的 firefox 调用点被条件渲染掉了。

    UI-02 导航置灰:把导航 `v-for` 的 `<div class="nav-item">` 用 `el-tooltip` 包起来(`v-for` 与 `:key` 上移到 tooltip 上),tooltip 的 `:disabled` 绑 `!isNavDisabled(item.key)`(只有被门控的项才显示气泡),`:content` 绑 `navDisabledReason(item.key)`,`placement="right"`。`nav-item` 的 `:class` 追加 `disabled: isNavDisabled(item.key)`。**`@click` 保持 `setActiveNav(item.key)` 不变,不加任何拦截** —— D-02 要求导航层与视图层双层呈现,用户必须能进到同步器视图里看到 04-04 做的横幅与禁用控件;把点击拦掉等于把入口藏了,直接违反 SC2 的「不隐藏」。原因文案取后端 reason 原值直接绑定,不经翻译函数(D-02 锁定「读字段,不在前端另写」),只有 reason 为空时才用本地双语兜底 key。

    在 `frontend/src/assets/global.css` 里,紧跟第 189-200 行 `.nav-item.active` 相关规则之后新增一条 `.nav-item.disabled` 规则:降低不透明度(约 0.45)、鼠标指针改为不可用形态、并用一条更具体的 `.nav-item.disabled:hover` 规则把悬停高亮的背景与文字色还原成非悬停态,避免置灰项看起来仍然可点。不要动既有三条规则。

    本任务不碰 `onMounted`、不碰首启弹窗、不碰 `backend/_g.py`。执行完 `npm run build` 会因为摘要失配而失败,这是预期的,由 Task 3 恢复。
  </action>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs';const s=readFileSync('frontend/src/App.vue','utf8');for(const k of ['isFirefoxEngineAvailable','getWindowFeatureGate','firefoxEngineVisible','isNavDisabled','navDisabledReason','platformLimits.windowsOnlyFallback'])if(!s.includes(k))throw new Error('missing '+k);if(!s.includes('setActiveNav(item.key)'))throw new Error('nav click handler must stay unconditional');if(/@click=\"[^\"]*isNavDisabled[^\"]*\"/.test(s))throw new Error('nav click must not be intercepted by the gate');if(!s.includes('oab:first-use-notice:v2')||!s.includes('first-use-notice-box'))throw new Error('open-source notice markers must remain');const css=readFileSync('frontend/src/assets/global.css','utf8');if(!css.includes('.nav-item.disabled'))throw new Error('missing disabled nav style');console.log('App shell gating OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 上面第二条命令退出码为 0 并打印 `App shell gating OK`。
    - `grep -c 'function isNavDisabled(' frontend/src/App.vue` 输出 1;`grep -c 'function navDisabledReason(' frontend/src/App.vue` 输出 1。
    - `grep -c 'firefoxEngineVisible' frontend/src/App.vue` 至少为 2(computed 声明 + 状态行 `v-if`)。
    - `grep -c '<span>Chrome</span>' frontend/src/App.vue` 输出 1,且该 `status-row` 上无 `v-if`。
    - `grep -c '.nav-item.disabled' frontend/src/assets/global.css` 至少为 2(基础规则 + hover 还原规则)。
    - `grep -rE "navigator\.(platform|userAgent)" frontend/src/App.vue | wc -l` 输出 0。
    - `git diff frontend/src/assets/global.css` 中不含以 `-` 开头的既有 `.nav-item` 规则行(三条既有规则一字未改)。
    - 行为验证(macOS 本机 `npm run dev`):侧栏内核状态卡片只剩 Chrome 一行;「同步器」导航项明显变淡、指针为不可用形态、悬停出现「窗口同步仅在 Windows 上可用」气泡;点击它仍能进入同步器视图并看到 04-04 的横幅。
  </acceptance_criteria>
  <reversibility rating="reversible">条件渲染 + 一条 CSS 规则,回退即删除。</reversibility>
  <done>侧栏只显示 Chrome 内核状态;同步器导航项置灰并给出后端原文原因,但入口仍可进入。</done>
</task>

<task type="auto">
  <name>Task 2: macOS 首次运行的 Gatekeeper 放行指引弹窗</name>
  <files>frontend/src/App.vue, frontend/src/assets/global.css</files>
  <read_first>
    - `frontend/src/App.vue`(第 280-318 行 `onMounted` 的整个 try/catch 体,注意 `await store.bootstrap()` → `await store.getBackendModeStatus()` → `await _0x31ab()` 的既有顺序;第 459-489 行 `_0x31ab()` 的 `ElMessageBox.alert` 调用形状)
    - `frontend/src/lib/macosGatekeeperNotice.js`(04-02 产出:`shouldShowGatekeeperNotice`、`markGatekeeperNoticeSeen`、`buildGatekeeperNoticeHtml`、`GATEKEEPER_NOTICE_KEY`)
    - `frontend/src/lib/openSourceNotice.js`(**只读,严禁修改** —— 被 `backend/_g.py:17` 哈希锁定)
    - `frontend/src/assets/global.css`(搜索 `first-use-notice-box`,照它的弹窗样式写法给新弹窗加同类样式)
    - `.planning/phases/04-frontend-platform-gating/04-02-SUMMARY.md`(`gatekeeper.title` / `gatekeeper.confirm` 两个 key 与 `GATEKEEPER_XATTR_COMMAND` 的逐字内容)
  </read_first>
  <action>
    在 `<script setup>` 里从 `./lib/macosGatekeeperNotice.js` import `shouldShowGatekeeperNotice`、`markGatekeeperNoticeSeen`、`buildGatekeeperNoticeHtml`。

    新增一个 `async function maybeShowGatekeeperNotice()`:先用 `shouldShowGatekeeperNotice(store.capabilities)` 判断,不满足直接 return;满足则 `await ElMessageBox.alert(buildGatekeeperNoticeHtml(t), t('gatekeeper.title'), { confirmButtonText: t('gatekeeper.confirm'), dangerouslyUseHTMLString: true, closeOnClickModal: false, closeOnPressEscape: false, showClose: false, customClass: 'gatekeeper-notice-box' })`,关闭后调 `markGatekeeperNoticeSeen()`。**不得往 HTML 字符串里插入任何来自 API、用户输入或 store 的动态值** —— 内容全部由 `buildGatekeeperNoticeHtml` 从 i18n 与模块常量构建,插入动态值即构成注入通路。

    在 `onMounted` 的既有 try 块里,把 `await maybeShowGatekeeperNotice()` 放在 `await _0x31ab()` **之后**、两个 `setInterval` 之前。顺序不可颠倒:开源声明首启提示必须先出现(它是本项目的反商业滥用机制),放行指引跟在其后,两个模态不重叠。不要给新调用另起 try/catch,沿用 `onMounted` 既有的那一个;`macosGatekeeperNotice.js` 内部已对 localStorage 异常做了降级,不会把启动流程打断。

    **严禁改动的部分(逐条自查):** `_0x5c10` 常量及其值 `oab:first-use-notice:v2`;`_0x31ab()` 函数体与它在 `onMounted` 中的调用;`customClass: 'first-use-notice-box'`;`_0x91f3`/`_0x72ad`/`_0x18c4` 三个 import 及其调用点;`sidebarSubtitle` computed 与 `document.title` 的赋值 watch。这些字符串同时也是 `backend/_g.py` 在构建产物里检查的标记,任何一处消失都会让 `npm run build` 的 postbuild 运行时校验失败,而且会削弱开源声明本身。

    在 `frontend/src/assets/global.css` 里新增 `.gatekeeper-notice-box` 相关样式:参照既有 `first-use-notice-box` 的宽度与排版写法,给弹窗一个合适的最大宽度,并给内部的 `code` 元素加等宽字体、浅底色、可选中、允许换行的样式,保证终端命令逐字可见且能被完整复制(命令中的连字符、斜杠、点号必须按原文呈现,不被样式或转义改变)。不要改动 `first-use-notice-box` 的任何既有规则。
  </action>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs';const s=readFileSync('frontend/src/App.vue','utf8');for(const k of ['shouldShowGatekeeperNotice','markGatekeeperNoticeSeen','buildGatekeeperNoticeHtml','maybeShowGatekeeperNotice','gatekeeper-notice-box','oab:first-use-notice:v2','first-use-notice-box','_0x31ab'])if(!s.includes(k))throw new Error('missing or removed: '+k);const i=s.indexOf('await _0x31ab()'),j=s.indexOf('await maybeShowGatekeeperNotice()');if(!(i >= 0 && j > i))throw new Error('gatekeeper notice must be awaited after the open-source notice');const css=readFileSync('frontend/src/assets/global.css','utf8');if(!css.includes('.gatekeeper-notice-box'))throw new Error('missing modal style');console.log('gatekeeper mount OK')"</automated>
    <automated>node --input-type=module -e "import {GATEKEEPER_XATTR_COMMAND,buildGatekeeperNoticeHtml} from './frontend/src/lib/macosGatekeeperNotice.js';const html=buildGatekeeperNoticeHtml(k => k);if(!html.includes(GATEKEEPER_XATTR_COMMAND))throw new Error('command missing from notice html');if(!/xattr -dr com\.apple\.quarantine \S+\.app$/.test(GATEKEEPER_XATTR_COMMAND))throw new Error('command must target exactly one app bundle');if(/sudo|spctl/.test(GATEKEEPER_XATTR_COMMAND))throw new Error('command must not escalate or disable gatekeeper globally');console.log('command literal OK:',GATEKEEPER_XATTR_COMMAND)"</automated>
  </verify>
  <acceptance_criteria>
    - 上面第二、第三条命令退出码均为 0,分别打印 `gatekeeper mount OK` 与 `command literal OK: xattr -dr com.apple.quarantine /Applications/Open-Anti-Browser.app`。
    - `grep -c 'async function maybeShowGatekeeperNotice(' frontend/src/App.vue` 输出 1。
    - `grep -c 'oab:first-use-notice:v2' frontend/src/App.vue` 输出 1;`grep -c 'first-use-notice-box' frontend/src/App.vue` 输出 1;`grep -c '_0x31ab' frontend/src/App.vue` 输出 2(声明 + `onMounted` 调用)。
    - `git status --porcelain frontend/src/lib/openSourceNotice.js` 无输出。
    - `sed -n '/^onMounted/,/^})/p' frontend/src/App.vue | grep -c 'try {'` 输出 1(未新增独立 try/catch)。
    - 行为验证(macOS 本机):删掉浏览器 localStorage 里的 `oab:macos-gatekeeper-notice:v1` 后刷新,先弹开源声明提示、关闭后再弹放行指引;指引里 `xattr -dr com.apple.quarantine /Applications/Open-Anti-Browser.app` 逐字显示、可整段选中复制,引号与连字符未被美化;点确认后刷新不再弹出。zh-CN 与 en-US 各验一遍。
  </acceptance_criteria>
  <reversibility rating="reversible">新增一个函数、一行 `onMounted` 调用与一条 CSS 规则;localStorage key 独立,回退无残留。</reversibility>
  <done>macOS 首次运行会在开源声明之后弹出一次放行指引,标记后不再弹出;Windows 与 capabilities 未解析时都不弹;开源声明相关的全部标记字符串完好。</done>
</task>

<task type="auto">
  <name>Task 3: 重算 App.vue 摘要写回完整性校验表(本 phase 最后一次 App.vue 编辑之后)</name>
  <files>backend/_g.py</files>
  <read_first>
    - `backend/_g.py`(全文 89 行 —— 第 12-19 行 `_0()` 解码函数与 `_1` 摘要表;第 21-28 行 `_2` 构建产物标记表;第 31-50 行 `_3`/`_4`/`_5`;第 53-77 行 `_6`/`_7`;第 80-89 行 CLI 入口)
    - `CLAUDE.md` §「资源完整性校验(重要)」
    - `.planning/phases/04-frontend-platform-gating/04-PATTERNS.md` § `_g.py` 哈希锁定 relock 程序
  </read_first>
  <precondition>Task 1 与 Task 2 对 `frontend/src/App.vue` 的全部编辑已定稿并提交;本任务之后若再改动 App.vue,必须重跑本任务。</precondition>
  <action>
    先确认 App.vue 不会再被改动,再执行本任务 —— 摘要必须对应最终字节。

    用 `python3 -c "import hashlib,pathlib;print(hashlib.sha256(pathlib.Path('frontend/src/App.vue').read_bytes()).hexdigest())"` 算出新摘要(这与 `_g.py` 的 `_3()` 用的是同一种算法与读法:整文件字节的 SHA-256 十六进制)。

    在 `backend/_g.py` 的 `_1` 字典里,把 base64 键 `ZnJvbnRlbmQvc3JjL0FwcC52dWU=` 对应的那个摘要值(改动前是 `cfa6427a0c0f17357a41888128ed7d391c2efde62214304d9654027250886104`)替换成新算出的值。**只改这一个字符串字面量**。

    以下改动一律禁止,它们会削弱本项目的反商业滥用机制:不得删除或新增 `_1` 中的条目;不得改动第一个条目(`openSourceNotice.js`,摘要 `2a766eeea1648831555dd1f5d00896ac507f6441a0b0e86cf9326617f14e4eff`);不得改动任何 base64 键;不得改动 `_2` 标记表;不得修改 `_3`/`_4`/`_5`/`_6`/`_7` 的任何逻辑(包括不得加白名单、不得把抛错改成告警、不得给某个文件加跳过分支);不得改动 `frontend/package.json` 里的 prebuild/postbuild 钩子。

    改完后依次跑构建期校验、字典自洽校验与完整前端构建,三者全绿才算完成。完整构建之所以必须跑,是因为 `--mode runtime` 还会检查构建产物里是否含开源声明标记(`_6()`),这条路径只有真正 build 出 `frontend/dist` 才覆盖得到。
  </action>
  <verify>
    <automated>python3 -m backend._g --mode build</automated>
    <automated>python3 -c "
import hashlib
from backend import _g
assert len(_g._1) == 2, 'integrity table must still hold exactly 2 entries'
for rel, expected in _g._1.items():
    got = hashlib.sha256((_g.PROJECT_ROOT / rel).read_bytes()).hexdigest()
    assert got == expected, f'digest mismatch for {rel}: file={got} table={expected}'
assert _g._1['frontend/src/lib/openSourceNotice.js'] == '2a766eeea1648831555dd1f5d00896ac507f6441a0b0e86cf9326617f14e4eff', 'openSourceNotice.js entry must be untouched'
assert len(_g._2) == 6, 'build-artifact marker table must still hold 6 markers'
print('integrity table OK:', sorted(_g._1))
"</automated>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m backend._g --mode build` 退出码为 0。
    - 第二条 Python 校验退出码为 0 并打印 `integrity table OK: ['frontend/src/App.vue', 'frontend/src/lib/openSourceNotice.js']`。
    - `cd frontend && npm run build` 退出码为 0(prebuild 的 `--mode build` 与 postbuild 的 `--mode runtime` 都通过,后者证明构建产物里开源声明标记完好)。
    - `git diff -U0 backend/_g.py | grep -cE '^[+-][^+-]'` 输出 2(恰好一行删除、一行新增)。
    - `git diff backend/_g.py | grep -c '2a766eeea1648831555dd1f5d00896ac507f6441a0b0e86cf9326617f14e4eff'` 输出 0(openSourceNotice.js 条目未出现在 diff 中)。
    - `git status --porcelain frontend/src/lib/openSourceNotice.js` 无输出;`git status --porcelain frontend/package.json` 无输出。
    - `python3 -c "from backend import _g; import inspect; print(len(inspect.getsource(_g._5).splitlines()), len(inspect.getsource(_g._7).splitlines()))"` 输出 `10 5`(校验逻辑行数未变,未被放宽)。
  </acceptance_criteria>
  <reversibility rating="reversible">单个字符串字面量替换;若 App.vue 后续再改,重跑同一程序即可。</reversibility>
  <done>`_g.py` 记录的 App.vue 摘要等于其最终真实摘要;构建期与运行时两种校验模式都通过;完整性机制的条目数、标记表与校验逻辑一字未改。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 源码字节 ↔ `backend/_g.py` 完整性校验表 | 反商业滥用机制:摘要表守护开源声明不被移除;它同时也是构建与启动的硬门禁 |
| 开发者自撰 i18n 文案 → `dangerouslyUseHTMLString` 首启弹窗 | 原始 HTML 注入进 DOM |
| 浏览器 localStorage → 应用启动流程 | 首启门控在 `onMounted` 内同步读写 |
| 前端导航置灰 → 后端窗口同步 API | 置灰只是 UX;后端在非 Windows 上独立拒绝 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | `backend/_g.py` 完整性机制 | high | mitigate | 唯一允许的改动是 App.vue 摘要值。验收项以「diff 恰好 2 行」「openSourceNotice.js 条目不出现在 diff」「`_1` 仍为 2 条」「`_2` 仍为 6 条」「`_5`/`_7` 源码行数不变」五重断言锁死,防止用删条目或放宽校验来「修好」构建。 |
| T-04-12 | Repudiation | 开源声明首启提示被新弹窗挤掉 | high | mitigate | 新弹窗在既有提示之后 await,不共用 key、不共用 customClass;验收项断言 `_0x31ab` 出现 2 次、两个标记字符串仍在、且 await 顺序正确。`npm run build` 的 postbuild 运行时校验会检查构建产物里的标记,构成第二道保险。 |
| T-04-02 | Tampering | `maybeShowGatekeeperNotice` → ElMessageBox HTML | high | mitigate | 内容全部来自 04-02 已单测锁定的 `buildGatekeeperNoticeHtml(t)`;调用点禁止拼接任何动态值。 |
| T-04-06 | Elevation of Privilege(用户被诱导降低本机防护) | 放行指引中的终端命令 | high | mitigate | 验收命令以正则断言命令形如「限定到单个 `.app` bundle」,且不含提权或全局关闭系统防护的成分。 |
| T-04-05 | Denial of Service(本机) | 首启门控读写 localStorage | medium | mitigate | 04-02 已在模块内做异常降级并单测;本 plan 不新增裸 localStorage 访问,复用该模块。 |
| T-04-11 | Denial of Service(用户对功能的可及性) | 同步器导航入口 | medium | mitigate | 置灰不拦点击;验收项以源码断言 `@click` 未被门控条件包裹,保证 D-02 视图层可达。 |
| T-04-SC | Tampering | 依赖供应链 | low | accept | 本 plan 不执行任何 npm/pip/cargo 安装(`npm run build` 只走既有 `node_modules`),无新增第三方包,包合法性门禁不适用。 |
</threat_model>

<verification>
1. `node --test frontend/src/lib/*.test.js` 全绿。
2. `python3 -m backend._g --mode build` 退出码 0;`cd frontend && npm run build` 退出码 0(prebuild + postbuild 双模式校验都过)。
3. `_1` 仍为 2 条、`_2` 仍为 6 条、`_5`/`_7` 逻辑未被放宽;`openSourceNotice.js` 与 `frontend/package.json` 零改动。
4. `grep -rE "navigator\.(platform|userAgent)" frontend/src/ | wc -l` 输出 0 —— 全前端范围内 D-00 成立。
5. macOS 本机跑 `python launch_app.py` 能正常启动(运行时完整性校验通过)。
6. `git diff --name-only` 仅含 `frontend/src/App.vue`、`frontend/src/assets/global.css`、`backend/_g.py`。
</verification>

<success_criteria>
- macOS 侧栏只显示 Chrome 内核状态;同步器导航项置灰带后端原文原因气泡,点击仍可进入视图。
- macOS 首次运行在开源声明之后弹一次放行指引,含逐字可复制的 `xattr -dr com.apple.quarantine` 命令(限定单个应用 bundle),标记后不再弹出;Windows 上不弹。
- `backend/_g.py` 的 App.vue 摘要与文件真实摘要一致,构建与启动两条校验路径都通过。
- 开源声明机制的条目、标记与校验逻辑一字未改;`openSourceNotice.js` 零改动。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

本 phase(04)新建的全部符号。本 plan 产出的项以 **[本 plan]** 标注。

**新文件**
- `frontend/src/lib/capabilitiesGating.js`、`frontend/src/lib/capabilitiesGating.test.js`(04-01)
- `frontend/src/lib/i18n-parity.test.js`、`frontend/src/lib/macosGatekeeperNotice.js`、`frontend/src/lib/macosGatekeeperNotice.test.js`(04-02)

**新导出函数 / 常量**
- `isFirefoxEngineAvailable(capabilities)`、`visibleEngineOptions(baseOptions, capabilities, currentEngine)`、`isEngineSelectorLocked(capabilities, currentEngine)`、`getWindowFeatureGate(capabilities, feature)`(04-01)
- `GATEKEEPER_NOTICE_KEY`、`GATEKEEPER_XATTR_COMMAND`、`hasSeenGatekeeperNotice()`、`markGatekeeperNoticeSeen()`、`shouldShowGatekeeperNotice(capabilities)`、`buildGatekeeperNoticeHtml(t)`(04-02)

**新组件内标识符**
- `stores/profile.js`: `capabilities` ref(04-01)
- `ProfileDialog.vue`: `engineOptions` computed、`engineSelectorLocked` computed(04-01)
- `ProfileList.vue`: `firefoxEngineVisible` computed、`isProfileStartBlocked(row)`(04-03)
- `SyncManager.vue`: `syncGate` computed、`arrangeGate` computed、首次引入 `useI18n`(04-04)
- `AppSettings.vue`: `platformLimitsVisible` computed、`openGatekeeperGuide()`(04-04)
- `App.vue`: `firefoxEngineVisible` computed **[本 plan]**、`isNavDisabled(key)` **[本 plan]**、`navDisabledReason(key)` **[本 plan]**、`maybeShowGatekeeperNotice()` **[本 plan]**

**新 localStorage key**
- `oab:macos-gatekeeper-notice:v1`(04-02 定义,本 plan 首次实际写入)

**新 CSS class**
- `.nav-item.disabled` **[本 plan]**(`frontend/src/assets/global.css`)
- `.gatekeeper-notice-box` **[本 plan]**(ElMessageBox customClass;04-04 的设置页复看入口也引用同一个)

**新 i18n key(zh-CN 与 en-US 各一份)**
- `platformLimits.engineLockedHint`(04-01)
- `platformLimits.{title,desc,windowsOnlyTag,windowsOnlyFallback,firefoxItem,windowItem,gatekeeperItem,startBlockedHint,reopenGatekeeper,kernelWindowsOnly}`、`syncer.{platformBannerTitle,platformBannerHint}`、`gatekeeper.{title,intro,stepsTitle,step1,step2,step3,step4,commandTitle,commandHint,settingsHint,confirm}`(04-02)
  本 plan 消费 `platformLimits.windowsOnlyFallback`、`gatekeeper.title`、`gatekeeper.confirm`,不新增 key。

**修改的既有文件(非新符号)**
- `CLAUDE.md`(04-01)
- `backend/_g.py` —— 仅 `_1` 中 `frontend/src/App.vue` 一项的摘要值 **[本 plan]**
</artifacts_produced>

<output>
Create `.planning/phases/04-frontend-platform-gating/04-05-SUMMARY.md` when done.
在 SUMMARY 中必须记录:(1) 写回 `_g.py` 的新摘要值与它对应的 App.vue commit;(2) 「GroupManager 分组内 Firefox 计数不隐藏」这条规划裁量及其理由;(3) 首启弹窗与开源声明弹窗的最终先后顺序。
</output>
