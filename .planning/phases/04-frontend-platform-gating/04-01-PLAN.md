---
phase: 04-frontend-platform-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - frontend/src/stores/profile.js
  - frontend/src/lib/capabilitiesGating.js
  - frontend/src/lib/capabilitiesGating.test.js
  - frontend/src/components/ProfileDialog.vue
  - frontend/src/i18n/zh-CN.js
  - frontend/src/i18n/en-US.js
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "在 capabilities.engines.firefox.available === false 的机器上,新建配置对话框的引擎选择器只提供 Chrome 一个选项(D-01 / UI-01 / SC1)。"
    - "store 暴露响应式 capabilities,并由 bootstrap() 从 /api/bootstrap 的 data.capabilities 赋值;所有门控判断只读这一个来源(D-00)。"
    - "在 macOS 编辑一个既有 engine=firefox 配置时,引擎选择器不得出现空选中态,也不得静默把 engine 改写成 chrome —— 该控件在此情况下整体禁用并保留原值。"
    - "当 capabilities 为 undefined/null(bootstrap 未完成或后端未返回 capabilities)时,引擎选项渲染不抛错,且缺省按「可用」处理 —— 不得连 Chrome 一起隐藏(Windows 零回归)。"
    - "引擎选项过滤掉 Firefox 后,剩余选项保持与过滤前一致的相对顺序(仅移除,不重排)。"
    - "Firefox 门控辅助函数为纯函数:以相同 capabilities 连续调用返回相同结果,且不原地修改传入的选项数组。"
    - "窗口能力门控辅助函数为纯函数:重复调用结果一致,不修改入参 capabilities 对象。"
  artifacts:
    - frontend/src/lib/capabilitiesGating.js
    - frontend/src/lib/capabilitiesGating.test.js
  key_links:
    - "GET /api/bootstrap 的 data.capabilities → stores/profile.js 的 capabilities ref → capabilitiesGating.js → ProfileDialog.vue 的 engineOptions computed(本 phase 所有门控的唯一链路)。"
    - "CLAUDE.md 记录的前端测试命令 → 本 phase 每个 plan 的 <automated> verify(命令写错则整条反馈回路失效)。"
  prohibitions:
    - "既有 firefox-engine 配置的用户数据不得被门控逻辑隐藏、删除或静默改写(包括保存编辑时把 engine 改成 chrome);门控只影响呈现与可操作性,profiles.json 中的数据必须保持完整、可见,且仅能由用户主动删除。"
---

<objective>
把 Phase 3 已经上线的 capabilities 契约接进前端,建立本 phase 唯一的平台门控事实源(D-00),并用它端到端证明 UI-01 的核心路径:macOS 上创建配置时引擎选择器不再出现 Firefox。

Purpose: 这是 tracer plan。它一次性打通 后端契约 → Pinia store → 纯函数门控模块 → Vue 组件条件渲染 这条本 phase 所有其它 plan 都要复用的链路;链路一旦被证明,后续 plan 只是横向铺开到别的界面。同时修正 CLAUDE.md 里已经失效的前端测试命令,让整个 phase 的自动化反馈回路真正可用。

Output: `frontend/src/lib/capabilitiesGating.js`(本 phase 的门控 API)、配套单测、store 上的 `capabilities` ref、ProfileDialog 引擎选择器的门控与编辑态锁定。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-platform-gating/04-CONTEXT.md
@.planning/phases/04-frontend-platform-gating/04-PATTERNS.md
@.planning/phases/04-frontend-platform-gating/04-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 端到端「macOS 上新建配置看不到 Firefox」—— 只打通这一条路径</name>
  <files>CLAUDE.md, frontend/src/lib/capabilitiesGating.js, frontend/src/lib/capabilitiesGating.test.js, frontend/src/stores/profile.js, frontend/src/components/ProfileDialog.vue</files>
  <read_first>
    - `CLAUDE.md`(「常用命令」段落里前端测试那一行,以及测试环境约定)
    - `frontend/src/stores/profile.js`(第 105-119 行 ref 声明区、第 180-194 行 `bootstrap()`、第 458-523 行 `return { ... }` 导出对象)
    - `frontend/src/components/ProfileDialog.vue`(第 26-31 行引擎 `el-segmented` 模板、第 500-540 行 `defineProps`/`useProfileStore()`/`engineOptions`/`proxyTypeOptions`)
    - `frontend/src/lib/proxyBypass.test.js`(全文 —— 本仓库唯一的前端测试写法:`node:test` + `node:assert/strict`,纯 `.js` 模块直接 ESM import,不做 SFC 编译)
    - `backend/browser_manager.py` 第 69-76 行 `bootstrap()` 与第 627-641 行 `get_platform_capabilities()`(契约源,只读不改)
    - `.planning/phases/04-frontend-platform-gating/04-PATTERNS.md`(§ `frontend/src/stores/profile.js`、§ `frontend/src/components/ProfileDialog.vue`)
  </read_first>
  <precondition>仓库根目录存在已安装 requirements.txt 的 Python 环境(本机为 `.venv/bin/python`),且 `.venv/bin/python -c "import psutil"` 成功;Node 版本 >= 20(本机 v22.18.0)。</precondition>
  <action>
    分四步,按顺序做完再验证。

    第一步,修正 CLAUDE.md 里的前端测试命令。当前文档写的目录形式命令在 Node 22 上会直接抛 MODULE_NOT_FOUND(Node 把目录当模块入口去 require,已在 v22.18.0 实测复现),导致本 phase 每个 plan 的自动化验证都是假绿。把「常用命令」段落里前端测试那一行改成经过实测可用的展开形式 `node --test frontend/src/lib/*.test.js`(由 shell 展开通配符,Node 20/22 都可用),并在同一行的说明里保留「无需额外依赖、从仓库根目录运行」的既有语义。不要改动该文件的其它任何内容。

    第二步,新建 `frontend/src/lib/capabilitiesGating.js`。这是 D-00 要求的「单一事实源」模块:本 phase 所有平台门控判断都必须经过它,任何组件都不得自行推导平台。本任务先落两个具名导出函数(用 `export function xxx(` 声明形式,与 `frontend/src/lib/openSourceNotice.js`、`proxyBypass.test.js` 的项目约定一致,不用箭头函数常量):
    - `isFirefoxEngineAvailable(capabilities)` —— 返回 `capabilities?.engines?.firefox?.available !== false`。注意判定用「不等于 false」而不是「等于 true」:bootstrap 解析前 capabilities 是 `{}`,字段是 undefined,此时必须按「可用」处理,保证 Windows 零回归、也不会在首帧闪掉 Chrome 选项。传入 undefined / null / `{}` 都必须返回 true 且不抛错。
    - `visibleEngineOptions(baseOptions, capabilities, currentEngine)` —— 入参 `baseOptions` 是形如 `{ label, value }` 的选项数组。Firefox 可用时原样返回一份**新数组**(不返回入参本身、也不原地修改入参)。Firefox 不可用时,用过滤(不是重新构造)剔除 `value === 'firefox'` 的项,从而保证剩余项的相对顺序与过滤前完全一致;但当 `currentEngine === 'firefox'` 时保留该项,这是编辑既有 Windows 迁移配置时避免选择器出现空选中态、避免静默把 engine 改写成 chrome 的唯一手段(Task 2 会给这种情况加禁用)。函数必须是纯函数:同样入参重复调用结果一致,且调用后 `baseOptions` 与 `capabilities` 都未被修改。

    第三步,新建 `frontend/src/lib/capabilitiesGating.test.js`,照 `proxyBypass.test.js` 的写法(`import test from 'node:test'` / `import assert from 'node:assert/strict'`,直接 ESM import 被测模块,描述性整句 test 标题)。至少覆盖:Firefox 可用时两个选项都在;`available` 为 false 时 Firefox 被剔除且 Chrome 仍在;capabilities 传 undefined / null / `{}` 时两个选项都在且不抛错;过滤后剩余选项的相对顺序与入参一致(用一个三项以上、Chrome 不在首位的数组断言);连续两次调用返回深相等结果;调用后入参数组与 capabilities 对象未被修改;`currentEngine === 'firefox'` 且 Firefox 不可用时该项被保留。

    第四步,接线并消费。在 `stores/profile.js` 里照 `engines`/`downloads` 的既有写法新增一个 `const capabilities = ref({})`,在 `bootstrap()` 中用与 `downloads.value = data.downloads || {}` 完全相同的防御式默认写法赋值 `data.capabilities`,并把 `capabilities` 加进 `useProfileStore` 底部的返回对象(与 `engines`、`downloads` 并列)。然后在 `ProfileDialog.vue` 里把当前那个静态的 `engineOptions` 常量数组改成 `computed`,内部调用 `visibleEngineOptions`,基础选项数组保持 Chrome 在前、Firefox 在后的原顺序,capabilities 传 `store.capabilities`,currentEngine 传 `form.engine`。该文件已有 `useProfileStore()` 实例和 `computed` 导入,直接复用,不要新增 import。整个过程不得从浏览器提供的平台字符串或 user-agent 推导平台(D-00 明令禁止),唯一信号源是 store 上的 capabilities。
  </action>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
    <automated>.venv/bin/python -c "import json;from backend.browser_manager import BrowserManager as B;print(json.dumps(B().get_platform_capabilities()))" > /tmp/oab-caps.json && node --input-type=module -e "import {readFileSync} from 'node:fs';import {visibleEngineOptions} from './frontend/src/lib/capabilitiesGating.js';const caps=JSON.parse(readFileSync('/tmp/oab-caps.json','utf8'));const o=visibleEngineOptions([{label:'Chrome',value:'chrome'},{label:'Firefox',value:'firefox'}],caps,'chrome');if(o.some(x => x.value==='firefox'))throw new Error('firefox still offered on '+caps.platform);console.log('E2E OK on',caps.platform,JSON.stringify(o))"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test frontend/src/lib/*.test.js` 退出码为 0,且输出包含 `capabilitiesGating` 相关用例。
    - 第二条 E2E 命令退出码为 0 并打印 `E2E OK on darwin`,证明「真实后端 capabilities → 门控模块 → 引擎选项列表」这条链路上 Firefox 已消失。
    - `grep -c 'export function isFirefoxEngineAvailable(' frontend/src/lib/capabilitiesGating.js` 输出 1;`grep -c 'export function visibleEngineOptions(' frontend/src/lib/capabilitiesGating.js` 输出 1。
    - `grep -c 'capabilities' frontend/src/stores/profile.js` 至少为 3(ref 声明、bootstrap 赋值、导出对象各一处)。
    - `grep -c 'visibleEngineOptions' frontend/src/components/ProfileDialog.vue` 至少为 2(import 与 computed 内调用)。
    - `grep -rE "navigator\.(platform|userAgent)" frontend/src/ | wc -l` 输出 0。
    - `grep -c 'frontend/src/lib/\*\.test\.js' CLAUDE.md` 至少为 1。
    - `git diff --name-only` 不包含 `frontend/src/App.vue`、`frontend/src/lib/openSourceNotice.js`、`backend/_g.py`。
  </acceptance_criteria>
  <reversibility rating="costly">D-00 把 capabilities 定为跨 Phase 3→4 的硬接口;若日后改成前端硬编码平台判断,UI-01/UI-02 的每个消费点都要逐处改回,且 SYNC-01(跨平台 CDP 同步)放开时代价更大。</reversibility>
  <done>真实 macOS 后端返回的 capabilities 经 store 与门控模块流到 ProfileDialog 的引擎选择器后,Firefox 选项消失;单测与 E2E 命令都绿;CLAUDE.md 的前端测试命令可实际执行。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 编辑既有 Firefox 配置时锁定引擎选择器,杜绝空选中态与静默改写</name>
  <files>frontend/src/lib/capabilitiesGating.js, frontend/src/lib/capabilitiesGating.test.js, frontend/src/components/ProfileDialog.vue, frontend/src/i18n/zh-CN.js, frontend/src/i18n/en-US.js</files>
  <read_first>
    - `frontend/src/lib/capabilitiesGating.js`(Task 1 刚建立的模块,保持同一导出风格)
    - `frontend/src/components/ProfileDialog.vue`(第 26-31 行引擎 `el-form-item` 与 `el-segmented`;第 32-35 行 `form-tip` 的既有用法,新提示要沿用这个 class)
    - `frontend/src/i18n/zh-CN.js`(第 308-337 行 `settings` 段,照它的嵌套与 camelCase 叶子键风格)
    - `frontend/src/i18n/en-US.js`(同一位置,结构与 zh-CN 一一对应)
    - `.planning/phases/04-frontend-platform-gating/04-RESEARCH.md` § Common Pitfalls 的 Pitfall 3(为什么 `el-segmented` 绑定值不在 options 里是数据损坏风险)
  </read_first>
  <action>
    先补测试再补实现。

    在 `capabilitiesGating.js` 新增具名导出 `isEngineSelectorLocked(capabilities, currentEngine)`:当 `currentEngine === 'firefox'` 且 `isFirefoxEngineAvailable(capabilities)` 为假时返回 true,其余一律返回 false;capabilities 传 undefined / null 时返回 false(未知平台不锁)。纯函数,不修改入参。

    在 `capabilitiesGating.test.js` 补齐对应用例(见 behavior 块),先让它们表达期望行为再让实现通过。

    在 `ProfileDialog.vue` 里新增一个 `engineSelectorLocked` computed 调用该函数(capabilities 传 `store.capabilities`,currentEngine 传 `form.engine`),把它绑到引擎 `el-segmented` 的 `:disabled` 上。因为 Task 1 的 `visibleEngineOptions` 在这种情况下会保留 firefox 选项,该控件会正常高亮显示当前值 Firefox,只是不可切换 —— 这正是「整体禁用并保留原值」的落地方式,既不出现空选中态,也不会在保存时把 engine 静默改成 chrome。不要在保存逻辑里对 `form.engine` 做任何回填或纠正。

    在引擎 `el-form-item` 内、`el-segmented` 之后追加一个 `v-if="engineSelectorLocked"` 的 `<div class="form-tip">`,文案取新增 i18n key `platformLimits.engineLockedHint`。为此在 `frontend/src/i18n/zh-CN.js` 与 `frontend/src/i18n/en-US.js` 顶层各新增一个 `platformLimits` 段(放在 `settings` 段之后、`apiAccess` 段之前,两个文件位置一致),本任务只放 `engineLockedHint` 这一个叶子键,后续 plan 会继续扩充同一个段。zh-CN 内容说明「该配置使用 Firefox 内核,当前系统不支持切换或启动,仅可在 Windows 上使用」;en-US 是同义英文,不得留空、不得直接复制中文。措辞要点名具体不可用的功能,不能读成「本应用不支持 macOS」。

    与 SC1 的关系必须留意:SC1 要求「创建/编辑配置界面完全不出现 Firefox 选项」。新建路径(`mode === 'create'`,engine 默认 chrome)下 Firefox 选项确实完全不出现,SC1 成立。编辑一个从 Windows 迁移来的 firefox 配置时,控件展示的是该配置**已有的值**且不可选择、不可切换,不是一个可选项 —— 这是 D-01「既有配置不删不藏、不静默丢数据」与 SC1 的调和结果,须在 SUMMARY 里明确记录。
  </action>
  <behavior>
    - `isEngineSelectorLocked({engines:{firefox:{available:false}}}, 'firefox')` 为 true。
    - `isEngineSelectorLocked({engines:{firefox:{available:false}}}, 'chrome')` 为 false。
    - `isEngineSelectorLocked({engines:{firefox:{available:true}}}, 'firefox')` 为 false。
    - `isEngineSelectorLocked(undefined, 'firefox')` 与 `isEngineSelectorLocked(null, 'firefox')` 均为 false 且不抛错。
    - Firefox 不可用且 `currentEngine === 'firefox'` 时,`visibleEngineOptions` 的返回结果中存在 `value === 'firefox'` 的项(保证绑定值可被选中渲染)。
    - 连续两次以相同入参调用 `isEngineSelectorLocked` 结果相同,且调用后 capabilities 对象深相等于调用前。
  </behavior>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
    <automated>node --input-type=module -e "import zh from './frontend/src/i18n/zh-CN.js';import en from './frontend/src/i18n/en-US.js';for(const l of [['zh',zh],['en',en]]){const v=l[1].platformLimits?.engineLockedHint;if(typeof v!=='string'||!v.trim())throw new Error('missing platformLimits.engineLockedHint in '+l[0]);}console.log('i18n key OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test frontend/src/lib/*.test.js` 退出码为 0,新增的 `isEngineSelectorLocked` 用例全部通过。
    - `grep -c 'export function isEngineSelectorLocked(' frontend/src/lib/capabilitiesGating.js` 输出 1。
    - `grep -c 'engineSelectorLocked' frontend/src/components/ProfileDialog.vue` 至少为 3(import/computed/`:disabled` 绑定/`v-if`)。
    - `grep -c 'platformLimits' frontend/src/i18n/zh-CN.js` 与 `grep -c 'platformLimits' frontend/src/i18n/en-US.js` 均至少为 1。
    - 上面第二条 i18n 校验命令退出码为 0 并打印 `i18n key OK`。
    - `git diff frontend/src/components/ProfileDialog.vue` 中不包含对 `form.engine` 的赋值或回填改动。
  </acceptance_criteria>
  <reversibility rating="reversible">纯前端条件渲染 + 一个纯函数;改回只需删除 `:disabled` 绑定。</reversibility>
  <done>在 macOS 上打开一个 engine=firefox 的既有配置进行编辑时,引擎选择器高亮显示 Firefox、整体禁用、下方给出中英双语说明;保存后 `profiles.json` 里的 engine 仍是 firefox。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 补齐窗口能力门控函数(UI-02 在 wave 3/4 的共享契约)</name>
  <files>frontend/src/lib/capabilitiesGating.js, frontend/src/lib/capabilitiesGating.test.js</files>
  <read_first>
    - `frontend/src/lib/capabilitiesGating.js`(保持同一导出风格与 optional-chaining 读法)
    - `frontend/src/lib/capabilitiesGating.test.js`(沿用已有用例风格)
    - `backend/browser_manager.py` 第 627-641 行(`window.arrange` 与 `window.sync` 是两个互相独立的布尔 + reason,不可混用)
  </read_first>
  <action>
    在 `capabilitiesGating.js` 新增具名导出 `getWindowFeatureGate(capabilities, feature)`,`feature` 取 `'sync'` 或 `'arrange'`。返回一个新对象 `{ disabled, reason }`:
    - `disabled` = `capabilities?.window?.[feature]?.available === false`(注意用「等于 false」而不是「不等于 true」:capabilities 尚未解析时字段是 undefined,此时必须返回 disabled 为 false,保证 Windows 与首帧零回归,不会误置灰)。
    - `reason` = `capabilities?.window?.[feature]?.reason` 为非空字符串时取其原值,否则取空字符串。这里只负责透传后端原文,**不做任何翻译、模板化或改写**(D-02 锁定「读字段,不在前端另写」);当后端没给或给了空串时返回空字符串,由消费方回退到本地 i18n 文案,以免出现空 tooltip / 空横幅。
    - 纯函数:同入参重复调用返回深相等结果,且不修改入参 capabilities;`sync` 与 `arrange` 必须各读各的字段,不得互相回退。

    之所以在本 plan(wave 1)就把这个函数落下来,是因为 wave 3 的 SyncManager 与 wave 4 的 App.vue 导航都要消费它;由单一 plan 拥有这个模块可以避免两个并行 plan 同时创建同一个符号。本任务不接任何消费方。

    补测试覆盖 behavior 块列出的全部情形。
  </action>
  <behavior>
    - `getWindowFeatureGate({window:{sync:{available:false,reason:'窗口同步仅在 Windows 上可用'}}}, 'sync')` 返回 `{disabled:true, reason:'窗口同步仅在 Windows 上可用'}`。
    - `getWindowFeatureGate({window:{arrange:{available:true,reason:null}}}, 'arrange')` 返回 `{disabled:false, reason:''}`。
    - `getWindowFeatureGate(undefined,'sync')`、`getWindowFeatureGate(null,'sync')`、`getWindowFeatureGate({},'sync')` 均返回 `{disabled:false, reason:''}` 且不抛错。
    - `available` 为 false 但 `reason` 缺失或为空串时,返回 `{disabled:true, reason:''}` —— 置灰仍然生效。
    - `sync` 不可用而 `arrange` 可用时,两次调用分别返回 disabled true 与 false,互不影响。
    - 连续两次以相同入参调用返回深相等结果,且调用后 capabilities 对象深相等于调用前。
  </behavior>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test frontend/src/lib/*.test.js` 退出码为 0。
    - `grep -c 'export function getWindowFeatureGate(' frontend/src/lib/capabilitiesGating.js` 输出 1。
    - `capabilitiesGating.test.js` 中出现 `getWindowFeatureGate` 的用例数不少于 6(`grep -c getWindowFeatureGate frontend/src/lib/capabilitiesGating.test.js` >= 6)。
    - 本任务不修改任何 `.vue` 文件:`git diff --name-only` 中除 `frontend/src/lib/capabilitiesGating.js` 与 `frontend/src/lib/capabilitiesGating.test.js` 外无其它前端源文件。
  </acceptance_criteria>
  <reversibility rating="reversible">新增一个未接线的纯函数,删除成本为零。</reversibility>
  <done>`getWindowFeatureGate` 已实现并被单测覆盖,wave 3/4 的 SyncManager 与 App.vue 可以直接 import,无需再自行读 capabilities 字段。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地后端 `/api/bootstrap` → 前端 store | 后端返回的 capabilities 被前端当作门控事实源;该 API 按既有架构无鉴权,仅监听 127.0.0.1 |
| 前端门控判断 → 后端实际能力执行 | 前端隐藏/禁用只是 UX 表现,真正的平台限制由后端独立强制(XPLAT-02,Phase 1 已测) |
| 用户已有 profiles.json 数据 → 编辑对话框回写 | 编辑既有 firefox 配置时表单可能改写持久化数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | 前端 capabilities 门控 | low | accept | 门控是 UX affordance,不是访问控制边界。后端 `window_manager.py` / `synchronizer.py` 在非 Windows 上独立拒绝调用(XPLAT-02 已测),前端即便被绕过也无法越权。 |
| T-04-02 | Tampering | ProfileDialog 引擎字段 → profiles.json | high | mitigate | `el-segmented` 绑定值不在 options 内会导致保存时把 engine 静默写成 chrome、损坏用户从 Windows 迁移来的配置。Task 2 通过「保留当前值选项 + 整体禁用 + 保存逻辑不回填 form.engine」消除该路径,并由单测锁定 `visibleEngineOptions` 在该情形保留 firefox 项。 |
| T-04-03 | Information Disclosure | capabilities 载荷 | low | accept | 载荷只含平台名与布尔能力位,无凭据、无路径、无用户数据;经本地回环传输。 |
| T-04-SC | Tampering | 依赖供应链 | low | accept | 本 plan 不执行任何 npm/pip/cargo 安装(RESEARCH.md § Package Legitimacy Audit 判定为 Not applicable),无新增第三方包,包合法性门禁不适用。 |
</threat_model>

<verification>
1. `node --test frontend/src/lib/*.test.js` 全绿(含 `proxyBypass.test.js` 既有 2 个用例)。
2. 真实后端 capabilities 经门控模块产出的引擎选项在本机(darwin)不含 firefox。
3. `grep -rE "navigator\.(platform|userAgent)" frontend/src/` 无输出 —— D-00 的「不硬编码平台」得到源码级保证。
4. `python3 -m backend._g --mode build` 退出码 0 —— 本 plan 未触碰被哈希锁定的文件。
5. `git status` 中不出现 `frontend/src/App.vue`、`frontend/src/lib/openSourceNotice.js`、`backend/_g.py`。
</verification>

<success_criteria>
- store 上存在响应式 `capabilities`,由 `bootstrap()` 从 `data.capabilities` 赋值并对外导出。
- `frontend/src/lib/capabilitiesGating.js` 导出 `isFirefoxEngineAvailable`、`visibleEngineOptions`、`isEngineSelectorLocked`、`getWindowFeatureGate` 四个纯函数,全部有单测覆盖。
- macOS 上新建配置对话框的引擎选择器只有 Chrome;编辑既有 firefox 配置时选择器显示 Firefox 且整体禁用,保存不改写 engine。
- CLAUDE.md 记录的前端测试命令在本机可实际执行并返回 0。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

本 phase(04)新建的全部符号,供下游 review 的 source-grounding 排除新符号误报。本 plan 产出的项以 **[本 plan]** 标注。

**新文件**
- `frontend/src/lib/capabilitiesGating.js` **[本 plan]**
- `frontend/src/lib/capabilitiesGating.test.js` **[本 plan]**
- `frontend/src/lib/macosGatekeeperNotice.js`(04-02)
- `frontend/src/lib/macosGatekeeperNotice.test.js`(04-02)
- `frontend/src/lib/i18n-parity.test.js`(04-02)

**新导出函数 / 常量**
- `isFirefoxEngineAvailable(capabilities)` **[本 plan]**
- `visibleEngineOptions(baseOptions, capabilities, currentEngine)` **[本 plan]**
- `isEngineSelectorLocked(capabilities, currentEngine)` **[本 plan]**
- `getWindowFeatureGate(capabilities, feature)` **[本 plan]**
- `GATEKEEPER_NOTICE_KEY`、`GATEKEEPER_XATTR_COMMAND`、`hasSeenGatekeeperNotice()`、`markGatekeeperNoticeSeen()`、`shouldShowGatekeeperNotice(capabilities)`、`buildGatekeeperNoticeHtml(t)`(04-02)

**新组件内标识符**
- `stores/profile.js`: `capabilities` ref **[本 plan]**
- `ProfileDialog.vue`: `engineOptions` 由常量改为 computed、`engineSelectorLocked` computed **[本 plan]**
- `ProfileList.vue`: `firefoxEngineVisible` computed、`isProfileStartBlocked(row)`(04-03)
- `SyncManager.vue`: `syncGate` computed、`arrangeGate` computed、首次引入 `useI18n`(04-04)
- `AppSettings.vue`: `platformLimitsVisible` computed、`openGatekeeperGuide()`(04-04)
- `App.vue`: `firefoxEngineVisible` computed、`isNavDisabled(key)`、`navDisabledReason(key)`、`maybeShowGatekeeperNotice()`(04-05)

**新 localStorage key**
- `oab:macos-gatekeeper-notice:v1`(04-02;与既有 `oab:first-use-notice:v2` 并列、互不干扰)

**新 CSS class**
- `.nav-item.disabled`(04-05,`frontend/src/assets/global.css`)
- `.gatekeeper-notice-box`(04-05,ElMessageBox customClass)

**新 i18n key(zh-CN 与 en-US 各一份)**
- `platformLimits.engineLockedHint` **[本 plan]**
- `platformLimits.{title,desc,windowsOnlyTag,windowsOnlyFallback,firefoxItem,windowItem,gatekeeperItem,startBlockedHint,reopenGatekeeper,kernelWindowsOnly}`(04-02)
- `syncer.{platformBannerTitle,platformBannerHint}`(04-02)
- `gatekeeper.{title,intro,stepsTitle,step1,step2,step3,step4,commandTitle,commandHint,settingsHint,confirm}`(04-02)

**修改的既有文件(非新符号)**
- `CLAUDE.md`(前端测试命令修正)**[本 plan]**、`backend/_g.py`(仅 App.vue 摘要值,04-05)
</artifacts_produced>

<output>
Create `.planning/phases/04-frontend-platform-gating/04-01-SUMMARY.md` when done.
在 SUMMARY 中必须记录:(1) `capabilitiesGating.js` 的完整导出 API 与语义(下游 plan 直接照此消费);(2) 编辑态保留 firefox 选项与 SC1 的调和理由;(3) CLAUDE.md 测试命令的修正原因(Node 22 目录形式失效)。
</output>
