---
phase: 04-frontend-platform-gating
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/src/components/SyncManager.vue
  - frontend/src/components/AppSettings.vue
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "在 capabilities.window.sync.available === false 的机器上进入同步器视图,顶部出现一条不可关闭的说明横幅,视图内所有同步与排列操作按钮均为禁用态,但视图本身仍可进入、内容仍可浏览(D-02「置灰+提示,不隐藏」/ UI-02 / SC2)。"
    - "横幅与控件提示中的原因文案取自后端 capabilities 的 reason 字段原文,前端不重写、不翻译(D-02)。"
    - "排列类操作读 capabilities.window.arrange,同步类操作读 capabilities.window.sync,两个布尔互相独立、不得互相回退。"
    - "设置页存在一张「平台限制说明」卡片,列出当前系统不可用的具体功能,并提供重新查看 Gatekeeper 放行指引的入口(D-03 / UI-03 / SC3)。"
    - "当 capabilities.window.sync.reason / arrange.reason 缺失或为空字符串时,置灰仍然生效,提示文案回退到本地 i18n 文案,不出现空 tooltip 或空横幅。"
    - "后端 reason 字符串按原文直接渲染,不得作为 i18n key 传入 t() —— 否则会产生缺 key 回显。源码断言:reason 绑定处无 t(...) 包装。"
    - statement: "卡片文案中的中文标点与内联代码片段按原文渲染,不被 HTML 转义或引号美化。"
      verification: backstop
  artifacts:
    - frontend/src/components/SyncManager.vue
    - frontend/src/components/AppSettings.vue
  key_links:
    - "store.capabilities → capabilitiesGating.getWindowFeatureGate('sync'|'arrange') → SyncManager 横幅与全部动作按钮的禁用态:同步器视图内所有门控共用这两个 gate,不得逐个按钮自行读 capabilities 字段。"
    - "AppSettings 平台限制卡片 → macosGatekeeperNotice.buildGatekeeperNoticeHtml:设置页的「重新查看」与 04-05 的首启弹窗共用同一份文案构建函数,保证两处内容永不分叉。"
  prohibitions:
    - "平台限制文案不得把限制表述成「本应用不支持 macOS」—— 只能点名具体不可用的功能;不得引导用户全局关闭 Gatekeeper 或对宽泛目录递归剥离 quarantine。"
    - "设置页既有的 Firefox 内核卡片不得被隐藏或移除 —— 只允许追加一个「仅 Windows 可用」标注;隐藏它会越出 D-01 明确枚举的三个隐藏点,属未经决策的范围扩张。"
---

<objective>
落地 UI-02 的视图层与 UI-03 的说明承载:同步器视图在 macOS 上以「顶部横幅 + 全部动作按钮禁用」的形式呈现平台限制(视图本身不隐藏、可正常进入),设置页新增一张「平台限制说明」卡片作为限制说明的永久家,并从这里可以随时重新查看 Gatekeeper 放行指引。

Purpose: D-02 要求置灰双层呈现的「视图层」,ROADMAP SC2 明确要求「置灰+提示,不隐藏」;D-03 把限制说明定在设置页卡片上,并要求 UI-02 的横幅与 UI-04 的首启弹窗都能指回这里。本 plan 一次做完这两个视图,因为它们共享同一批 i18n key 与同一个「重新查看指引」入口。

Output: `SyncManager.vue` 的平台横幅与全量按钮门控;`AppSettings.vue` 的平台限制卡片、Firefox 内核卡片标注、重新查看指引按钮。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-platform-gating/04-CONTEXT.md
@.planning/phases/04-frontend-platform-gating/04-PATTERNS.md
@.planning/phases/04-frontend-platform-gating/04-RESEARCH.md
@.planning/phases/04-frontend-platform-gating/04-01-SUMMARY.md
@.planning/phases/04-frontend-platform-gating/04-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 同步器视图的平台横幅与全量动作按钮门控</name>
  <files>frontend/src/components/SyncManager.vue</files>
  <read_first>
    - `frontend/src/components/SyncManager.vue`(第 1-30 行 hero 区与三个同步按钮;第 135-160 行列表头的显示窗口/统一大小;第 265-310 行 console 主操作区与 compact 面板;第 320-390 行窗口排列/文本管理/标签页管理面板;第 407-420 行 `<script setup>` 的 import 区与 `useProfileStore()`)
    - `frontend/src/lib/capabilitiesGating.js`(04-01 产出的 `getWindowFeatureGate(capabilities, feature)`:返回 `{ disabled, reason }`,capabilities 未解析时 disabled 为 false)
    - `.planning/phases/04-frontend-platform-gating/04-02-SUMMARY.md`(i18n key 清单,本任务用 `syncer.platformBannerTitle`、`syncer.platformBannerHint`、`platformLimits.windowsOnlyFallback`)
    - `backend/browser_manager.py` 第 627-641 行(`window.arrange` 与 `window.sync` 是两个独立布尔 + 独立 reason)
    - `.planning/phases/04-frontend-platform-gating/04-RESEARCH.md` § Common Pitfalls 的 Pitfall 2(本文件目前零 i18n、全硬编码中文;只给新增包装文案上 i18n 是预期结果,不要顺手全量迁移)
  </read_first>
  <action>
    在 `<script setup>` 里新增 `import { useI18n } from 'vue-i18n'` 与 `const { t } = useI18n()`(本文件此前没有 i18n,这是首次引入),并从 `../lib/capabilitiesGating.js` import `getWindowFeatureGate`。新增两个 computed:`syncGate` 调用 `getWindowFeatureGate(store.capabilities, 'sync')`,`arrangeGate` 调用 `getWindowFeatureGate(store.capabilities, 'arrange')`。两者是本文件所有平台门控的唯一来源,任何按钮都不得自行读 capabilities 字段,也不得从浏览器提供的平台字符串或 user-agent 推导平台(D-00)。因为是 computed,bootstrap 迟到解析时禁用态会自动生效,不残留首帧的可点击状态。

    横幅:在 `sync-page` 根元素内、`sync-hero` 之前插入一个 `el-alert`,`v-if="syncGate.disabled || arrangeGate.disabled"`,`type="warning"`,`:closable="false"`,`show-icon`。`:title` 取 `t('syncer.platformBannerTitle')`。默认插槽内放两行:第一行是原因原文 —— 直接绑 `syncGate.reason` 与 `arrangeGate.reason`(两条都非空时各显示一行,去重后展示),取值为空时改用 `t('platformLimits.windowsOnlyFallback')` 兜底,保证任何情况下横幅都不为空;第二行是 `t('syncer.platformBannerHint')`,提示可到设置页的平台限制说明查看详情。**原因字符串必须原样绑定输出,不得传进翻译函数**(后端给的是完整中文句子,当成 key 会渲染出缺 key 回显);兜底文案与提示文案则必须走 i18n 双语。

    按钮门控:把 gate 或进每个动作按钮既有的 `:disabled` 表达式(用 `||` 追加,保留原有条件不动),按动作性质分派 gate,不得混用:
    - 用 `syncGate.disabled` 的:hero 区与 console 主操作区的启动同步 / 重启同步 / 停止同步(共 6 处,第 12/16/20/272/273/274 行);文本管理面板的全部执行按钮(清空内容、相同内容、输入随机数字、输入、执行输入,第 331/332/343/350/373 行);标签页管理面板的全部动作按钮(统一标签页、关闭其他、关闭当前、关闭空白页,第 382-385 行)与 compact 面板里的关闭空白页(第 283 行);统一打开网址区域的同步打开与同步主标签页当前网址按钮。这些动作全部经由同步器向跟随窗口重放,属同步能力。
    - 用 `arrangeGate.disabled` 的:一键排列(第 282、323 行)、显示窗口(第 145、280、303 行与行内第 223 行)、统一大小(第 149、281、304 行)。这三类操作走的是 win32 窗口管理接口,属排列能力。
    - 不加门控的:分组筛选下拉、设为主控、同步设置弹层里的开关与输入框、`activePanel` 分段器 —— 这些是本地状态,禁用它们只会让用户连内容都看不了,违背 D-02「不隐藏」的意图。

    为被禁用的按钮补充悬浮说明:把 hero 区的三个主按钮与「一键排列」这四个最显眼的入口各包一层 `el-tooltip`(照本文件第 216-226 行既有写法),`:disabled` 绑对应 gate 的 `!disabled`,`content` 取对应 gate 的 reason 原值、为空时回退到 `t('platformLimits.windowsOnlyFallback')`。同样地,reason 原值直接绑定,不经翻译函数。其余按钮由横幅统一解释,不必逐个包 tooltip。

    本文件其余上千行的硬编码中文一律不动 —— 只有本任务新增的包装文案走 i18n。产生一个「旧硬编码 + 新 i18n」的混合文件是预期结果,不是需要顺手修掉的回归。
  </action>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs';const s=readFileSync('frontend/src/components/SyncManager.vue','utf8');for(const k of ['useI18n','getWindowFeatureGate','syncGate','arrangeGate','syncer.platformBannerTitle','syncer.platformBannerHint','platformLimits.windowsOnlyFallback','el-alert'])if(!s.includes(k))throw new Error('missing '+k);const bad=[...s.matchAll(/t\(\s*[A-Za-z_$][\w$.]*Gate\.reason/g)];if(bad.length)throw new Error('backend reason string must not be passed through the translation function');const sd=(s.match(/syncGate\.disabled/g)||[]).length,ad=(s.match(/arrangeGate\.disabled/g)||[]).length;if(!(sd >= 15))throw new Error('sync-gated controls too few: '+sd);if(!(ad >= 7))throw new Error('arrange-gated controls too few: '+ad);console.log('SyncManager gating OK sync='+sd+' arrange='+ad)"</automated>
  </verify>
  <acceptance_criteria>
    - 上面第二条命令退出码为 0 并打印 `SyncManager gating OK sync=<n> arrange=<m>`,其中 n >= 15、m >= 7。
    - `grep -c 'const syncGate = computed(' frontend/src/components/SyncManager.vue` 输出 1;`grep -c 'const arrangeGate = computed(' frontend/src/components/SyncManager.vue` 输出 1。
    - `grep -c "getWindowFeatureGate(store.capabilities, 'sync')" frontend/src/components/SyncManager.vue` 输出 1;`grep -c "getWindowFeatureGate(store.capabilities, 'arrange')" frontend/src/components/SyncManager.vue` 输出 1(两个 gate 各自独立取值,不互相回退)。
    - `grep -rE "navigator\.(platform|userAgent)" frontend/src/components/SyncManager.vue | wc -l` 输出 0。
    - `git diff frontend/src/components/SyncManager.vue` 中新增的条件渲染属性只出现在新增的 `el-alert` 上;既有的任何按钮、面板、表单项都没有被新加条件渲染 —— 视图内容一律不隐藏,只禁用。
    - `git diff frontend/src/components/SyncManager.vue` 中不包含任何把既有硬编码中文改成翻译调用的行(范围守住)。
    - 行为验证(macOS 本机):点击侧栏同步器仍可正常进入视图;顶部出现橙色不可关闭横幅,内含后端原文「窗口同步仅在 Windows 上可用」;启动同步、一键排列、显示窗口、关闭空白页等按钮均为灰色不可点;分组筛选下拉与同步设置弹层仍可正常操作。
    - `node --test frontend/src/lib/*.test.js` 退出码为 0。
  </acceptance_criteria>
  <reversibility rating="reversible">全部是 `:disabled` 表达式追加与一处 `el-alert`,回退即删除 gate computed 与追加条件。</reversibility>
  <done>macOS 上同步器视图可进入、内容可读,横幅给出后端原文的限制原因,所有同步与排列动作按钮禁用;排列与同步两个 gate 各读各的字段。</done>
</task>

<task type="auto">
  <name>Task 2: 设置页新增平台限制说明卡片与放行指引重看入口</name>
  <files>frontend/src/components/AppSettings.vue</files>
  <read_first>
    - `frontend/src/components/AppSettings.vue`(全文 111 行 —— 第 23-41 行 Chrome/Firefox 内核卡片的 `page-panel` + `panel-title-row` + `h3` + `panel-desc` 结构就是新卡片要复用的模板;第 45-53 行 `<script setup>` 已有 `useI18n` 与 `useProfileStore`)
    - `frontend/src/lib/macosGatekeeperNotice.js`(04-02 产出的 `buildGatekeeperNoticeHtml(t)` 与 `GATEKEEPER_XATTR_COMMAND`)
    - `frontend/src/App.vue` 第 480-487 行(`ElMessageBox.alert` 带 `dangerouslyUseHTMLString` 的既有调用形状)
    - `.planning/phases/04-frontend-platform-gating/04-02-SUMMARY.md`(i18n key 清单,本任务用 `platformLimits.{title,desc,firefoxItem,windowItem,gatekeeperItem,reopenGatekeeper,kernelWindowsOnly}` 与 `gatekeeper.{title,confirm}`)
    - `.planning/phases/04-frontend-platform-gating/04-RESEARCH.md` § Open Questions 第 1 条(既有 Firefox 内核卡片只标注、不隐藏的理由)
  </read_first>
  <action>
    在 `<script setup>` 里 import `buildGatekeeperNoticeHtml` 与 `GATEKEEPER_XATTR_COMMAND`(来自 `../lib/macosGatekeeperNotice.js`)以及 `ElMessageBox`(element-plus,本文件已 import 了 `ElMessage`,在同一条语句里追加)。新增一个 `platformLimitsVisible` computed,取 `store.capabilities?.platform` 存在且不等于 `'win32'`。按 D-03 的默认取向,这张卡片是 macOS-only:Windows 上没有需要说明的限制,显示一张空洞的「本平台无限制」卡片只会增加噪音;capabilities 尚未解析时也不显示,避免闪现。不得从浏览器提供的平台字符串或 user-agent 推导平台(D-00)。

    新增一个 `openGatekeeperGuide()` 函数,调用 `ElMessageBox.alert(buildGatekeeperNoticeHtml(t), t('gatekeeper.title'), { confirmButtonText: t('gatekeeper.confirm'), dangerouslyUseHTMLString: true, customClass: 'gatekeeper-notice-box' })`。它与 04-05 的首启弹窗共用同一个内容构建函数,保证两处文案永不分叉。这里不设置 `closeOnClickModal: false` 之类的强制项 —— 这是用户主动点开的复看入口,允许随手关掉。**不得往 HTML 字符串里插入任何来自 API、用户输入或 store 的动态值**,内容全部来自 i18n 与模块常量。

    在模板的 `settings-stack` 内、Chrome 内核卡片(第 23 行那个 `page-panel`)之前插入新的平台限制卡片,`v-if="platformLimitsVisible"`,沿用完全相同的 `page-panel` → `panel-title-row` → `h3` + `panel-desc` 结构:标题取 `t('platformLimits.title')`,描述取 `t('platformLimits.desc')`。卡片主体是一个三项无序列表,依次渲染 `t('platformLimits.firefoxItem')`、`t('platformLimits.windowItem')`、`t('platformLimits.gatekeeperItem')`。在 gatekeeper 那一项下面追加一个 `<code>` 元素,内容用双大括号插值渲染 `GATEKEEPER_XATTR_COMMAND` —— 必须走 Vue 的文本插值,不要在这张卡片里使用任何原始 HTML 渲染指令,这样命令才会逐字呈现且可整段复制。卡片底部放一个 `el-button`(`plain`、`size="small"`),文案取 `t('platformLimits.reopenGatekeeper')`,点击调 `openGatekeeperGuide`,复用现有的 `action-row` class 排版。

    既有 Firefox 内核卡片(第 33-41 行)**保持原样、不隐藏、不禁用** —— D-01 只枚举了创建对话框、列表筛选、侧栏状态三个隐藏点,把这张内核管理卡片也藏掉属于未经决策的范围扩张;Phase 1 D-08 也刻意保留了 firefox 的跨平台元数据可读性。只在它的 `panel-title-row` 里、既有引擎状态 `el-tag` 旁边追加一个 `el-tag`(`type="info"`、`size="small"`),文案取 `t('platformLimits.kernelWindowsOnly')`,并加 `v-if="platformLimitsVisible"`,让用户一眼看出这块内容在当前系统上不适用。Chrome 内核卡片一字不动。

    文案红线:卡片描述必须点名具体不可用的功能(Firefox 内核、窗口排列、窗口同步),不得写成会让用户误以为整个应用不支持 macOS 的表述。
  </action>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs';const s=readFileSync('frontend/src/components/AppSettings.vue','utf8');for(const k of ['buildGatekeeperNoticeHtml','GATEKEEPER_XATTR_COMMAND','platformLimitsVisible','openGatekeeperGuide','platformLimits.title','platformLimits.desc','platformLimits.firefoxItem','platformLimits.windowItem','platformLimits.gatekeeperItem','platformLimits.reopenGatekeeper','platformLimits.kernelWindowsOnly','gatekeeper-notice-box'])if(!s.includes(k))throw new Error('missing '+k);if(!s.includes(\"t('settings.firefoxTitle')\"))throw new Error('existing firefox kernel card must stay');if(!s.includes(\"t('settings.chromeTitle')\"))throw new Error('existing chrome kernel card must stay');if(/v-html/.test(s))throw new Error('the settings card must render the command as escaped text, not raw markup');console.log('AppSettings card OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 上面第二条命令退出码为 0 并打印 `AppSettings card OK`。
    - `grep -c 'page-panel' frontend/src/components/AppSettings.vue` 相对改动前恰好增加 1(新增一张卡片,既有三张全部保留)。
    - `grep -c "t('settings.firefoxTitle')" frontend/src/components/AppSettings.vue` 输出 1,且该卡片所在的 `page-panel` 根元素上没有 `v-if`(既有内核卡片不被隐藏)。
    - `grep -c 'function openGatekeeperGuide(' frontend/src/components/AppSettings.vue` 输出 1。
    - `grep -rE "navigator\.(platform|userAgent)" frontend/src/components/AppSettings.vue | wc -l` 输出 0。
    - 行为验证(macOS 本机,zh-CN 与 en-US 各看一遍):设置页顶部出现「平台限制说明」卡片,三条说明分别点名 Firefox 内核、窗口排列/同步、首次运行放行;卡片里的终端命令逐字显示为 `xattr -dr com.apple.quarantine /Applications/Open-Anti-Browser.app`,中文标点与命令中的连字符、斜杠均按原文呈现,未被转义成实体、未被美化成弯引号;点「重新查看放行指引」弹出与首启同款的指引弹窗;Firefox 内核卡片仍在,旁边多一个「仅 Windows」标注。
    - `node --test frontend/src/lib/*.test.js` 退出码为 0。
  </acceptance_criteria>
  <reversibility rating="reversible">新增一张卡片与一个标注,回退即删除新增块。</reversibility>
  <done>设置页在 macOS 上有一张列出三条具体限制的平台说明卡片,可随时重看 Gatekeeper 指引;既有三张卡片一张未少,Firefox 内核卡片多一个「仅 Windows」标注。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后端 `capabilities.window.*.reason` 字符串 → 同步器横幅/tooltip DOM | 后端字符串被原样渲染进界面 |
| 开发者自撰 i18n 文案 → `dangerouslyUseHTMLString` 复看弹窗 | 设置页的复看入口与首启弹窗共用同一条原始 HTML 注入路径 |
| 前端控件禁用 → 后端窗口排列/同步 API | 禁用只是 UX;后端在非 Windows 上独立拒绝(XPLAT-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | `openGatekeeperGuide` → ElMessageBox HTML | high | mitigate | 内容全部来自 `buildGatekeeperNoticeHtml(t)`(04-02 已单测锁定其只含开发者自撰文案与模块常量);本任务禁止在调用点拼接任何动态值,验收项断言设置页卡片本身不使用原始 HTML 渲染指令。 |
| T-04-10 | Tampering | 后端 reason 字符串 → 横幅/tooltip | low | accept | reason 是 `browser_manager.py` 里的两个硬编码中文常量,非用户输入、非外部数据;经 Vue 文本插值渲染(自动转义),无注入面。 |
| T-04-01 | Elevation of Privilege | 同步/排列按钮禁用 | low | accept | 门控是 UX affordance;后端 `window_manager.py` / `synchronizer.py` 在非 Windows 上独立拒绝(Phase 1 已测)。 |
| T-04-11 | Denial of Service(用户对信息的可及性) | 同步器视图与设置页既有卡片 | medium | mitigate | D-02 与 SC2 要求「不隐藏」;本 plan 只加禁用与横幅,新增 `v-if` 仅横幅一处,既有内核卡片一律保留。验收项以 `v-if` 计数增量与 `page-panel` 计数增量断言。 |
| T-04-SC | Tampering | 依赖供应链 | low | accept | 本 plan 不执行任何 npm/pip/cargo 安装,无新增第三方包,包合法性门禁不适用。 |
</threat_model>

<verification>
1. `node --test frontend/src/lib/*.test.js` 全绿。
2. 两个组件里都不出现浏览器平台字符串推导:`grep -rE "navigator\.(platform|userAgent)" frontend/src/components/SyncManager.vue frontend/src/components/AppSettings.vue | wc -l` 输出 0。
3. 同步器视图可进入、内容可读、动作按钮禁用、横幅含后端原文。
4. 设置页卡片在 zh-CN 与 en-US 下都渲染完整非空内容,终端命令逐字可复制。
5. `python3 -m backend._g --mode build` 退出码 0 —— 本 plan 未触碰被哈希锁定的文件。
6. `git diff --name-only` 仅含 `frontend/src/components/SyncManager.vue` 与 `frontend/src/components/AppSettings.vue`。
</verification>

<success_criteria>
- macOS 上同步器视图:可进入、顶部横幅、同步类与排列类按钮分别按各自 gate 禁用、本地状态控件不受影响。
- 后端 reason 原文出现在界面上且未经翻译函数包装;reason 为空时有本地双语兜底文案,不出现空横幅/空气泡。
- 设置页存在「平台限制说明」卡片(macOS-only),含三条具体限制、逐字终端命令、重新查看指引按钮。
- 既有 Chrome / Firefox 内核卡片与设置项一张未少,Firefox 卡片多一个「仅 Windows」标注。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

本 phase(04)新建的全部符号。本 plan 产出的项以 **[本 plan]** 标注。

**新文件**
- `frontend/src/lib/capabilitiesGating.js`、`frontend/src/lib/capabilitiesGating.test.js`(04-01)
- `frontend/src/lib/i18n-parity.test.js`、`frontend/src/lib/macosGatekeeperNotice.js`、`frontend/src/lib/macosGatekeeperNotice.test.js`(04-02)

**新导出函数 / 常量**
- `isFirefoxEngineAvailable(capabilities)`、`visibleEngineOptions(baseOptions, capabilities, currentEngine)`、`isEngineSelectorLocked(capabilities, currentEngine)`、`getWindowFeatureGate(capabilities, feature)`(04-01)
- `GATEKEEPER_NOTICE_KEY`、`GATEKEEPER_XATTR_COMMAND`、`hasSeenGatekeeperNotice()`、`markGatekeeperNoticeSeen()`、`shouldShowGatekeeperNotice(capabilities)`、`buildGatekeeperNoticeHtml(t)`(04-02)

**新组件内标识符**
- `stores/profile.js`: `capabilities` ref(04-01)
- `ProfileDialog.vue`: `engineOptions` computed、`engineSelectorLocked` computed(04-01)
- `ProfileList.vue`: `firefoxEngineVisible` computed、`isProfileStartBlocked(row)`(04-03)
- `SyncManager.vue`: `syncGate` computed **[本 plan]**、`arrangeGate` computed **[本 plan]**、首次引入 `useI18n` **[本 plan]**
- `AppSettings.vue`: `platformLimitsVisible` computed **[本 plan]**、`openGatekeeperGuide()` **[本 plan]**
- `App.vue`: `firefoxEngineVisible` computed、`isNavDisabled(key)`、`navDisabledReason(key)`、`maybeShowGatekeeperNotice()`(04-05)

**新 localStorage key**
- `oab:macos-gatekeeper-notice:v1`(04-02)

**新 CSS class**
- `.nav-item.disabled`(04-05)
- `.gatekeeper-notice-box`(ElMessageBox customClass;本 plan 与 04-05 共同引用,样式定义归 04-05)

**新 i18n key(zh-CN 与 en-US 各一份)**
- `platformLimits.engineLockedHint`(04-01)
- `platformLimits.{title,desc,windowsOnlyTag,windowsOnlyFallback,firefoxItem,windowItem,gatekeeperItem,startBlockedHint,reopenGatekeeper,kernelWindowsOnly}`、`syncer.{platformBannerTitle,platformBannerHint}`、`gatekeeper.{title,intro,stepsTitle,step1,step2,step3,step4,commandTitle,commandHint,settingsHint,confirm}`(04-02)
  本 plan 消费其中 12 条,不新增 key。

**修改的既有文件(非新符号)**
- `CLAUDE.md`(04-01)、`backend/_g.py`(仅 App.vue 摘要值,04-05)
</artifacts_produced>

<output>
Create `.planning/phases/04-frontend-platform-gating/04-04-SUMMARY.md` when done.
在 SUMMARY 中必须记录:(1) 同步类与排列类按钮的最终分派清单(哪些按钮走 `syncGate`、哪些走 `arrangeGate`、哪些不门控);(2) 平台限制卡片的最终文案(zh-CN 与 en-US 各一份原文),供 04-06 人工核对措辞是否点名具体功能而非否定整个平台。
</output>
