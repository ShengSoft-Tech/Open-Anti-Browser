---
phase: 04-frontend-platform-gating
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/lib/i18n-parity.test.js
  - frontend/src/i18n/zh-CN.js
  - frontend/src/i18n/en-US.js
  - frontend/src/lib/macosGatekeeperNotice.js
  - frontend/src/lib/macosGatekeeperNotice.test.js
autonomous: true
requirements: [UI-03, UI-04]

must_haves:
  truths:
    - "zh-CN 与 en-US 的完整嵌套 key 集合互为子集(两侧差集均为空),且该性质由一个可自动运行的测试持续守护(UI-03 / SC3 / SC4)。"
    - "i18n parity 测试递归比较 zh-CN 与 en-US 的完整嵌套 key 集合(非仅顶层),两侧差集必须均为空。"
    - "平台限制说明卡片引用的每个 i18n key 在两个 locale 下的值都是非空字符串。"
    - "i18n parity 比较基于 key 集合而非序列,对两个 locale 文件中 key 的书写顺序不敏感。"
    - "首次运行时 localStorage 无该 key,hasSeenGatekeeperNotice() 返回 false 并触发弹窗;key 存在但值为空字符串时同样视为未看过。"
    - "markGatekeeperNoticeSeen() 重复调用幂等;标记后再次启动不再弹出该弹窗。"
    - "localStorage 读写抛异常时(不可用/配额),首启提示辅助函数捕获异常并降级返回,不向上抛出未捕获异常阻断应用启动。"
    - "buildGatekeeperNoticeHtml(t) 产出的内容里,`xattr -dr com.apple.quarantine` 命令以字面形式出现且只指向单个已安装的应用 bundle 路径(D-04)。"
  artifacts:
    - frontend/src/lib/i18n-parity.test.js
    - frontend/src/lib/macosGatekeeperNotice.js
    - frontend/src/lib/macosGatekeeperNotice.test.js
  key_links:
    - "i18n-parity.test.js → zh-CN.js / en-US.js:SC3/SC4 要求的中英同步由这个测试自动强制,而不是靠人工比对。"
    - "macosGatekeeperNotice.js 的纯函数导出 → 04-05 的 App.vue 首启弹窗 与 04-04 的 AppSettings 重看按钮:两个消费方共用同一份文案构建函数,避免文案分叉。"
  prohibitions:
    - "macOS 放行与平台限制文案不得引导用户全局关闭 Gatekeeper,也不得对宽泛目录递归剥离 quarantine —— 命令必须限定到单个已安装的应用 bundle;文案不得把限制表述成「本应用不支持 macOS」,只能点名具体不可用的功能。"
    - "backend/_g.py 的开源声明完整性机制不得被削弱、绕过或移除;本 plan 不得以任何形式修改 frontend/src/lib/openSourceNotice.js,新首启提示必须使用独立文件与独立 localStorage key。"
---

<objective>
把本 phase 全部新增用户可见文案一次性落到 zh-CN 与 en-US 两份词典里,建立一个自动守护中英同步的 parity 测试,并交付 UI-04 首启放行提示所需的纯逻辑模块(localStorage 门 + 文案构建),供 wave 3/4 的两个消费方共用。

Purpose: SC3 与 SC4 都硬性要求「zh-CN 与 en-US 文案同步」,而本仓库此前没有任何 i18n 一致性检查(RESEARCH 已核实为零)。先把守护装上、再把 key 一次加齐,可以让 wave 3/4 三个并行 plan 直接消费成熟的 key,不必各自补文案、也不会互相抢改同两个词典文件。把 Gatekeeper 弹窗的内容构建做成不依赖 Element Plus 的纯函数,则让 04-05(首启)与 04-04(设置页重看)共用同一份文案,并让「命令必须逐字可复制」这条要求变成可自动断言的事实。

Output: `i18n-parity.test.js`、两个 locale 里新增的 23 个叶子 key、`macosGatekeeperNotice.js` 及其单测。本 plan 不改任何 `.vue` 文件。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-platform-gating/04-CONTEXT.md
@.planning/phases/04-frontend-platform-gating/04-PATTERNS.md
@.planning/phases/04-frontend-platform-gating/04-RESEARCH.md
@.planning/phases/04-frontend-platform-gating/04-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 i18n 中英 parity 自动守护</name>
  <files>frontend/src/lib/i18n-parity.test.js</files>
  <read_first>
    - `frontend/src/lib/proxyBypass.test.js`(全文 —— `node:test` + `node:assert/strict` 的项目写法;纯 `.js` 模块直接 ESM import,不做 SFC 编译)
    - `frontend/src/i18n/zh-CN.js`(第 1-60 行足以看清 `export default { section: { camelCaseLeaf: '文案' } }` 的嵌套结构)
    - `frontend/src/i18n/en-US.js`(同一位置,结构与 zh-CN 一一对应)
    - `frontend/src/i18n/index.js`(确认两个词典就是这两个模块的 default export,`fallbackLocale` 为 zh-CN)
    - `CLAUDE.md`(前端测试运行命令,已由 04-01 修正)
  </read_first>
  <action>
    新建 `frontend/src/lib/i18n-parity.test.js`,直接 ESM import 两个 locale 模块的 default export。

    写一个本地递归工具把嵌套对象拍平成完整点分路径的 key **集合**(例如 `settings.chromeTitle`),必须递归到叶子而不是只比顶层 section;数组值按叶子处理。比较基于集合(排序后比对或用 Set 差集),因此对两个文件中 key 的书写顺序完全不敏感。

    至少写三个用例:
    - zh-CN 相对 en-US 的差集为空,且 en-US 相对 zh-CN 的差集为空;断言失败时的消息要把缺失的具体路径打印出来,否则出问题时无从定位。
    - 两个 locale 的每个叶子值都是 string 且 `trim()` 后非空(杜绝为凑 parity 而塞空串)。
    - 两个 locale 的叶子总数相等且大于 0(当前基线为各 350 个,不要把 350 写死成期望值,只断言相等且非零)。

    注意本任务只建守护、不加任何 key。执行时两个词典已由 04-01 各加过一个 `platformLimits.engineLockedHint`,parity 应当天然成立,测试应直接通过 —— 若不通过,说明 04-01 漏加了某一侧,先修复该缺口再继续。
  </action>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test frontend/src/lib/i18n-parity.test.js` 退出码为 0,且至少报告 3 个通过用例。
    - `node --test frontend/src/lib/*.test.js` 退出码为 0(与既有 `proxyBypass.test.js`、`capabilitiesGating.test.js` 共存不互相干扰)。
    - `grep -c "en-US.js" frontend/src/lib/i18n-parity.test.js` 至少为 1;`grep -c "zh-CN.js" frontend/src/lib/i18n-parity.test.js` 至少为 1。
    - 人为在 `frontend/src/i18n/en-US.js` 临时删掉任意一个叶子 key 后重跑该测试会失败、且失败消息里出现被删 key 的完整点分路径(验证后恢复,`git diff frontend/src/i18n/en-US.js` 归零)。
    - 本任务不修改任何 `.vue` 或 i18n 文件:`git status --porcelain` 中仅出现 `frontend/src/lib/i18n-parity.test.js`。
  </acceptance_criteria>
  <done>存在一个可自动运行的测试,能在任一 locale 漏译时以明确的 key 路径失败;当前仓库状态下它通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 一次性补齐本 phase 全部中英双语文案</name>
  <files>frontend/src/i18n/zh-CN.js, frontend/src/i18n/en-US.js, frontend/src/lib/i18n-parity.test.js</files>
  <read_first>
    - `frontend/src/i18n/zh-CN.js`(第 308-337 行 `settings` 段与第 359 行起的 `syncer` 段 —— 新 key 要沿用同样的嵌套层级与 camelCase 叶子命名;04-01 已在 `settings` 之后新建了 `platformLimits` 段,本任务在该段内扩充)
    - `frontend/src/i18n/en-US.js`(同两处,保持与 zh-CN 完全一致的结构与顺序)
    - `frontend/src/lib/i18n-parity.test.js`(Task 1 产出,本任务要在其中追加必备 key 清单断言)
    - `.planning/phases/04-frontend-platform-gating/04-RESEARCH.md` § State of the Art(macOS Sequoia 之后正确的放行路径:先触发拦截 → 系统设置 → 隐私与安全性 → 仍要打开 → 输入密码;旧的「右键打开」写法已不可靠,不要采用)
    - `.planning/phases/04-frontend-platform-gating/04-CONTEXT.md` § Implementation Decisions(D-03 卡片是限制说明的永久家;D-04 首启弹窗内容构成)
  </read_first>
  <action>
    在 `frontend/src/i18n/zh-CN.js` 与 `frontend/src/i18n/en-US.js` 两个文件的相同位置,新增下列全部叶子 key(zh-CN 先写,en-US 逐条对应;en-US 必须是地道英文,不得留空、不得复制中文)。

    扩充已存在的 `platformLimits` 段,补齐这 10 个叶子(`engineLockedHint` 已由 04-01 加过,保留不动):
    `title` 卡片标题「平台限制说明」;`desc` 一句话说明当前系统上有哪些功能不可用;`windowsOnlyTag` 极短标签文案「仅 Windows」;`windowsOnlyFallback` 后端未提供 reason 时的兜底说明「该功能仅在 Windows 上可用」;`firefoxItem` 说明 Firefox 指纹内核没有 macOS 构建、因此新建配置只提供 Chrome、既有 Firefox 配置保留但不能启动;`windowItem` 说明窗口排列与窗口同步依赖 Windows 专有接口、在当前系统不可用;`gatekeeperItem` 说明本应用未做 Apple 签名、首次打开需要手动放行;`startBlockedHint` 列表里被禁用启动的 Firefox 配置的悬浮说明;`reopenGatekeeper` 「重新查看放行指引」按钮文案;`kernelWindowsOnly` 设置页 Firefox 内核卡片旁的「仅 Windows 可用」标注。

    扩充已存在的 `syncer` 段,补齐这 2 个叶子:`platformBannerTitle` 同步器视图顶部横幅的标题(例如「同步器在当前系统不可用」);`platformBannerHint` 一句指引,提示可到设置页的平台限制说明查看详情。注意横幅里那句具体原因不放在这里 —— 它由后端 `capabilities.window.sync.reason` 原文提供(D-02),这两个 key 只是它的包装文案。

    新增顶层 `gatekeeper` 段(放在 `platformLimits` 之后,两个文件位置一致),含这 11 个叶子:`title` 弹窗标题;`intro` 一句话说明本应用未做 Apple 开发者签名、首次打开会被系统拦截,这是预期行为;`stepsTitle` 步骤小标题;`step1` 先双击打开一次应用、看到「无法打开,因为无法验证开发者」的提示后点关闭;`step2` 打开「系统设置 → 隐私与安全性」,滚动到「安全性」区域;`step3` 找到关于 Open-Anti-Browser 的那条提示,点击「仍要打开」;`step4` 在弹出的确认框里再次点击「打开」并按提示输入登录密码;`commandTitle` 终端替代方案的小标题;`commandHint` 说明如果把应用装在别的位置需要把命令里的路径换成实际安装路径;`settingsHint` 说明这份指引之后可以在「设置 → 平台限制说明」里重新查看;`confirm` 弹窗确认按钮文案。

    文案红线:必须点名具体不可用的功能,不得写成「本应用不支持 macOS」之类会让用户误判整体可用性的表述(Phase 1 D-08 已就此立过规矩);不得出现引导用户全局关闭 Gatekeeper 的说法,也不得出现对宽泛目录递归剥离 quarantine 的做法 —— 终端命令由 Task 3 以模块常量形式提供并限定到单个应用 bundle,i18n 里只放它的标题与说明,不放命令本身。

    最后在 `i18n-parity.test.js` 里追加一个用例:声明一份本 phase 必备 key 路径清单(上述 10 + 2 + 11,加上 04-01 的 `platformLimits.engineLockedHint`,共 24 条),断言每条在两个 locale 下都存在且值为非空字符串。这条断言把「卡片引用的每个 key 在两个 locale 下都非空」变成可自动验证的事实。
  </action>
  <behavior>
    - 24 条必备 key 路径在 zh-CN 与 en-US 下都能取到值,且 `typeof value === 'string'` 且 `value.trim().length > 0`。
    - 通用 parity 用例(Task 1 的三条)在加完 key 后依然通过 —— 两侧差集仍为空,叶子总数仍相等。
    - 任一 locale 少加一条 key 时,必备清单用例失败并打印出缺失的完整点分路径。
    - 新增 key 不改变既有 350 个 key 的任何值(既有文案零回归)。
  </behavior>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
    <automated>node --input-type=module -e "import zh from './frontend/src/i18n/zh-CN.js';import en from './frontend/src/i18n/en-US.js';const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'&&!Array.isArray(v)?flat(v,p+k+'.'):[p+k]);const a=flat(zh).sort(),b=flat(en).sort();if(a.length!==b.length||a.some((k,i) => k!==b[i]))throw new Error('locale key sets differ');const need=['platformLimits.engineLockedHint','platformLimits.title','platformLimits.desc','platformLimits.windowsOnlyTag','platformLimits.windowsOnlyFallback','platformLimits.firefoxItem','platformLimits.windowItem','platformLimits.gatekeeperItem','platformLimits.startBlockedHint','platformLimits.reopenGatekeeper','platformLimits.kernelWindowsOnly','syncer.platformBannerTitle','syncer.platformBannerHint','gatekeeper.title','gatekeeper.intro','gatekeeper.stepsTitle','gatekeeper.step1','gatekeeper.step2','gatekeeper.step3','gatekeeper.step4','gatekeeper.commandTitle','gatekeeper.commandHint','gatekeeper.settingsHint','gatekeeper.confirm'];const miss=need.filter(k => !a.includes(k));if(miss.length)throw new Error('missing: '+miss.join(', '));console.log('all',need.length,'keys present; total leaves',a.length)"</automated>
  </verify>
  <acceptance_criteria>
    - 第二条命令退出码为 0 并打印 `all 24 keys present; total leaves 374`(350 基线 + 24 新增;若基线因其它改动变化,只要求打印的两个数字自洽且 24 条全在)。
    - `node --test frontend/src/lib/*.test.js` 退出码为 0。
    - `grep -c "gatekeeper:" frontend/src/i18n/zh-CN.js` 输出 1;`grep -c "gatekeeper:" frontend/src/i18n/en-US.js` 输出 1。
    - `grep -c "platformBannerTitle" frontend/src/i18n/zh-CN.js` 与 `grep -c "platformBannerTitle" frontend/src/i18n/en-US.js` 均输出 1。
    - `git diff --stat frontend/src/i18n/zh-CN.js frontend/src/i18n/en-US.js` 显示两个文件的新增行数相等(结构 1:1 对应)。
    - `git diff -U0 frontend/src/i18n/zh-CN.js | grep '^-' | grep -v '^---' | wc -l` 输出 0(只新增、不改动既有文案)。
    - 两个 locale 的 `gatekeeper.step2`/`step3` 描述的是系统设置里的「隐私与安全性 / Privacy & Security」路径,而不是旧的右键打开写法。
  </acceptance_criteria>
  <reversibility rating="reversible">纯文案新增,不改既有 key,回退即删除新增段落。</reversibility>
  <done>本 phase 需要的 24 条文案在两个 locale 下齐备且非空,parity 测试与必备清单断言全绿,既有 350 条文案一字未改。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gatekeeper 首启提示的纯逻辑模块(独立 key、独立文件、异常降级)</name>
  <files>frontend/src/lib/macosGatekeeperNotice.js, frontend/src/lib/macosGatekeeperNotice.test.js</files>
  <read_first>
    - `frontend/src/lib/openSourceNotice.js`(**只读,严禁修改** —— 被 `backend/_g.py:17` 哈希锁定;这里只是参考「首启一次 + localStorage 记忆」这个形状,不要照搬它的 base64 混淆,那是反篡改机制不是通用写法)
    - `frontend/src/App.vue` 第 459-489 行 `_0x31ab()`(既有首启弹窗的调用形状:`ElMessageBox.alert(html, title, { dangerouslyUseHTMLString: true, ... })`;新模块要产出的就是这里的 html 参数)
    - `frontend/src/lib/proxyBypass.test.js`(测试写法)
    - `frontend/src/lib/capabilitiesGating.js`(04-01 产出;保持同样的 `export function xxx(` 具名导出风格)
    - `.planning/phases/04-frontend-platform-gating/04-RESEARCH.md` § Code Examples 末段(用户面向的 xattr 命令指向的是外层 `.app` bundle,与 `backend/services/chrome.py` 里给内核剥离 quarantine 的钩子是两回事,文案里不要混淆)
  </read_first>
  <action>
    新建 `frontend/src/lib/macosGatekeeperNotice.js`。该模块**不得 import element-plus 或任何 Vue 运行时**,以便被 `node:test` 直接 import 测试。全部用具名 `export function` / `export const` 声明。

    导出两个常量:
    - `GATEKEEPER_NOTICE_KEY` = `oab:macos-gatekeeper-notice:v1` —— 与既有的开源声明首启 key 完全独立、互不干扰(D-04)。
    - `GATEKEEPER_XATTR_COMMAND` —— 逐字的终端命令,内容是 `xattr -dr com.apple.quarantine` 加上一个指向单个已安装应用 bundle 的路径(`/Applications/Open-Anti-Browser.app`)。命令必须限定到这一个 bundle:不得指向 `~/Downloads`、`/Applications` 之类的宽泛目录,也不得引导用户全局关闭 Gatekeeper。把它做成模块常量而不是 i18n 文案,是为了保证中英两版展示的是同一份逐字命令、且可被单测断言。

    导出三个 localStorage 相关函数,三个都必须把 `localStorage` 访问包在 try/catch 里 —— 桌面壳或隐私模式下 localStorage 可能不可用或写满配额,任何未捕获异常都会打断 `App.vue` 的 `onMounted`、进而阻断整个应用启动:
    - `hasSeenGatekeeperNotice()` —— 读不到 key、值为空字符串、或读取抛异常时,一律返回 false(视为没看过)。只有读到明确的已看过标记值(`'1'`)才返回 true。
    - `markGatekeeperNoticeSeen()` —— 写入标记值;抛异常时静默吞掉,不向上抛。重复调用幂等:多次调用后 `hasSeenGatekeeperNotice()` 仍返回 true,存储里也只有一个 key。
    - `shouldShowGatekeeperNotice(capabilities)` —— 返回 `capabilities?.platform === 'darwin' && !hasSeenGatekeeperNotice()`。用「等于 darwin」而不是「不等于 win32」:capabilities 还没解析时 platform 是 undefined,此时不应该弹一个 macOS 专属弹窗;这也保证 Windows 上恒为 false、零回归。不得从浏览器提供的平台字符串或 user-agent 推导平台(D-00)。

    再导出一个纯函数 `buildGatekeeperNoticeHtml(t)`,入参是 vue-i18n 的翻译函数,返回弹窗要用的 HTML 字符串。它按 `gatekeeper.intro` → `gatekeeper.stepsTitle` → `gatekeeper.step1..step4`(有序列表)→ `gatekeeper.commandTitle` → `GATEKEEPER_XATTR_COMMAND`(放在 `<code>` 元素里,逐字输出)→ `gatekeeper.commandHint` → `gatekeeper.settingsHint` 的顺序拼装。安全红线:除了 `t()` 取出的开发者自撰文案与这个模块常量之外,**不得把任何来自 API、用户输入或 store 的动态值插进这个 HTML 字符串** —— 它会以 `dangerouslyUseHTMLString` 方式注入 DOM,插入动态值即构成 XSS 通路。函数本身不得读写 localStorage、不得有副作用。

    新建 `frontend/src/lib/macosGatekeeperNotice.test.js`。Node 没有浏览器 `localStorage`,需要自己在 `globalThis` 上装一个最小桩:一个用 Map 实现的 `getItem`/`setItem`/`removeItem`,并额外提供一个「所有方法都抛异常」的桩用于降级用例;每个用例前重置。覆盖 behavior 块列出的全部情形。
  </action>
  <behavior>
    - 桩存储为空时 `hasSeenGatekeeperNotice()` 返回 false。
    - key 存在但值为空字符串时 `hasSeenGatekeeperNotice()` 返回 false(视为未看过)。
    - `markGatekeeperNoticeSeen()` 之后 `hasSeenGatekeeperNotice()` 返回 true。
    - 连续调用 `markGatekeeperNoticeSeen()` 三次后 `hasSeenGatekeeperNotice()` 仍为 true,且桩存储中该 key 只有一条、值不变(幂等)。
    - 换成「所有方法都抛异常」的桩时,`hasSeenGatekeeperNotice()` 返回 false 且不抛,`markGatekeeperNoticeSeen()` 不抛,`shouldShowGatekeeperNotice({platform:'darwin'})` 不抛。
    - `shouldShowGatekeeperNotice({platform:'darwin'})` 在未标记时为 true、标记后为 false。
    - `shouldShowGatekeeperNotice({platform:'win32'})`、`shouldShowGatekeeperNotice({})`、`shouldShowGatekeeperNotice(undefined)`、`shouldShowGatekeeperNotice(null)` 均为 false 且不抛。
    - `GATEKEEPER_NOTICE_KEY` 严格等于 `oab:macos-gatekeeper-notice:v1`,且不等于既有的开源声明 key。
    - `buildGatekeeperNoticeHtml(k => k)`(用恒等函数当 t)返回的字符串包含 `GATEKEEPER_XATTR_COMMAND` 的逐字内容,且包含 `gatekeeper.step1` 到 `gatekeeper.step4` 四个 key 名(证明四个步骤都被引用)。
    - `GATEKEEPER_XATTR_COMMAND` 以 `xattr -dr com.apple.quarantine ` 开头,其后的路径以 `.app` 结尾,且整串不含 `spctl`、不含 `sudo`。
    - `buildGatekeeperNoticeHtml` 连续两次调用返回相同字符串,且调用前后桩存储内容未变(无副作用)。
  </behavior>
  <verify>
    <automated>node --test frontend/src/lib/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test frontend/src/lib/macosGatekeeperNotice.test.js` 退出码为 0,通过用例数不少于 10。
    - `node --test frontend/src/lib/*.test.js` 退出码为 0。
    - `grep -c "oab:macos-gatekeeper-notice:v1" frontend/src/lib/macosGatekeeperNotice.js` 输出 1。
    - `grep -cE "^export (function|const) " frontend/src/lib/macosGatekeeperNotice.js` 输出 6(两个常量 + 四个函数)。
    - `grep -c "element-plus" frontend/src/lib/macosGatekeeperNotice.js` 输出 0(模块保持可被 node:test 直接 import)。
    - `git status --porcelain frontend/src/lib/openSourceNotice.js` 无输出(该文件一字未改)。
    - `python3 -m backend._g --mode build` 退出码为 0。
  </acceptance_criteria>
  <reversibility rating="reversible">全新独立文件 + 独立 localStorage key,不触碰任何被锁定资产;回退即删文件。</reversibility>
  <done>首启门控与文案构建全部以纯函数形式就绪并被单测锁定;localStorage 不可用时静默降级;逐字终端命令限定到单个应用 bundle 且可被断言。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 开发者自撰 i18n 文案 → `dangerouslyUseHTMLString` 弹窗 DOM | `buildGatekeeperNoticeHtml` 的返回值会被以原始 HTML 注入,任何动态值都会成为注入面 |
| 浏览器 localStorage → 应用启动流程 | 首启门控在 `onMounted` 里同步读写存储,存储不可用会传播成启动失败 |
| 应用内文案 → 用户在自己机器上执行的终端命令 | 文案直接指导用户执行破坏性/降低系统防护的操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | `buildGatekeeperNoticeHtml(t)` → ElMessageBox HTML | high | mitigate | 内容 100% 来自开发者自撰 i18n 文案与本模块常量,禁止插入任何 API/用户/store 提供的动态值;单测以恒等 `t` 断言输出成分。与既有开源声明弹窗依赖的是同一条安全性质。 |
| T-04-05 | Denial of Service (本机) | `hasSeenGatekeeperNotice` / `markGatekeeperNoticeSeen` | medium | mitigate | 三个存储函数全部 try/catch 降级;单测用「全抛异常」桩验证不向上抛,保证 `App.vue` 的 `onMounted` 不被打断。 |
| T-04-06 | Elevation of Privilege(用户被诱导降低本机防护) | Gatekeeper 放行文案与终端命令 | high | mitigate | 命令做成模块常量并限定到单个 `.app` bundle;单测断言其不含 `sudo`、不含 `spctl`、路径以 `.app` 结尾;文案禁止出现全局关闭 Gatekeeper 的引导。 |
| T-04-07 | Tampering | `backend/_g.py` 完整性锁 | high | mitigate | 本 plan 不触碰 `App.vue` 与 `openSourceNotice.js`;验收项含 `python3 -m backend._g --mode build` 退出 0 与 `openSourceNotice.js` 无 diff。 |
| T-04-SC | Tampering | 依赖供应链 | low | accept | 本 plan 不执行任何 npm/pip/cargo 安装,无新增第三方包,包合法性门禁不适用。 |
</threat_model>

<verification>
1. `node --test frontend/src/lib/*.test.js` 全绿(proxyBypass + capabilitiesGating + i18n-parity + macosGatekeeperNotice)。
2. 24 条必备 i18n key 在两个 locale 下齐备且非空;两侧 key 集合差集为空。
3. `git status --porcelain frontend/src/lib/openSourceNotice.js` 与 `git status --porcelain frontend/src/App.vue` 均无输出。
4. `python3 -m backend._g --mode build` 退出码 0。
5. `git diff --name-only` 中不出现任何 `.vue` 文件 —— 本 plan 纯词典与纯逻辑,不碰组件。
</verification>

<success_criteria>
- 存在一个能在任一 locale 漏译时以明确 key 路径失败的自动测试。
- 本 phase 全部 24 条新增文案在 zh-CN 与 en-US 下齐备、非空、结构 1:1;既有 350 条一字未改。
- `macosGatekeeperNotice.js` 提供独立 key 的首启门控、异常降级、以及不含动态值的文案构建函数,全部被单测覆盖。
- 终端命令逐字限定到单个已安装应用 bundle,不引导用户全局降低系统防护。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

本 phase(04)新建的全部符号。本 plan 产出的项以 **[本 plan]** 标注。

**新文件**
- `frontend/src/lib/capabilitiesGating.js`(04-01)
- `frontend/src/lib/capabilitiesGating.test.js`(04-01)
- `frontend/src/lib/i18n-parity.test.js` **[本 plan]**
- `frontend/src/lib/macosGatekeeperNotice.js` **[本 plan]**
- `frontend/src/lib/macosGatekeeperNotice.test.js` **[本 plan]**

**新导出函数 / 常量**
- `isFirefoxEngineAvailable(capabilities)`、`visibleEngineOptions(baseOptions, capabilities, currentEngine)`、`isEngineSelectorLocked(capabilities, currentEngine)`、`getWindowFeatureGate(capabilities, feature)`(04-01)
- `GATEKEEPER_NOTICE_KEY` **[本 plan]**
- `GATEKEEPER_XATTR_COMMAND` **[本 plan]**
- `hasSeenGatekeeperNotice()` **[本 plan]**
- `markGatekeeperNoticeSeen()` **[本 plan]**
- `shouldShowGatekeeperNotice(capabilities)` **[本 plan]**
- `buildGatekeeperNoticeHtml(t)` **[本 plan]**

**新组件内标识符**
- `stores/profile.js`: `capabilities` ref(04-01)
- `ProfileDialog.vue`: `engineOptions` computed、`engineSelectorLocked` computed(04-01)
- `ProfileList.vue`: `firefoxEngineVisible` computed、`isProfileStartBlocked(row)`(04-03)
- `SyncManager.vue`: `syncGate` computed、`arrangeGate` computed、首次引入 `useI18n`(04-04)
- `AppSettings.vue`: `platformLimitsVisible` computed、`openGatekeeperGuide()`(04-04)
- `App.vue`: `firefoxEngineVisible` computed、`isNavDisabled(key)`、`navDisabledReason(key)`、`maybeShowGatekeeperNotice()`(04-05)

**新 localStorage key**
- `oab:macos-gatekeeper-notice:v1` **[本 plan]**(与既有 `oab:first-use-notice:v2` 并列、互不干扰)

**新 CSS class**
- `.nav-item.disabled`、`.gatekeeper-notice-box`(04-05)

**新 i18n key(zh-CN 与 en-US 各一份)**
- `platformLimits.engineLockedHint`(04-01)
- `platformLimits.{title,desc,windowsOnlyTag,windowsOnlyFallback,firefoxItem,windowItem,gatekeeperItem,startBlockedHint,reopenGatekeeper,kernelWindowsOnly}` **[本 plan]**
- `syncer.{platformBannerTitle,platformBannerHint}` **[本 plan]**
- `gatekeeper.{title,intro,stepsTitle,step1,step2,step3,step4,commandTitle,commandHint,settingsHint,confirm}` **[本 plan]**

**修改的既有文件(非新符号)**
- `CLAUDE.md`(04-01)、`backend/_g.py`(仅 App.vue 摘要值,04-05)
</artifacts_produced>

<output>
Create `.planning/phases/04-frontend-platform-gating/04-02-SUMMARY.md` when done.
在 SUMMARY 中必须记录:(1) 24 条 key 的完整路径清单(04-03/04-04/04-05 直接照此消费,不要另起 key);(2) `macosGatekeeperNotice.js` 的完整导出 API;(3) `GATEKEEPER_XATTR_COMMAND` 的逐字内容。
</output>
